LMC DINT packet map
Status 2026-08-07: delivery C# and the tracked canonical TCPMotionInterface have
request/public/handler paths for all 23 captured commands plus two project-local
motion extensions, 0x204A and 0x204B. LASAL-local Admin v1 also defines
0x7D22 GroupMoveLinearRelative. The 0x7D12 SetAxisPosition wire remains dormant.
The project-specific Home name is LMC_Home. Its dedicated durable triad is
0x7D13 StartLmcHome, 0x7D18 ReadLmcHomeOutcome and 0x7D19
RetireLmcHomeOutcome. 0x7D13 is a 56-byte current-position-zero request. It
contains no direction, search velocity, backoff, travel, Home-switch or
limit-switch field and must not call MoveReference.
The C# contract also reserves read-only 0x7D14 ReadAxisSetPositionOutcome; the
current PLC has no retained store or route and does not advertise feature bit 5.
The 0x7D15 StartAxisDs402Home, read-only 0x7D16
ReadAxisDs402HomeOutcome, and idempotent 0x7D17
RetireAxisDs402HomeOutcome contract accepts only DS402 method 37, Home offset 0,
zero velocity/acceleration/distance/torque and Aborting mode. It performs no
switch search and no intentional physical motion. Admin feature bits 4 and 6
must track their dedicated source gates and complete triads atomically. Advertising
them proves protocol availability, not PLC download or physical-axis validation.
The 10:02:20 LASAL save regression that emptied three public RT InputLatch
DS402 Home methods was recovered exactly from the 09:33 pre-save blob. Current
SourceOnly passes the InputLatch owner-token mailbox assertion. A fresh LASAL
F9 build, implementation-index smoke and PLC runtime proof are still required.
LASAL-DINT Diagnostics v1 additionally reserves the dedicated encoder-maintenance
triad 0x7E53 Start, 0x7E54 Outcome and 0x7E55 Retire. D0-D3, D4 single-bank Ring/Trigger,
the D5 general-inline SDO Read ticket flow, fail-closed SDO Write infrastructure,
and the dormant EtherCAT node-health/digital-I/O read owner are implemented.
D4 Double bank, PI Write, digital-output Write, and extended SDO results remain
off. Axis 1 exact 0x2F00:24 Int32/4 SDO Write is source/policy active, but Axis
2..4 remain blocked and no current PLC Write has been proven.
The dedicated Home and encoder-maintenance extensions are project-local and are
not captured Elmo commands. Do not infer their availability from an older command
inventory count; use the paired capability bits and exact command routes.
The five previously
blocked group commands 0x2049, 0x2085, 0x20A4, 0x2051 and 0x20E7 are active in
source. Current C# Debug/Release forced Rebuild counts are recorded in
AUTOMATED_TESTS_2026-07-10.md. The expansion
covers actual fake-TCP output submission paths, twenty mutation-journal
persistence/reopen/corruption/lock/process-termination/parent-EOF tests, a five-case CREVIS
status-cause matrix, three deterministic topology/D5/recoverable-Recorder parser
property tests, and the strict opt-in eight-family parser-stress CLI contract,
and a listener-lifetime
generation fence that prevents a late old callback-handler failure from leaking
into a replacement RPC session. A fixed Release seed
completed 100,000 bounded in-memory mutations without file, network, or PLC I/O.
The current development WPF build and actual-control smoke suite pass their latest
Debug/Release forced Rebuild baseline recorded in AUTOMATED_TESTS_2026-07-10.md.
They include configured CREVIS auto-load, local
SDO draft editing while an ordinary submission is in flight, a current-session/
build/boot/map exact-target proof gate before manual SDO Write, and suppression of invalid PI/Bulk stale
raw values. The process-level smoke observes SDO, digital-output, and active
Double-bank journal recovery without 0x7E50/0x7E53/0x7E23/0x7E40..0x7E4F replay and
preserves byte-identical journals across forced exit and restart. A locked
Double-bank journal also fails new mutation admission closed. The live Double
qualification/recovery gate remains closed. The current-position-zero Home,
method-37 DS402 Home and dedicated encoder-maintenance changes require a fresh
LASAL F9 build, implementation-index smoke, PLC download and runtime proof. Any
earlier switch-search Home, DS402 method 35 or generic-0x7E50 TW20 result does not
prove this contract. The Test2 capture proves
the static topology wire path with
capability 0x0000613F, topology revision 0x15867EEC, one 0x7E11 response, and seven
0x7E12 responses for the exact configured inventory. That historical capture did
not send 0x7E13/0x7E22/0x7E23 and does not prove dynamic node-health or digital-I/O.
The current source now implements and routes 0x7E13/0x7E22 over a coherent
464-byte snapshot with wired CREVIS coupler/input/output clients, but capability
bits 15 and 16 remain zero. 0x7E23 remains absent and bit 17 remains zero.
The PC-only same-value SDO Write qualification uses four distinct tickets for
baseline Read, unchanged-value pre-Write guard Read followed by the final second safety check,
one byte-identical Write, and guarded exact Readback. The current PLC/SDK source
gate and SDK allowlist admit only Axis 1 exact 0x2F00:24 Int32/4; Axis 2..4 and
every non-exact target remain zero-wire. Manual Write uses an identity-pinned SDK
mutation gate. It reads fresh
capabilities inside the serialized mutation path and requires the proof-bound session,
DiagnosticsBuild, BootId, MapRevision, and exact target; a mismatch is NotAttempted and
sends no 0x7E50 frame. Any observed identity mismatch or disconnect permanently revokes
that process-local proof, including an A-to-B-to-A observation sequence.
No current PLC/live SDO Write has been
proven.
The UI-independent D5 disconnect/orphan qualification core has twenty-eight
deterministic PC regressions and a production WPF application-recovery adapter.
The adapter performs a local TCP zero-linger close without RPC Close 0x405D,
opens a distinct connection, binds fresh capability samples to that owner and
session, and requires two exact 0x6061:0 Int8 recovery tickets plus a final stable
capability sample. Stability includes BootId, MapRevision, DiagnosticsBuild,
CapabilityBits, BaseCycleTimeUs, MaxSdoDataBytes, and request/response payload limits.
An old executor ResourceBusy is retried every 25 ms only when the Submit is an exact
Rejected/ResourceBusy result, within a monotonic retry-admission budget of the request
timeout plus 5 seconds capped at 120 seconds. That budget does not claim to bound an
already-started RPC. The PASS log commits before quarantine clear;
then the GUI adopts the new connection and auto-loads CREVIS topology. Running and
Queued observations both remain ApplicationRecoveryOnly with orphanQualified=false.
A durable PLC MarkOrphan/executor-token/late-callback witness and live PLC/pcap are
still required for orphan qualification.
On 2026-07-23 the live PLC captures passed Admin 0x7D00/0x7D10/0x7D20, 0x7D22
Aborting/Buffered acceptance and per-axis XYZU mapping, dynamic group monitoring,
PowerOff final status, axis 1..4 drive reads, the four-entry PI/Bulk happy path, and
D5 general-inline 1/2/4-byte Read plus same-BootId TypeMismatch recovery.
Fault/stale/queue-race matrices, CPU core/priority placement and broader packet
recapture are still pending. The untracked
_Edit variant is not canonical. See API_DEVELOPMENT_BACKLOG_2026-07-10.md and
../../../docs/architecture/LMC_DIAGNOSTICS_INTERNAL_PLC_TEST_GUIDE_2026-07-21.md.

All numeric motion fields below are signed 32-bit little-endian LASAL internal DINT.
The API writes caller-provided DINT values directly to the packet.
Caller code must multiply the physical value by the selected LMC_Units constant,
round/check the DINT range, and pass the converted int to the motion API.
Read APIs return raw LASAL DINT; caller code divides by the same UNIT for display.
Current saved _LMCAxis1..9 profile (confirmed in Motion_Network):
  IntUnits      1 mm; UNIT MM = 10000
  VMax          75 mm
  AMax          7500 mm
  MoveType      _JERK_PROFILE
  JMax          75000 mm
  position      value * MM       (10000)
  velocity      value * MM       (10000), because MoveAbsolute expects app units/s
  acceleration  value * MM       (10000)
  deceleration  value * MM       (10000)
  jerk          (physical axis-unit/s^3 / 1000) * MM
RPM is not the MoveAbsolute velocity UNIT unless the PLC method explicitly
declares an RPM input. unit.h has no dedicated jerk UNIT; use the selected axis
application-unit constant after applying the _LMCAxis /1000 definition. Nonzero
Jerk affects _JERK_PROFILE only. Verify the downloaded PLC MoveType/JMax before
live motion.
Current saved _LMCRobotBase1 group profile:
  MoveType      _JERK_PROFILE
  JMax          50000 mm
Nonzero group Jerk is ignored unless the robot profile is _JERK_PROFILE. Verify
the downloaded robot MoveType/JMax before GroupStop or MoveLinear testing.
See UNIT_CONVERSION_MANUAL_2026-07-10.md.
LASAL project-local header: CommandId UINT[0], Reserved UINT[2] = 0,
PayloadLength UINT[4], Reference UINT[6]. This is not the native MMCLib RPC
header; native captures use the offset-2 field as a request numerator/response echo.

LASAL-local Admin v1 common request payload:
P0 SchemaVersion UINT16 = 1
P2 Flags UINT16 = 0
P4 RequestId UDINT, nonzero

Admin common response payload:
P0  SchemaVersion UINT16 = 1
P2  ResponseFlags UINT16 = 0
P4  CommandStatus UINT16, 0 success / 1 domain error
P6  ErrorId INT16, domain error is -31000
P8  Echoed RequestId UDINT
P12 DetailCode UDINT: 1 schema, 2 flags, 3 request ID, 4 reference,
    5 payload shape, 6 parameter key, 7 client, 8 selection mask,
    9 motion parameters, 10 invalid motion state, 11 native command rejected,
    12 nonzero velocity, 13 active axis error,
    14 invalid SetPosition safety configuration,
    15 failed coordinate precondition,
    16 diagnostics build mismatch, 17 diagnostics BootId mismatch,
    18 diagnostics map revision mismatch, 19 SetPosition outcome not found,
    20 SetPosition outcome indeterminate, 21 outcome store corrupt,
    22 outcome exact-key mismatch, 23 outcome slot occupied,
    24 outcome storage unavailable, 25 DS402 Home outcome not found,
    26 DS402 Home outcome indeterminate, 27 DS402 Home store corrupt,
    28 DS402 Home exact-key mismatch, 29 DS402 Home storage unavailable,
    30 DS402 Home runtime execution failed, 31 DS402 Home aborted,
    32 DS402 Home outcome slot occupied,
    33 LMC_Home outcome not found, 34 LMC_Home outcome indeterminate,
    35 LMC_Home store corrupt, 36 LMC_Home exact-key mismatch,
    37 LMC_Home storage unavailable, 38 LMC_Home execution failed,
    39 LMC_Home aborted, 40 LMC_Home outcome slot occupied,
    41 axis ownership conflict, 42 axis ownership quarantined.
For detail 1..10, 12..15, and 16..24 ErrorId is -31000. Detail 11 preserves a positive representable
LASAL GroupProfile error in ErrorId or uses adapter fallback -6; interpret positive
values only in the GroupProfile domain. SetAxisPosition requires ErrorId=-6 for
detail 11 and preserves the full native command-state bitfield in its
command-specific response extension. LMC_Home start admits only command-specific
details 10, 13, 15, 16, 17, 18, 40, 41, and 42 in addition to the common
envelope details. Detail 11 is not valid for LMC_Home start: execution failures
are published in its retained outcome instead of claiming a native rejection in
the start ACK.
Details 25..29 are 0x7D16 query or 0x7D17 retire-domain failures with
ErrorId=-31000. Details 30..31 occur only inside a successfully queried retained
DS402 Home record. Detail 32 is a 0x7D15 start-admission failure with
ErrorId=-31000; it means the same axis has an unretired terminal outcome.
Details 33..37 are 0x7D18/0x7D19 outcome-domain failures. The exact LMC_Home
query/retire failure allow-list is 16, 17, 18, and 33..37. Details 38..39 occur
inside a successfully queried retained LMC_Home record. Detail 40 is a 0x7D13
start-admission failure. Details 41..42 are common axis-owner admission failures.

0x7D00 GetAdminCapabilities, request payload 8 bytes, Reference=0.
Success response payload is exactly 40 bytes:
P16 FeatureBits U32 base = 0x00000007 (axis/group parameter read and group relative move)
    Bit 4 AxisHome follows LMC_ADMIN_AXIS_HOME_ENABLED and is added only with the
    complete 0x7D13/18/19 implementation.
    Bit 6 AxisDs402Home follows LMC_DIAG_DS402_HOME_ENABLED and is added only with
    the complete 0x7D15/16/17 implementation.
P20 AxisParameterMask U32 = 0x0000003F
P24 GroupParameterMask U32 = 0x00000007
P28 PhysicalAxisCount U16 = 4
P30 MaxAxisParameterCount U16 = 1
P32 MainGroupReference U16 = 0x0100
P34 MaxGroupParameterCount U16 = 3
P36 ErrorCatalogVersion U16: base 1; at least 4 with bit 6 and at least 5 with bit 4
P38 Reserved U16 = 0
FeatureBits 0x00000007 requires the matching Phase 2 PC DLL. Older strict clients
that know only bits 0..1 reject bit 2, so deploy the PLC and PC API as a pair.

0x7D10 ReadAxisParameter, request payload 12 bytes, Reference=axis 1..4:
P8 ParameterKey U16, P10 Reserved U16 = 0.
Keys are project semantic IDs, not native MotionLib enum values:
1 SoftwareMinPosition, 2 SoftwareMaxPosition,
3 EndPositionToleranceWindow, 4 MaxVelocity, 5 MaxAcceleration,
6 ReferencePosition. Values use application units.
Success response payload is exactly 28 bytes:
P16 ParameterKey U16, P18 ValueType U16 = 1 (Int32),
P20 Unit U16 (1 app units, 2 app units/s, 3 app units/s^2),
P22 Reserved U16 = 0, P24 Value DINT.
Software limits use ReadSWEndPos application-unit modes. Key 3 uses
LMCAXIS_PAR_RD_SWLIMWINDOW; it is the axis end-position tolerance window and is
not the robot ProfileInPosition window.

0x7D12 SetAxisPosition, request payload 48 bytes, Reference=axis 1..4:
P8  ExpectedDiagnosticsBuild U32, nonzero, pinned fresh 0x7E00 identity
P12 ExpectedDiagnosticsBootId U32, nonzero
P16 ExpectedMapRevision U32, nonzero
P20 ClientIntentId0 U32
P24 ClientIntentId1 U32
P28 ClientIntentId2 U32
P32 ClientIntentId3 U32; the complete 128-bit intent must not be all zero
P36 TargetPosition DINT [application units]
P40 ExpectedActualPosition DINT [application units], exact stale-read CAS
P44 ExecuteToken U32 = 0x50544553, little-endian bytes 53 45 54 50 (`SETP`)
The PC API exposes only semantic mode 1, ActualAndDestinationApplicationUnits.
It never sends the native MotionLib enum value. The prepared command pins owner,
session, diagnostics identity, four raw U32 intent words, axis, target, expected
actual position and capability evidence, and may
cross the TCP write boundary at most once. Post-boundary loss is outcome-uncertain
and must not be replayed after reconnect.
OriginalRequestId at common P4 restarts per LMCAdmin instance and is not an
authoritative duplicate-suppression key by itself. The four raw intent words are
CSPRNG-generated before preparation. Do not expose Guid.ToByteArray mixed-endian
layout as a wire semantic.
Success response payload is exactly 28 bytes:
P16 AppliedPosition DINT, P20 SemanticMode U16 = 1,
P22 Reserved U16 = 0, P24 NativeCommandState U32.
Success requires AppliedPosition=TargetPosition and NativeCommandState=0. A
pre-native validation failure requires both extension values to be 0. Detail 11
NativeCommandRejected requires ErrorId=-6, AppliedPosition=0 and nonzero
NativeCommandState; the SDK preserves that full bitfield in its typed rejection
context. A positive/other ErrorId or any other contradictory
correlated response is malformed/outcome-uncertain and faults the exact session.
Current PLC FeatureBits stays 0x00000007, so bit 3 is not advertised. The current
0x7D12 handler is intentionally fail-closed and calls no native SetPosition method.
Activation requires an IDE-created two-bank retained outcome store, read-only
0x7D14 exact query, separate terminal-retirement CAS, a separate
application-approved SetPositionMaxJump, verified same-core/equal-or-lower-priority
task placement, runtime state/ownership guards, IDE build/download, and PLC/pcap
proof. LMCAXIS_PAR_RD_SWLIMWINDOW is the software end-position standstill tolerance
and must not be reused as the jump cap.

0x7D14 ReadAxisSetPositionOutcome, request payload 48 bytes,
Reference=original physical axis 1..4:
P8  ExpectedDiagnosticsBuild U32, nonzero
P12 ExpectedDiagnosticsBootId U32, nonzero
P16 ExpectedMapRevision U32, nonzero
P20 OriginalRequestId U32
P24 ClientIntentId0 U32
P28 ClientIntentId1 U32
P32 ClientIntentId2 U32
P36 ClientIntentId3 U32
P40 TargetPosition DINT
P44 ExpectedActualPosition DINT
Common P4 is a new nonzero QueryRequestId. Semantic mode is fixed to 1 for v1.
An exact terminal record returns exactly 84 payload bytes:
P16 RecordState U16, 2 Succeeded / 3 Rejected
P18 SemanticMode U16 = 1
P20 DiagnosticsBuild U32
P24 DiagnosticsBootId U32
P28 MapRevision U32
P32 OriginalRequestId U32
P36/P40/P44/P48 ClientIntentId0..3 U32
P52 AxisReference U16, P54 Reserved U16 = 0
P56 TargetPosition DINT, P60 ExpectedActualPosition DINT
P64 AppliedPosition DINT
P68 OriginalCommandStatus U16
P70 OriginalErrorId I16
P72 OriginalDetailCode U32
P76 NativeCommandState U32
P80 RecordGeneration U32, nonzero and no wrap
Succeeded requires original status/error/detail/native state 0 and
AppliedPosition=TargetPosition. Rejected requires original status 1 and the same
pre-native/native-reject value combinations as 0x7D12. NotFound, identity mismatch,
Armed/Indeterminate, corrupt, or unavailable storage returns only the 16-byte common
failure and never proves that the mutation was not dispatched. The query is
read-only and never replays SetPosition.
Feature bit 5 is AxisSetPositionOutcomeRead. It may be advertised before bit 3,
but strict clients require bit 3 AxisSetPosition to imply bit 5. Details 16..24
require ErrorCatalogVersion 2 in the paired activation. The current PLC remains
FeatureBits 0x00000007, ErrorCatalogVersion 1, has no 0x7D14 route/store, and does
not advertise either bit 3 or bit 5.

0x7D13 StartLmcHome, request payload 56 bytes, Reference=axis 1..4:
P8  ExpectedDiagnosticsBuild U32, nonzero
P12 OriginalDiagnosticsBootId U32, nonzero
P16 ExpectedMapRevision U32, nonzero
P20/P24/P28/P32 ClientIntentId0..3 U32, not all zero
P36 SemanticMode U16 = 1 CurrentPositionZero
P38 Reserved U16 = 0
P40 ExpectedActualPosition DINT [application units], exact stale-read CAS
P44 TargetPosition DINT = 0
P48 TimeoutMilliseconds U32, 100..5000
P52 ExecuteToken U32 = 0x454D4F48, little-endian `HOME`
The wire contains no recipe, direction, search/backoff velocity, acceleration,
jerk, travel, Home-switch or limit-switch field. The implementation must use the
RT InputLatch mailbox and `_LMCAxis.SetPosition` with current-position-zero
semantics; `MoveReference` is forbidden. The same-core/equal-or-lower-priority
MotionLib rule remains a deployment requirement.
The success response payload is exactly 24 bytes:
P16 EchoedSemanticMode U16 = 1, P18 Reserved U16 = 0,
P20 NativeCommandState U32 = 0.
Success is start acceptance only. The prepared command crosses the TCP write
boundary at most once and is never replayed after an uncertain result.
The correlated start-failure allow-list is detail 10, 13, 15, 16, 17, 18, 40,
41, or 42. Detail 11 NativeCommandRejected is malformed for LMC_Home and faults
the exact session because this path has no native motion-command rejection ACK.

0x7D18 ReadLmcHomeOutcome, request payload 56 bytes,
Reference=original axis 1..4:
P8 DiagnosticsBuild U32, P12 OriginalDiagnosticsBootId U32,
P16 MapRevision U32, P20 CurrentDiagnosticsBootId U32
P24 OriginalRequestId U32, P28/P32/P36/P40 ClientIntentId0..3 U32
P44 SemanticMode U16 = 1, P46 Reserved U16 = 0
P48 ExpectedActualPosition DINT, P52 TargetPosition DINT = 0
An exact outcome response is 144 bytes:
P16 RecordState U16: 1 Running, 2 Succeeded, 3 Failed, 4 Aborted,
    5 Quarantined
P18 SemanticMode U16 = 1
P20 DiagnosticsBuild, P24 OriginalDiagnosticsBootId, P28 MapRevision U32
P32 OriginalRequestId, P36/P40/P44/P48 ClientIntentId0..3 U32
P52 AxisReference U16, P54 Reserved U16 = 0
P56 ExpectedActualPosition DINT, P60 TargetPosition DINT = 0
P64 TimeoutMilliseconds U32
P68 OriginalCommandStatus U16, P70 OriginalErrorId I16
P72 OriginalDetailCode U32, P76 AxisStatus U32, P80 AxisError DINT
P84 RawDrivePositionBefore DINT, P88 RawDrivePositionAfter DINT
P92 ActualApplicationPositionAfter DINT, P96 SetApplicationPositionAfter DINT
P100 ActualInternalPositionAfter DINT, P104 SetInternalPositionAfter DINT
P108 DestinationInternalPositionAfter DINT, P112 MasterInternalPositionAfter DINT
P116 NativeCommandState U32, P120 EvidenceFlags U32
P124 StartMilliseconds U32, P128 CompletionMilliseconds U32
P132 StopState U32 raw bit pattern, P136 RuntimePhase U32,
P140 RecordGeneration U32
StopState carries the retained DINT RT/service FailureCode without changing its
32-bit wire representation. Diagnostics must print both raw hexadecimal and the
two's-complement signed DINT interpretation; for example 0xFFFFFFF9 is -7,
RT verification mismatch. Zero is required for Succeeded.
Only a Succeeded exact record proves completion, and it must contain all of the
following fail-closed evidence: original status/error/detail, AxisError,
NativeCommandState, and StopState are zero; AxisStatus contains the native LASAL
Standstill bit 0x02000000; the wrap-safe U32 difference
RawDrivePositionAfter-RawDrivePositionBefore is 0, 1, 2, 0xFFFFFFFF (-1 count),
or 0xFFFFFFFE (-2 counts).
The two raw fields are physical-feedback evidence snapshots, not a promise of
bit-identical stationary samples; a delta of +/-3 counts or more fails closed.
All six application/internal actual, set, destination,
and master positions at P92..P112 are zero; EvidenceFlags is exactly 0x3F;
StartMilliseconds, CompletionMilliseconds, and RecordGeneration are nonzero.
RuntimePhase remains an opaque diagnostic value because this wire version does
not define its numeric states; the PC parser must not invent a terminal value.
The ACK alone is not completion evidence.

The internal retained ZeroHomeState owner-release receipt occupies slots 60..63;
these are not additional wire fields:
  [60] U32 magic 0x484F4D50
  [61] DINT phase: 1 PREPARED, 2 IDENTITY, 3 OBSERVER, 4 SINGLETON,
       5 RECORD, 6 COMPLETE
  [62] DINT initial PublishAxisOwnership result, exactly 0 in v1
  [63] U32 exact RecordGeneration bound to the receipt
The first ordinary Home success/safe-failure publish records PREPARED without
releasing the owner. After the caller records finalize acceptance, exact replay
advances the staged cleanup to COMPLETE. This receipt supports same-instance and
warm continuation of a partial cleanup only. It is not a cold-persistent journal,
and the current receipt change is source/static evidence, not a C78 build,
download, runtime, or cold-restart recovery proof.
An authenticated COMPLETE receipt remains terminal evidence but does not
intercept a later ownership publish with a different tuple. An incomplete or
corrupt receipt continues to block a different publish fail-closed.

The 2026-08-04 runtime evidence is limited. On separate fresh-boot first attempts,
Axis1 and Axis2 showed the current-position-zero/reference effect, but the old WPF
log did not preserve the exact 144-byte terminal outcome. The immediately following
axis was rejected with ErrorId=-31000, DetailCode=41. A quarantined first record
that retained the singleton/common owner is the strongest current hypothesis, not
a confirmed diagnosis. Start ACK and Read Home Status RPC PASS are not terminal
success evidence.
Query/retire domain failures admit only identity details 16..18 and LMC_Home
outcome details 33..37, always in the 16-byte common failure envelope.

0x7D19 RetireLmcHomeOutcome, request payload 60 bytes, repeats the exact 0x7D18
key at P8..P52 and adds P56 RecordGeneration U32. It accepts only a terminal exact
record and returns the same 144-byte terminal snapshot. Retry of the exact retire
is idempotent. Query/retire never dispatches SetPosition and never replays Home.
Admin feature bit 4 AxisHome is indivisible: it implies 0x7D13/0x7D18/0x7D19 and
ErrorCatalogVersion>=5. Gate/bit activation is source availability only; F9,
download, same-core task placement and PLC/pcap proof remain separate evidence.

0x7D15 StartAxisDs402Home, request payload 72 bytes, Reference=axis 1..4:
P8  ExpectedDiagnosticsBuild U32, nonzero
P12 ExpectedDiagnosticsBootId U32, nonzero
P16 ExpectedMapRevision U32, nonzero
P20/P24/P28/P32 ClientIntentId0..3 U32, not all zero
P36 HomingMethod DINT = 37, Homing on current position
P40 Position DINT = 0, DS402 0x607C Home Offset
P44 Velocity DINT = 0; method 37 performs no search motion
P48 Acceleration DINT = 0
P52 DistanceLimit DINT = 0; method -1/-2 distance-limit homing is not v1
P56 TorqueLimit DINT = 0; torque-limit homing is not v1
P60 BufferMode U16 = 1 Aborting, P62 Reserved U16 = 0
P64 TimeoutMilliseconds U32, nonzero overall PLC-side homing watchdog. It is
    not one SDO TimeoutCycles value; each individual SDO remains constrained to
    the executor's 1..60000-cycle limit.
P68 ExecuteToken U32 = 0x32303448, little-endian bytes 48 34 30 32 (`H402`)
TCP ownership reservation is attempted only for this complete valid request
shape and a valid axis. Malformed 0x7D15 requests remain delegated to the
Diagnostics parser with zero ownership tokens. For a valid request, owner
reservation result -2 is returned as detail 41; disconnected or every other
nonzero admission result is returned as detail 42. This 24-byte mapper is
specific to 0x7D15 and is never applied to encoder-maintenance 0x7E53.
The success/failure response is exactly 24 bytes:
P16 EchoedHomingMethod DINT, P20 NativeCommandState U32.
Success and pre-native failures require NativeCommandState=0. Detail 11 requires
ErrorId=-6 and nonzero NativeCommandState. A success is only start acceptance;
it does not prove HomingAttained, final zero position or safe motion readiness.
The public project name is LMC_HomeDS402. Method 35, arbitrary offsets and every
switch-seeking method fail closed. Method 37 uses no Home or limit switch and
does not require Operation Enabled as an admission precondition. The SDK faults
an outcome-uncertain session after a post-write transport or parser failure.
The PLC stores `ops.tAbsolute` service time at DS402 runtime slot 118 and cleanup
start time at slot 119. Overall timeout and cleanup timeout use unsigned
`serviceNow - serviceStart` and inclusive `>=` expiry; they continue when the RT
latch cycle is frozen.
Fresh latch samples gate stability/evidence only. A condition shaped as
`newCycle AND timeout` is forbidden because it can suppress the watchdog forever.

0x7D16 ReadAxisDs402HomeOutcome, request payload 44 bytes,
Reference=axis 1..4:
P8 DiagnosticsBuild U32, P12 DiagnosticsBootId U32, P16 MapRevision U32
P20 OriginalRequestId U32
P24/P28/P32/P36 ClientIntentId0..3 U32
P40 HomingMethod DINT
Success response payload is exactly 92 bytes:
P16 RecordState U16: 1 Running, 2 Succeeded, 3 Failed, 4 Aborted
P18 Reserved U16 = 0
P20 DiagnosticsBuild, P24 DiagnosticsBootId, P28 MapRevision U32
P32 OriginalRequestId U32, P36/P40/P44/P48 ClientIntentId0..3 U32
P52 AxisReference U16, P54 Reserved U16 = 0, P56 HomingMethod DINT
P60 OriginalCommandStatus U16, P62 OriginalErrorId I16
P64 OriginalDetailCode U32, P68 DS402StatusWord U16, P70 Reserved U16 = 0
P72 ActualPosition DINT, P76 StartCycle U32, P80 CompletionCycle U32
P84 NativeCommandState U32, P88 RecordGeneration U32
Running has CompletionCycle=0 and is not recovery resolution. Terminal records
require CompletionCycle>=StartCycle. The PLC may commit Succeeded only after it
has observed three fresh method-37 samples with homing attained (bit 12), target
reached (bit 10), homing error clear (bit 13), and ActualPosition=0 before
cleanup. P68 is the last StatusWord after restoring CSP mode 8, so bits 10 and
12 no longer carry the homing-mode meaning and the PC parser must not require
them in that post-cleanup snapshot. A Succeeded record still requires original
status/error/detail/native state all zero, ActualPosition=0, StatusWord bit 3
Fault clear, bit 13 clear, and a DS402 base state of 0x0040, 0x0021, 0x0023, or
0x0027. The final success snapshot must be a fresh latch cycle observed after
the RT control-word owner has been released, with no pending SDO, drain,
mode-restore, RT-owner, or uncertainty flag. Stage 101 is indeterminate detail
26, not Running success. Failed and Aborted preserve the terminal error/status snapshot; they do
not authorize automatic replay. Query failures admit only identity details
16..18 and DS402 Home outcome details 25..29; they return only the 16-byte
common envelope and leave recovery unresolved.
0x7D17 RetireAxisDs402HomeOutcome, request payload 48 bytes,
Reference=axis 1..4:
P8 DiagnosticsBuild U32, P12 DiagnosticsBootId U32, P16 MapRevision U32
P20 OriginalRequestId U32
P24/P28/P32/P36 ClientIntentId0..3 U32
P40 HomingMethod DINT
P44 RecordGeneration U32, nonzero and copied from the exact 0x7D16 result
Success response payload is exactly 92 bytes and repeats the 0x7D16 terminal
record layout at P16..P88. RecordState must be the masked base state 2, 3, or 4;
Running and an unmasked internal tombstone value are malformed. The echoed
identity and P88 RecordGeneration must exactly match the retire request. The
first exact retire stores a tombstone; an exact retry after response loss returns
the same success snapshot. Failures admit only identity details 16..18 and
DS402 Home outcome details 25..29, and return exactly the 16-byte common envelope.
Detail 26 covers Running/indeterminate, detail 28 covers exact-key or generation
mismatch, and detail 32 is not valid for 0x7D17.

Admin feature bit 6 AxisDs402Home is indivisible: it implies 0x7D15 start,
0x7D16 outcome query, and 0x7D17 terminal retirement plus
ErrorCatalogVersion>=4. Advertising any subset is forbidden. A same-axis new
start returns detail 32 while a terminal record remains unretired; an exact
retired tombstone may be replaced by the next admitted start. The current PLC
implementation must change bit 6 atomically with the method-37-only parser,
fresh-latch watchdog and owner interlock. F9 build, download and live DS402/pcap
proof remain separate qualification gates even when the source bit is enabled.
Earlier method-35 or switch-search results are not evidence for this contract.
The 2026-08-04 source gate remains FALSE. Admission and final-evidence hardening
alone do not activate the command. Prepared-stage warm reconciliation and the
one-second fail-closed bound for cleanup stages 90..99 are source-implemented.
Durable owner-release/rollback-complete receipt, DS402 bit-4 safety
drain/tombstone, C78 build, download and live-axis proof remain required.

0x7D20 ReadGroupParameters, request payload 12 bytes, Reference=0x0100:
P8 SelectionMask U32, nonzero subset of 0x07. Bits select path velocity limit,
path acceleration limit and jerk time. Jerk time is milliseconds, not jerk rate.
Success response payload is exactly 32 bytes:
P16 PresentMask U32, P20 PathVelocityLimit DINT [app units/s],
P24 PathAccelerationLimit DINT [app units/s^2], P28 JerkTime DINT [ms].
The PLC maps the three semantic bits explicitly to ReadGroupParameter(GrpNo=1)
and never accepts native enum values from the caller.

0x7D22 GroupMoveLinearRelative, request payload 104 bytes, Reference=0x0100:
P8..P68  Distance[16] DINT. The current X/Y/Z/U group uses slots 1..4 and
          requires slots 5..16 to be zero.
P72       Velocity DINT, greater than zero
P76       Acceleration DINT, greater than zero
P80       Deceleration DINT, greater than zero
P84       Jerk DINT, zero or greater
P88       CoordinateSystem DINT = 0 (None)
P92       TransitionMode DINT = 0 ExactStop or 2 ContinuousDirect
P96       BufferMode DINT = 1 Aborting or 2 Buffered
P100      Execute DINT = 1
The PLC requires Robot and Axis1..4 clients, GroupKinematicReady, Robot power,
and profile lock before calling LMCRobot.MoveRelativeCoord. The response is the
16-byte Admin common response. Success means the native profile command was
accepted; completion and profile error are read through 0x2045 GroupReadStatus.

0x7E00 GetDiagnosticsCapabilities, request payload 8 bytes
[8]  SchemaVersion UINT16 = 1
[10] RequestFlags UINT16 = 0
[12] RequestId UDINT, nonzero
Header Reference [6] must be 0.

Success response outer payload is exactly 68 bytes (76 bytes including header):
[8]  SchemaVersion UINT16 = 1
[10] ResponseFlags UINT16 = 0
[12] CommandStatus UINT16 = 0
[14] ErrorId INT16 = 0
[16] Echoed RequestId UDINT
[20] DetailCode UDINT = 0
[24] DiagnosticsBuild UDINT = 1
[28] CapabilityBits UDINT
     0x00004007 while DiagnosticsBootId is unavailable: D1 plus static
     EtherCATTopology only
     0x000C633F when D1-D3, D4 RecorderTrigger, D5 SDORead,
     SDOReadGeneralInline, and the current Axis 1 exact SDO Write target are
     available with a retained BootId. The pre-SDO-Write base mask is
     0x0000613F. Bit 13 requires bit 8 and MaxSdoDataBytes=4. Bit 9 SDOWrite is
     set only when the global gate and at least one per-axis target gate are
     enabled; the current source enables only Axis 1. Bit 14
     EtherCATTopology is active for the fixed seven-node 0x7E11/0x7E12 source.
     Bits 15 EtherCATNodeHealth and 16 DigitalIORead remain zero while the
     implemented read owner is dormant and awaits current PLC qualification.
     Bit 17 DigitalIOWrite remains zero because 0x7E23 has no PLC route/handler.
     Bit 15 and bit 16 require bit 14. Bit 17 requires bits 14..16 and a
     nonzero DiagnosticsBootId. Bit 18 EncoderTw20ErrorWarningReset follows
     LMC_DIAG_ENCODER_TW20_ENABLED and bit 19 EncoderTw19MultiturnPositionReset
     follows LMC_DIAG_ENCODER_TW19_ENABLED. Both gates are TRUE for the exact
     fixed-value contract below. Each requires the dedicated 0x7E53/54/55 triad,
     adequate request/response capacity, and nonzero DiagnosticsBuild,
     DiagnosticsBootId and MapRevision. They do not depend on generic SDORead,
     SDOWrite or SDOReadGeneralInline advertisement. Neither bit authorizes a
     special-object bypass in generic 0x7E50. All UDINT capability overlays use
     integer OR. LASAL `|` is forbidden here because C78 reduced the live mask to
     BOOL 1 when it was used for these overlays.
[32] MapRevision UDINT = 0x957F101E when D1 is available
[36] CatalogEntryCount UINT16 = 24 when D1 is available
[38] MaxBulkSignals UINT16 = 24 when D2 is available, otherwise 0
[40] MaxRecorderChannels UINT16 = 24 when D3 is available, otherwise 0
[42] RecorderBufferCount UINT16 = 1 when D3/D4 is available, otherwise 0
     D4 currently advertises only bank 0; Double-bank bit 6 remains zero.
[44] MaxRecorderSamples UDINT = 320000 when D3 is available, otherwise 0
     This is the absolute one-channel limit. ConfigureRecorder returns
     AcceptedCapacity=min(requested,floor(1280000/(channelCount*4)));
     current limits are 20000 samples at 16 channels and 13333 at 24 channels.
[48] BaseCycleTimeUs UDINT = 1000
[52] MaxRequestPayloadBytes UINT16 = 1320
[54] MaxResponsePayloadBytes UINT16 = 2040
[56] MaxChunkDataBytes UINT16 = 1280
[58] CatalogEntryStride UINT16 = 80
[60] SignalValueEntryStride UINT16 = 16
[62] Reserved UINT16 = 0
[64] RecorderBytesPerBank UDINT = 1280000 when D3 is available, otherwise 0
[68] MaxSdoDataBytes UINT16 = 4 when D5 SDORead and retained BootId are
     available, otherwise 0
[70] Reserved UINT16 = 0
[72] DiagnosticsBootId UDINT = retained nonzero generation for D2-D5;
     0 only when the retained generation is unavailable/faulted

Malformed diagnostics requests keep the same 68-byte response shape and set
CommandStatus=1, ErrorId=-32000, and DetailCode to 1 UnsupportedSchema,
2 UnsupportedFeature, or 12 BoundsInvalid. DiagnosticsBootId=0 is legal only
while all stateful Bulk, Recorder, PI Write, SDO, and DigitalIOWrite capability
bits are zero.
A nonzero retained boot generation is required before any stateful D2-D5 or
DigitalIOWrite capability can be advertised.
RequestId zero is reserved and returns BoundsInvalid. If a malformed request
declares at least the complete 8-byte common envelope, the response still
echoes the received RequestId; a shorter request cannot provide an ID to echo.

Diagnostics common request payload (all commands, outer header excluded):
P0 SchemaVersion UINT16 = 1
P2 RequestFlags UINT16 = 0
P4 RequestId UDINT, nonzero

Diagnostics common response payload:
P0  SchemaVersion UINT16 = 1
P2  ResponseFlags UINT16
P4  CommandStatus UINT16, 0 success / 1 domain error
P6  ErrorId INT16, domain error is -32000
P8  Echoed RequestId UDINT
P12 DetailCode UDINT

D1 active read-only commands:
- 0x7E01 GetSignalCatalogInfo: request 8, response 36 bytes
- 0x7E02 GetSignalCatalogChunk: request 16, response 28 + 80*N bytes
  P8 ExpectedMapRevision U32, P12 StartIndex U16, P14 MaxEntries U16
- 0x7E10 ReadEtherCATHealth: request 8, response 72 + 32*4 = 200 bytes
- 0x7E20 ReadPI: request 20, response 52 bytes
  P8 ExpectedMapRevision U32, P12 SignalId U32, P16 ExpectedType U8,
  P17..19 reserved zero
  Response P36 is one 16-byte SignalValueEntry. Valid is exclusive and uses
  DetailCode=0. An invalid entry with SlaveOffline uses DetailCode=18;
  every other invalid status uses DetailCode=11. SlaveOffline has priority
  when several invalid status bits are set. RawValue32 can retain the last
  sampled value while invalid and must not be displayed or judged as current.

EtherCAT topology and digital IO contract. Static topology is source-active;
dynamic health and digital-I/O read handlers are implemented but dormant, and
their capability bits remain off. Digital-output Write is not implemented:

- 0x7E11 GetEtherCATTopologyInfo: request 8, response exactly 44 bytes
  P16 TopologyRevision U32, nonzero canonical entry CRC32
  P20 TotalNodeCount U16
  P22 EntryStride U16 = 96
  P24 MaxEntriesPerChunk U16, range 1..16
  P26 ConfiguredSlaveCount U16
  P28 SlotModuleCount U16
  P30 PhysicalAxisCount U16
  P32 TopologyFlags U32 = 0x0000000F: fixed stride, ASCII name,
      canonical CRC, and opaque NodeId
  P36 CrcKind U32 = 1, CRC32/ISO-HDLC
  P40 Reserved U32 = 0
  TopologyRevision is independent from the D1 signal MapRevision. A zero CRC
  result is encoded as 0xFFFFFFFF.

- 0x7E12 GetEtherCATTopologyChunk: request 16, response 28 + 96*N bytes
  Request P8 ExpectedTopologyRevision U32, P12 StartIndex U16,
  P14 MaxEntries U16 in range 1..16.
  Response P16 TopologyRevision U32, P20 StartIndex U16,
  P22 ReturnedCount U16, P24 TotalNodeCount U16, P26 EntryStride U16 = 96,
  and entries at P28. ResponseFlags is LastChunk only for the final range.
  ReturnedCount must equal min(requested MaxEntries, TotalNodeCount-StartIndex).
  Maximum response payload is 1564 bytes.

  Topology entry, 96 bytes:
  E0 NodeId U32, nonzero opaque identifier
  E4 ParentNodeId U32, zero for an EtherCAT slave and parent slave for a slot
  E8 TopologyIndex U16
  E10 MasterSlaveIndex U16; index zero is valid, 0xFFFF means absent
  E12 NodeKind U8: 1 EtherCATSlave, 2 SlotModule
  E13 Reserved U8 = 0
  E14 NodeFlags U16: bit 0 master index, bit 1 SDO, bit 2 physical axis,
      bit 3 input, bit 4 output, bit 5 DS402, bit 6 IO coupler,
      bit 7 digital IO
  E16 SdoSlaveReference U16, zero when absent
  E18 PhysicalAxisReference U16, zero when absent
  E20 SlotIndex U16, 0xFFFF for a slave
  E22 Reserved U16 = 0
  E24 VendorId U32, E28 ProductCode U32, both nonzero
  E32 RevisionNumber U32, E36 SerialNumber U32
  E40 InputBytes U16, E42 OutputBytes U16
  E44 Name ASCII[48], NUL terminated and zero padded
  E92 IOReference U32, zero when no public digital IO target exists
  Complete topology validation requires slave MasterSlaveIndex values in
  canonical 0..N-1 order, unique nonzero SDO/physical-axis/IO references, and
  unique SlotIndex values per parent. A nonzero IOReference requires at least
  one input/output direction and at most 8 bytes per direction. The same
  IOReference may select input and output separately with ExpectedDirection.

- 0x7E13 ReadEtherCATNodeHealth: request 16, response exactly 72 bytes
  Request P8 TopologyRevision U32, P12 NodeId U32; both nonzero.
  Response P16 TopologyRevision U32, P20 NodeId U32,
  P24 CapturePhase U16 = InputMapped(1), P26 HealthFlags U16,
  P28 CycleCounter U32, P32 TimestampMicroseconds U64,
  P40 SnapshotSequence U32, nonzero and even, P44 Online U8,
  P45 EtherCATState U8,
  P46 ALStatusCode U16, P48 SlaveState U32, P52 ClassState U32,
  P56 DS402StatusWord U32, P60 AxisError U32,
  P64 LastValidCycle U32, P68 LastStateChangeCycle U32.
  HealthFlags bits 0..5 are Configured, Detected, IdentityMatched, DataValid,
  DataDefaulted, and DS402DataPresent. Configured is required, Online equals
  Detected, detected requires a nonzero EtherCATState, and offline requires
  EtherCATState zero. Exactly one of DataValid/DataDefaulted is set. DataValid
  requires Detected and IdentityMatched. Offline/defaulted is not valid data.

- 0x7E22 ReadDigitalIO: request 20, response exactly 56 bytes
  Request P8 TopologyRevision U32, P12 IOReference U32,
  P16 ExpectedDirection U8: 1 input or 2 output,
  P17 ExpectedBitWidth U8 in range 1..64, P18 Reserved U16 = 0.
  Response P16 TopologyRevision U32, P20 IOReference U32, P24 NodeId U32,
  P28 Direction U8, P29 BitWidth U8, P30 StatusFlags U16,
  P32 Value U64, P40 ValidMask U64, P48 CycleCounter U32,
  P52 OutputRevision U32. Input requires OutputRevision=0; an output shadow
  requires a nonzero revision. Status bits are Valid, StaleFrame,
  MasterNotOperational, NodeOffline, NodeNotOperational, AlError,
  SourceUnavailable, IdentityMismatch, and DataDefaulted. A Valid response has
  no fault flags. An invalid response requires DataDefaulted plus at least one
  cause flag from StaleFrame through IdentityMismatch, Value=0, and ValidMask=0.
  The SDK binds a facade-read value to its diagnostics owner, connection
  session, capability bits, and DiagnosticsBootId. Parser-only values remain
  detached protocol artifacts.

- 0x7E23 SubmitDigitalOutputWrite: request 40, response ticket 32 bytes
  Request P8 TopologyRevision U32, P12 IOReference U32,
  P16 Value U64, P24 Mask U64, P32 ExpectedOutputRevision U32,
  P36 DiagnosticsBootId U32. Revision, IOReference, mask, output revision, and
  BootId are nonzero. Value bits outside Mask must be zero. The PLC RT owner
  must atomically reject a stale output revision or apply
  NewOutput=(OldOutput & ~Mask) | (Value & Mask). Whole-word write uses the
  complete advertised output valid mask. PC-side read-modify-write is forbidden.
  Executable SDK requests must be created with
  CreateDigitalOutputWriteRequest from a valid current-session Output snapshot.
  Detached raw requests, foreign/stale sessions, incomplete source capability
  provenance, or a changed BootId are rejected before command 0x7E23 is sent.
  Success returns OperationKind=DigitalOutputWrite(4) and reuses 0x7E03/0x7E04
  for status/cancel. The ticket exposes SubmissionTopologyRevision while its
  SubmissionMapRevision is zero.
  Terminal Success is acceptance of the RT-owner operation, not physical-output
  proof. The client must reread 0x7E22 for the same node/topology/IOReference and
  verify a nonzero changed OutputRevision, unchanged BitWidth/ValidMask, and the
  complete NewOutput value including preservation of every unmasked bit.
  Submission ambiguity, disconnect, or failed/mismatched readback must retain a
  mutation/Close interlock and must never trigger automatic replay. Explicit
  operator acknowledgement may clear only the client interlock after separate
  physical verification; it is not a VERIFIED write result.
  Submission exceptions retain their original type and expose a separate
  failure context: NotAttempted, explicit RPC/PLC Rejected, wire
  OutcomeUncertain, or Accepted with a ticket before post-submit session
  validation failed. Fake TCP tests cover sync/async Accepted, explicit RPC
  rejection, response-loss OutcomeUncertain, and the accepted-session race.

The SDK validates capability dependencies before sending these commands. The
current LASAL source routes and handles 0x7E11/0x7E12 with a fixed seven-node
topology, TopologyRevision=0x15867EEC, and MaxEntriesPerChunk=1. It also routes
and handles 0x7E13/0x7E22 through LMCEcatInputLatch's coherent 464-byte snapshot;
the CREVIS coupler, input slot, and output slot clients are wired. Only bit 14 is
advertised: bits 15 and 16 remain zero until current PLC read-only qualification.
The digital-output SDK allowlist is currently empty, and no 0x7E23 PLC route,
handler, or RT output owner exists, so bit 17 remains zero. Source/static/IDE
Rebuild success is not a claim of PLC download, live response, or hardware support.

Topology/IO diagnostics detail codes reserved by the SDK contract are:
26 TopologyRevisionMismatch, 27 NodeNotFound, 28 IOReferenceNotFound,
29 OutputRevisionMismatch, 30 OutputMaskInvalid, 31 RTOwnerUnavailable.
The common diagnostics domain additionally defines 32
RecorderConfigurationAbsent. Only ReadRecorderBankInventory promotes this
detail to the typed SDK absence result described below.
Malformed shape remains BoundsInvalid(12), write policy denial uses
WriteDenied(7) or UnsafeWriteBlocked(8), BootId mismatch is 25, and slave
offline is 18.

D2 active Bulk commands:
- 0x7E30 ConfigureBulk: request 20 + 4*N, response 36 bytes
  P8 ExpectedMapRevision U32, P12 RequestedBulkId U32,
  P16 SignalCount U16, P18 reserved, P20 SignalId[N]
- 0x7E31 ReadBulkStatus: request 20, response 36 bytes
- 0x7E32 ReadBulkSnapshot: request 20, response 56 + 16*N bytes
- 0x7E33 ReleaseBulk: request 20, response common 16 bytes
  Identity request P8 BulkId, P12 ConfigRevision, P16 MapRevision.

D3/D4 active Recorder commands:
- 0x7E40 ConfigureRecorder: request 56 + 4*N, response 56 bytes
- 0x7E41 StartRecorder: request 28, response 40 bytes
- 0x7E43 StopRecorder: request 28, response common 16 bytes
- 0x7E44 ReadRecorderStatus: request 28, response 76 bytes
- 0x7E45 ReadRecorderHeader: request 28, response 112 + 4*N bytes
- 0x7E46 ReadRecorderChunk: request 32, response 52 + data bytes
- 0x7E47 ReleaseRecorderBuffer: request 28, response common 16 bytes
- 0x7E48 ReleaseRecorder: request 28, response common 16 bytes
- 0x7E49 AdoptRecorder/AdoptActiveRecorder: request 20, response 36 bytes
- 0x7E4A ReadRecorderBankInventory: request 24, response 88 bytes
- 0x7E4B AdoptEmptyRecorderConfiguration: request 28, response 40 bytes
- 0x7E4C ConfigureRecoverableDoubleRecorder: request 72 + 4*N, response 72 bytes
- 0x7E4D ReadRecoverableRecorderBankInventory: request 36, response 104 bytes
Recorder commands 0x7E42..0x7E47 use P8 RecordId, P12 BufferId,
P16 MapRevision, P20 OwnerSessionEpoch, P24 DiagnosticsBootId. Command 0x7E49
instead uses P8 RecordId, P12 BufferId, P16 DiagnosticsBootId; zero/zero selects
single-bank discovery. D3 Single mode accepts only
manual/no-trigger finite capture. D4 Ring mode requires edge/window/mask trigger
and supports RT condition or explicit TriggerRecorder. This test build advertises
one bank, BufferId 0, and a maximum of 24 channels. RecorderDoubleBank
capability bit 6 is zero and Configure BufferMode=Double returns UnsupportedFeature.
ReadRecorderBankInventory is a dormant, read-only Double-bank recovery contract
behind the same disabled gate. Its request is P8 DiagnosticsBootId U32,
P12 ConfigId U32, P16 MapRevision U32, P20 ConfigRevision U32, where zero
ConfigRevision discovers the exact current revision and a nonzero value requires
an exact match. Success keeps configuration identity visible even with zero
occupied banks: P16 DiagnosticsBootId, P20 ConfigId, P24 ConfigRevision,
P28 MapRevision, P32 configuration OwnerSessionEpoch, P36 configuration
ClosedSessionEpoch, P40 configuration State U16, P42 BufferMode U8,
P43 RecorderBufferCount U8, P44 OccupiedBankCount U8, P45..47 reserved zero.
Up to two compact occupied-bank entries begin at P48 with 20-byte stride:
RecordId U32, BufferId U32, OwnerSessionEpoch U32, ClosedSessionEpoch U32,
State U16, reserved U16. Entries are ordered by BufferId; unused entries are
all zero. The command never adopts, releases, or rewrites ownership. It rejects
mixed configuration/bank identity, mixed owner closure, active foreign owners,
non-Double configurations, and ambiguous bank states before returning metadata.
When the request carries a nonzero ConfigRevision, its BootId matches the
current diagnostics generation, its MapRevision matches the Recorder map, and
the complete Recorder store is canonically Empty (class identity/shape fields
zero, BufferReleased=true, and both bank descriptors and states empty), the PLC
returns the common error envelope with detail 32 RecorderConfigurationAbsent.
This is a read-only terminal absence proof for a persisted exact configuration
Release intent; it does not adopt, release, clear, or rewrite anything. The SDK
maps it to LMCRecorderConfigurationAbsentException bound to the requested
BootId/ConfigId/ConfigRevision/MapRevision. ConfigRevision=0 is never promoted
to this typed proof and remains fail-closed when no configuration exists.
Fault is deliberately outside the inventory/adoption recovery contract:
ReadRecorderBankInventory and AdoptRecorder reject a Fault bank, while
AdoptEmptyRecorderConfiguration accepts only exact Configured state. A Fault
record remains fail-closed for explicit manual recovery/reset and must not be
treated as empty, released, or safe to rebind.
AdoptEmptyRecorderConfiguration is a second dormant Double-bank recovery command
for the exact Configure-success/response-loss case where inventory reports zero
occupied banks. Its request is P8 DiagnosticsBootId U32, P12 ConfigId U32,
P16 ConfigRevision U32, P20 MapRevision U32, and P24 previous
OwnerSessionEpoch U32. All five identity values are required and exact. Success
requires Configured/Double/count-2 state, both physical banks Configured with
zero bank identity fields, the exact previous owner marked closed, and a distinct
nonzero caller session. It rebinds only the configuration OwnerSessionEpoch and
clears its ClosedSessionEpoch. It does not reset bank/data metadata or infer any
identity. The fixed response is P16 DiagnosticsBootId, P20 ConfigId,
P24 ConfigRevision, P28 MapRevision, P32 new OwnerSessionEpoch, P36 Configured
State U16, P38 Double BufferMode U8, and P39 RecorderBufferCount=2 U8. The SDK
returns a release-only recovered configuration lease; only exact 0x7E48 release
is defined for that type, and StartRecorder does not accept it. A lost 0x7E4B
success response is outcome-uncertain and is not retried in the same session:
the store does not retain a separate FromOwner proof after rebind. The client
faults/reconnects, reads 0x7E4A again after the attempted owner is closed, and
uses that newly returned exact previous owner for the next 0x7E4B request.
ConfigureRecoverableDoubleRecorder is the only Double Configure wire that may
be enabled with capability bit 6. Ordinary 0x7E40 keeps rejecting Double mode.
0x7E4C preserves the existing Configure fields at P8..P55, then carries an
opaque nonzero 128-bit recovery token at P56..P71 and SignalId[N] from P72.
RequestedConfigId must be nonzero. Success preserves the existing Configure
response at P0..P55 and echoes the exact token at P56..P71. The token, complete
configuration, and two Configured banks are committed before StateValue is
published. A token-qualified configuration cannot be overwritten by 0x7E40 or
a repeated 0x7E4C. Exact 0x7E48 Release clears the active token in the same
configuration-release generation and retains a per-boot last-token replay
fence; a late 0x7E4C using that token is rejected without changing ConfigId,
ConfigRevision, or NextConfigId.
ReadRecoverableRecorderBankInventory is the read-only recovery resolver for a
Configure response lost before ConfigRevision became durable on the client.
Its request is P8 DiagnosticsBootId U32, P12 requested ConfigId U32,
P16 MapRevision U32, and P20..P35 the exact token. Success uses the complete
0x7E4A inventory layout at P0..P87 and echoes the token at P88..P103. The client
persists the discovered nonzero ConfigRevision, then issues ordinary 0x7E4A
again before existing 0x7E4B/0x7E49 adoption and Release logic. If the current
BootId/map match and the complete Recorder store is canonically Empty with no
active token, detail 32 is promoted only on this command to
LMCRecoverableRecorderConfigurationAbsentException bound to the requested
BootId/ConfigId/map/token. That proof resolves only a v3 journal record marked
as ClientTokenV1. A legacy v2 ConfigRevision=0 record was never bound on wire and
remains zero-wire fail-closed. 0x7E4D never adopts, releases, changes ownership,
or repairs partial state; token mismatch, a live foreign owner, Fault, reboot,
or inconsistent bank/configuration metadata remains unresolved.
For a Mask trigger, TriggerValue is unused and must be zero; only the nonzero
TriggerMask participates in all-set/any-set/all-clear evaluation.
Automatic edge/window/mask evaluation requires MasterState=OP(8), zero consecutive
invalid cycles, and the trigger axis Online with ESMState=OP(8) and ALStatusCode=0.
An invalid cycle clears the previous-value history. A forced TriggerRecorder
request bypasses this input-health gate after the configured pre-history is ready.
StopRecorder publishes a stop sequence. When RT observes it, the store freezes
before copying that cycle's sample. A non-empty record retains EndCycle and
EndTimestamp from its last copied sample; Stop does not append an extra sample.

D4 active TriggerRecorder 0x7E42:
Request payload is exactly 28 bytes and uses the Recorder identity layout above.
Success response is common-only 16 bytes. Current PLC capability bit 5 is one.
The PLC validates identity/owner/map/BootId/state and publishes a forced-trigger
sequence to the RT recorder. Manual configurations and inactive records reject it.
TriggerRecorder and StopRecorder success ACKs mean that their request sequence was
published, not that RT already applied it. If a request races a natural terminal
transition, the final ReadRecorderStatus StopReason and TriggerIndex are authoritative.
Ring upload remaps wrapped physical samples to chronological offset order.
Recorder header DataCrcPolicy is None, DataCrcPresent is clear, and every Recorder
chunk DataCRC32 is zero. DroppedSamples/DroppedCycles and OverflowCount are reserved
and currently always zero; zero is not PLC runtime proof of no loss.
Before a trigger, Ring recording can update its bounded pre-history indefinitely.
Exact reconnect recovery keeps the saved BootId/RecordId/BufferId and calls
AdoptRecorder. If Start response loss or an app crash loses RecordId, the client
first verifies the nonzero BootId and calls AdoptActiveRecorder, which sends
RecordId=0 and BufferId=0 on the same 0x7E49 wire command. The PLC discovers the
one current closed-owner bank and returns its actual identity. Mixed zero/nonzero
identity is rejected. This discovery contract is single-bank only; Double-bank
capability remains off.
The SDK validates exact pre/trigger/post shape only for a locally configured
identity: TriggerIndex=PreTriggerSamples and TriggerComplete SampleCount equals
PreTriggerSamples+1+PostTriggerSamples. AdoptRecorder does not return that shape,
so adopted identities are checked only against common frozen wire invariants.

D5 GetOperationStatus 0x7E03 and CancelOperation 0x7E04:
Request payload is exactly 16 bytes:
P8 TicketId U32, P12 DiagnosticsBootId U32.
The general-inline test profile validates ticket, BootId and owner session. Status
returns the fixed 64-byte operation envelope and its P24 SubmitCycle must exactly
match the QueuedCycle captured in the accepted ticket. Cancel succeeds only for the
owning Queued ticket; Running and terminal tickets return InvalidState.
Malformed sizes return BoundsInvalid. A gate-off build returns UnsupportedFeature.

D5 reserved PI Write 0x7E21:
Request payload is exactly 28 bytes:
P8  MapRevision U32
P12 SignalId U32
P16 ValueType U8
P17..19 reserved zero
P20 RawValue32 U32
P24 DiagnosticsBootId U32
Future success response is the 32-byte operation-ticket payload. Current PLC
capability bit 7 is zero and the exact request returns UnsupportedFeature.

D5 SubmitSDO 0x7E50 request header is 32 bytes plus write data:
P8 MapRevision U32, P12 SlaveReference U16, P14 OperationFlags U16,
P16 ObjectIndex U16, P18 SubIndex U8, P19 ValueType U8,
P20 TimeoutCycles U32, P24 DataLength U16, P26 reserved,
P28 DiagnosticsBootId U32, P32 WriteData[]. The current test profile sets bit 8
SDORead, bit 9 SDOWrite and bit 13 SDOReadGeneralInline. SDOWrite advertises only
the Axis 1 exact target described below; it is not a general write capability.
The parser accepts only OperationFlags 0/1, reserved=0, an exact 32-byte read
request, or an exact 32+DataLength-byte write request. The active general-inline
profile allows only OperationFlags=0, slave 1..4, nonzero ObjectIndex, any U8
SubIndex and TimeoutCycles=1..60000. DataLength must exactly match ValueType:
Bool/Int8/UInt8/BitField8=1, Int16/UInt16/BitField16=2, and
Int32/UInt32/Real32/BitField32=4. It returns a Queued operation ticket. A
successful terminal status carries exactly 1, 2 or 4 inline bytes and all unused
result-tail bytes are zero. 8/12-byte reads and result chunks remain fail-closed;
malformed shapes return BoundsInvalid or CapacityExceeded.

The general prepared Write path accepts only OperationFlags=1, an exact 36-byte request,
ValueType=Int32(4), DataLength=4 and four little-endian data bytes. There is no
TW19/TW20 exception in this parser or its central policy. When the PLC
global gate and the matching per-axis gate are enabled and every policy/state
check passes, the returned ticket uses OperationKind=SDOWrite(3); successful
terminal status has ResultLength=0 and zero result data. The only approved
source/policy target is Axis 1 Gold UI[24], SlaveReference=1, 0x2F00:24,
Int32/4, with the conservative local range -1073741823..1073741823.
LMC_DIAG_D5_SDO_WRITE_GLOBAL_ENABLED and
LMC_DIAG_D5_SDO_WRITE_UI24_AXIS1_ENABLED are TRUE; the Axis 2..4 gates remain
FALSE. The matching C# SdoWriteEnabled/SdoWriteUi24Axis1Enabled gates and exact
allowlist are enabled while the Axis 2..4 gates remain false. The WPF manual
Write control remains disabled until baseline Read, pre-write guard Read,
same-value Write and guarded readback complete with four distinct tickets on
the exact current connection/session/DiagnosticsBuild/BootId/MapRevision and
target. Its second confirmation uses the SDK identity-pinned submit path, which
refreshes capabilities inside the mutation gate and sends no 0x7E50 frame on
identity drift. This is source/PC activation, not current PLC or hardware proof.
Arbitrary
SDO writes are not supported. DS402 direct motion/control objects 0x6040, 0x607A,
0x60FF and 0x6071 are permanently UnsafeWriteBlocked. PI Write 0x7E21 and extended
result 0x7E51 remain disabled.

Dedicated encoder maintenance uses only the following triad. Header Reference is
zero; the target drive is carried inside the request.

0x7E53 StartEncoderMaintenance, request payload 72 bytes:
P8 DiagnosticsBuild U32, P12 DiagnosticsBootId U32, P16 MapRevision U32
P20/P24/P28/P32 ClientIntentId0..3 U32, not all zero
P36 Kind U16: 1 TW20 Error/Warning Reset, 2 TW19 Multiturn Position Reset
P38 CompatibilityProfileId U16, nonzero recovery/audit identity
P40 DriveReference U16, 1..4
P42 FeedbackSocket U16, 1..4 legacy recovery/audit identity only
P44 CommandValue U32, exactly 1
P48 TimeoutMilliseconds U32, 1..60000
P52/P56/P60/P64 CompatibilityEvidenceId0..3 U32, not all zero
P68 ExecuteToken U32: 0x30325754 (`TW20`) for kind 1 or
    0x39315754 (`TW19`) for kind 2
Kind 1 dispatches exactly 0x20FC:0x02, UInt16 value 1, 2 bytes. Kind 2 dispatches
exactly 0x20FC:0x01, UInt16 value 1, 2 bytes. DriveReference selects the physical
drive; neither the axis number nor FeedbackSocket becomes the write value.
Profile/socket/evidence fields remain exact recovery and audit identity only and
do not select or authorize the SDO target. Both require motor off and the common
axis-owner token. The generic TW[] LONG aliases
0x3204:0x14/0x13 are excluded; substitution, fallback, retry or dual-send is
forbidden.
TimeoutMilliseconds is the overall unsigned `ops.tAbsolute` service-clock
budget from request acceptance through terminal cleanup. RT latch cycle counts
are evidence only and must never be interpreted as milliseconds. Before the one
SDO write is submitted, the PLC computes elapsedMs and passes only the bounded
remainingMs to `LMCSdoExecutor.TryStartWrite(..., TimeoutMs:=remainingMs)`.
The generic D5 SDO `TimeoutCycles` contract is separate and remains unchanged.
Success response payload is exactly 40 bytes:
P16 Kind U16, P18 CompatibilityProfileId U16, P20 DriveReference U16,
P22 FeedbackSocket U16, P24 CommandValue U32, P28 RecordGeneration U32,
P32 OwnerGeneration U32, P36 StartCycle U32.

0x7E54 ReadEncoderMaintenanceOutcome, request payload 72 bytes, repeats the exact
identity at P8..P68 but places OriginalRequestId at P20 and the client intent at
P24..P36. 0x7E55 RetireEncoderMaintenanceOutcome adds P72 RecordGeneration and
has a 76-byte request. Both successful responses are exactly 156 bytes:
P16 RecordState U16, P18 Kind U16
P20 DiagnosticsBuild, P24 DiagnosticsBootId, P28 MapRevision U32
P32 OriginalRequestId, P36/P40/P44/P48 ClientIntentId0..3 U32
P52 CompatibilityProfileId U16, P54 DriveReference U16,
P56 FeedbackSocket U16, P58 Reserved U16 = 0
P60 CommandValue U32, P64 TimeoutMilliseconds U32,
P68/P72/P76/P80 CompatibilityEvidenceId0..3 U32
P84 OriginalCommandStatus U16, P86 OriginalErrorId I16,
P88 OriginalDetailCode U32, P92 SdoAbortCode U32
P96 StartCycle, P100 WriteCompletionCycle, P104 CompletionCycle U32
P108 ExecutorState U32, P112 VerificationFlags U32
P116 PreEvidence0, P120 PostEvidence0, P124 PreEvidence1,
P128 PostEvidence1 U32, P132 StatusWord U16, P134 Reserved U16 = 0
P136 AxisError DINT, P140 DriveErrorCode U32, P144 ActualPosition DINT,
P148 RecordGeneration U32, P152 OwnerGeneration U32.
Only an exact Succeeded terminal record with all required verification flags and
executor released proves the maintenance workflow completed. It still does not
by itself prove the physical encoder fault disappeared or the axis is motion-ready.
All LASAL VerificationFlags accumulation and the retired-state tombstone use
integer OR. Logical `|` is forbidden for these integer expressions because C78
collapses their nonzero result to BOOL 1.
Capability bits 18 and 19 are source-active for this fixed-value dedicated path.
That source advertisement is not PLC download or physical-effect proof; current
PLC identity and live before/after evidence are still required.

The executor publishes Running before StartReadSDO or StartWriteSDO can callback.
It copies Write data into executor-owned storage before dispatch. It owns cleanup
through a private Releasing state, returns an owned validation failure as a
terminal ticket result, and releases a late orphan callback back to Idle. An
unsolicited/duplicate callback or impossible compare-exchange transition remains
hard Quarantined. Submit reports InternalError detail 24 when no public ticket or
drain owns a non-Idle executor; ResourceBusy detail 9 is reserved for an owned
queued/running/draining operation.

The SDO Write source/API/WPF infrastructure, PC tests, static contracts and
current LASAL IDE build are not PLC download, EtherCAT mailbox, drive-program,
or hardware behavior proof.

D5 ReadSDOResultChunk 0x7E51 request is exactly 28 bytes:
P8 TicketId U32, P12 ByteOffset U32, P16 RequestedBytes U16,
P18 reserved, P20 Sequence U32, P24 DiagnosticsBootId U32.
Future success response is 48 + data bytes:
P16 TicketId, P20 ByteOffset, P24 ReturnedBytes U16, P26 reserved,
P28 Sequence, P32 TotalResultLength, P36 DataCRC32, P40 DiagnosticsBootId,
P44 ValueType U8, P45..47 reserved, P48 Data[]. Current capability bit 12 is
zero and the exact request returns UnsupportedFeature.

Full Catalog, Health, Bulk, Recorder and operation response field definitions
are canonical in docs/architecture/LMC_ETHERCAT_PI_BULK_RECORDER_IMPLEMENTATION_DESIGN_2026-07-20.md
and are enforced by C# golden/parser tests plus Verify-LasalContract.ps1.

Target lookup/dispatch:
- 0x103C/0x1042 carry the caller-provided ASCII object name.
- LASAL compares it with actual names read from connected objects.
- The returned UINT16 is an opaque descriptor, not a pointer.
- Current deployment: single-axis descriptors 1..9, group 0x0100; 0 is invalid.
  Cartesian group identity/motion remains limited to axes 1..4 (X/Y/Z/U).
- GroupReadStatus carries the group descriptor in both header [6] and payload [8].

0x209F MoveAbsoluteEx, payload 32 bytes
[8] Position
[12] Velocity
[16] Acceleration
[20] Deceleration
[24] Jerk
[28] Direction
[32] BufferMode
[36] Execute
Current LASAL contract: Direction must be Shortest (2).

0x20A0 MoveRelativeEx, payload 32 bytes
Same layout as 0x209F. [8] is relative distance.
Direction must be Shortest (2); the signed distance selects relative direction.

0x20A2 MoveVelocityEx, payload 24 bytes
[8] Velocity
[12] Acceleration
[16] Deceleration
[20] Jerk
[24] Direction
[28] Execute
Deceleration must be 0 because `_LMCAxis.MoveEndless` has no deceleration input.
Direction must be Positive (1) or Negative (3). The C# serializer normalizes
the velocity sign and LASAL rejects a sign/direction mismatch.

0x20A4 MoveLinearAbsoluteEx, payload 96 bytes
[8..71] Position[0..15], 4 bytes each
[72] Velocity
[76] Acceleration
[80] Deceleration
[84] Jerk
[88] LASAL CoordSystem index
[92] TransitionMode
[96] BufferMode
[100] Execute
Current four-axis LASAL restrictions:
- Position[0..3] are X/Y/Z/U in the statically connected axis order.
- Position[4..15] must be zero.
- Velocity, acceleration and deceleration must be positive; jerk must be >= 0.
- CoordSystem must be None (0).
- TransitionMode must be ExactStop (0) or ContinuousDirect (2).
- BufferMode must be Aborting (1) or Buffered (2).
- Execute must be 1.
- Aborting maps to CmdConfig.MoveImmediately (bit 5, value 16); Buffered maps
  to CmdConfig 0. The LASAL call is LMCRobot.MoveLinearCoord.
The C# request builder enforces this same whitelist before any RPC frame is
sent. Defined but unsupported enum values throw NotSupportedException; unknown
enum values and invalid numeric parameters are rejected as argument errors.

0x2051 GroupReadActualPosition, payload 8 bytes
[8] CoordinateSystem DINT: None=0, ACS=1, MCS=2, PCS=3
[12] Enable BYTE = 1
[13..15] Padding = 0
The v1 LASAL adapter accepts None and ACS only. With no dynamic CalcModel, both
names are aliases for the statically wired member-slot identity read performed
with GetRobotPosition(CoordSystem:=0). MCS and PCS are not converted: the C#
builder throws NotSupportedException and the PLC returns ErrorId=-7 to older
clients. Unknown coordinate values remain malformed input (ErrorId=-3).
The tracked handler copies the complete 36-byte _LMCPROF_POS (Pos1..Pos9) into
response slots 1..9 and leaves slots 10..16 zero. These are software group
member slots; this does not expand Move/SetKin/Lock beyond the four physical
X/Y/Z/U axes. The result CoordinateSystem property echoes the requested enum;
it is not a coordinate field returned by the PLC. ACS runtime equivalence still
requires a PLC test and packet capture.

LASAL-DINT v1 success response payload is exactly 68 bytes:
[8..71] Position DINT[16]
[72] FunctionStatus UINT16
[74] ErrorId INT16
A 4-byte command-error envelope is preserved as an unsuccessful typed result.
The captured PMAS 136-byte LREAL response is legacy and the PC parser rejects it.

0x20E7 SetKinTransformCartesian4Axis, payload 1320 bytes
[8..647] MC_KIN_NODE_DEF[16], 40 bytes each
  node+0  BackwardRatio LREAL
  node+8  ForwardRatio LREAL
  node+16 BackwardShift LREAL
  node+24 TransformFunction DINT
  node+28 AxisReference UDINT
  node+32 AxisType DINT
  node+36 Padding DINT
[648] iNumAxes DINT = 4
[652..1311] MC_KIN_REF aligned union remainder
[1312] eKinType DINT = 0 (Cartesian)
[1316] BufferMode DINT = 2 (Buffered)
[1320] Execute BYTE = 1
[1321..1327] ABI tail padding = 0
Public support is restricted to four identity-shift nodes X/Y/Z/U using unique
axis references from the same LMCConnection. Generic node counts, coefficients,
axis types and buffer modes are not capture-verified. The canonical LASAL source
uses a 2048-byte receive accumulator, 1328-byte request buffer and 1320-byte
queue payload, so the exact command fits without a second staging path. The
handler validates the entire captured identity shape, confirms axis clients
1..4, and configures the static identity mapping only. It does not call
LockProfile and does not create or modify a dynamic CalcModel. Profile locking
is the separate 0x2047 command.
The captured response payload is exactly 4 bytes (`UINT16 status + INT16
error`). The public method rejects the generic 8-byte acknowledgement shape.

0x2022 Stop, payload 16 bytes
[8] Deceleration
[12] Jerk
[16] BufferMode
[20] Execute
Current LASAL-local Axis Stop requires Deceleration>0, Jerk>=0,
BufferMode=Aborting (1), and Execute=1. The C# builder rejects an invalid
Deceleration or Jerk before a frame is created. A well-formed legacy wire
request with an unsupported value combination is rejected by the PLC with
ErrorId=-7 and does not call LMCAxis.StopMove. Shape/reference errors remain
ErrorId=-3, and an unavailable axis client remains ErrorId=-2.
A success ACK means StopMove returned an all-zero _LMCAXIS_CMDERROR at dispatch;
it does not prove standstill. Poll 0x2028 and require stable IsStandstill status.

0x2085 GroupStop, payload 16 bytes
Same layout as Stop.
Current LASAL validation requires BufferMode=Aborting (1), Execute=1,
Deceleration>=0, Jerk>=0 and Deceleration>0 when Jerk>0. The handler calls
LMCRobot.StopMove(Mode:=3, Decel, Jerk).
StopMove returns UDINT StopCmdNo, the profile-buffer command index where the
stop ends; it is not an error or acceptance code and must not be mapped to ACK
status/error. The success ACK means the request was validated, the robot client
was connected and StopMove was dispatched. Poll GroupReadStatus afterward for
ProfileError and in-position/standstill state.

0x2049 GroupReset, payload 1 byte
[8] Execute BYTE = 1
The handler validates group reference 0x0100 and calls
LMCRobot.AxQuitError(AxisNo:=0).
A success ACK means only that the reset call was dispatched. QuitError processing
may complete later in the realtime task, and the ACK does not prove that every
member axis error cleared. GroupReadStatus alone is insufficient because 0x2045
does not return each member's ReadAxisError value.

The implemented stable completion contract first validates and pins the observed
0x20D2 member snapshot, sends exactly one 0x2049, and publishes
PendingGroupResetWaitContinuation. Resume never replays 0x2049. Each Resume round
sends one 0x2045 followed by one 0x2028 for every pinned member in snapshot order.
A full-clear round requires successful reads, GroupErrorId=0 and every member
AxisErrorId=0; completion requires three consecutive full-clear rounds. Any
group/member nonzero error resets the stable count. The generic SDK accepts a
successful 1..16-member snapshot with nonzero unique axis references. This proves
only the observed snapshot; it does not attest the expected topology, the current
PLC build, or the tracked LASAL source's nine-member layout. Stop/PowerOff/safe
Disable may preempt the
monitor, and Reset proof does not prove or promote power, profile lock, DS402 fault
clearance, reference/home or motion readiness.

The implemented public surface includes LMCGroupResetPreparedEvidence,
LMCGroupResetDurableRecoveryRecord, PendingGroupResetWaitContinuation,
BeginGroupResetWaitForStableErrorClearanceAsync,
AttachGroupResetDurableRecoveryAsync,
ResumeGroupResetWaitForStableErrorClearanceAsync and the compound facade. The local no-wire
SupersedePendingGroupResetAfterCapturedMemberSafetyMutation helper reconciles an
accepted/outcome-uncertain captured-member Axis safety generation with the exact
pending Reset; a rolled-back valid NACK returns false and preserves the continuation.
Raw GroupReset[Async] remains the
ACK-only compatibility surface, while the WPF Group Reset button uses the stable
path. Timeout, cancellation, and status failure preserve a same-session status-only
continuation. Accepted or outcome-uncertain group/captured-member mutation is a
terminal supersede; a pre-wire failure or structurally valid safety NACK preserves
the Reset continuation. The compound facade shares one Begin+Resume deadline, but a
later split Resume starts a new status-only timeout epoch and stable count.

While exact live proof is pending or the Reset submission outcome is uncertain, WPF
blocks fresh Reset, PowerOn, Enable, SetKin, Move, mutation qualification,
Connect/Reconnect, and connection/window Close. It allows exact status-only Resume,
read-only inspection, Stop, PowerOff, and safe Disable. Accepted Reset invalidates
cached power-active, identity, Home, and profile-lock readiness; a successful proof
does not restore them. WPF durably arms the exact endpoint, Diagnostics identity,
group, ordered members, stable count, and operation ID at the prepared callback before
the Reset write. A spontaneous disconnect or restart discards the old session
continuation and promotes that record to recovery-required without replay. After exact
endpoint/build/BootId/Map/group matching, Attach reads one fresh 0x20D2 and publishes a
new-session status-only continuation only when count/order/name/reference/device all
match. Attach and Resume send zero 0x2049. Identity mismatch preserves the durable
record and sends no status wire. See
GROUP_RESET_STABLE_MEMBER_ERROR_CLEARANCE_2026-07-31.md.

0x204A GroupPowerOn, payload 1 byte (project-local LASAL extension; absent from the
original Elmo capture set, current LASAL live capture available)
[8] Execute BYTE = 1
The handler starts asynchronous LMCRobot.RobotOn processing. A success ACK means
the request was accepted, not that every member axis is servo-ready. Poll
GroupReadStatus until local state bit 0x00040000 (IsPowerOn) is set.

0x204B GroupPowerOff, payload 1 byte (project-local LASAL extension; absent from the
original Elmo capture set, current LASAL frame plus final 0x2045 status captured)
[8] Execute BYTE = 1
The handler starts asynchronous LMCRobot.RobotOff processing. A success ACK means
the request was accepted, not that every member axis is powered off. Poll
GroupReadStatus until local state bit 0x00040000 (IsPowerOn) is cleared.

The current client completion contract sends each 0x204A or 0x204B request exactly
once, then WaitForPowerStateAsync polls only 0x2045 until three consecutive
successful samples match the expected IsPowerOn value. Resuming pending
verification sends status reads only and never replays the original power command.
One manual GroupReadStatus cannot by itself complete pending power or Enable
verification or promote unverified ACTIVE/profile-lock readiness. A successful
response published against the current safety generation does, however, accumulate
in the matching pending GroupEnable continuation proof. At 3/3 Locked Standby proof,
Resume can complete by reusing the accepted ACK without replaying 0x2047. A
Stop/PowerOff safety reservation
resets that proof to zero while preserving the accepted ACK and continuation. A
response arriving after the reservation is drained and discarded as ResultDiscarded
without being observed. Recovery-required is entered only in the narrow race where
SDK completion publication finished before the reservation but WPF had not applied
that completion yet. While connected and unresolved, group-name mutation, group
reload, clean connection/window close, connected reconnect, and a new PowerOn are
blocked. After an external connection loss, reconnect entry promotes the state to
recovery for the retained exact group name, and only that group may be reloaded in
the new session. A successful explicit 0x2048 GroupDisable ACK means only that
UnlockProfile was accepted; it does not release pending or recovery-required state.
Both states are released only after three consecutive powered Disabled/Unlocked
samples or three consecutive PowerOn=False samples for the retained exact group. A successful
PowerOn alone does not release either state, and no path replays 0x2047. This is
deterministic PC/fake-TCP evidence, not PLC runtime proof.

Before a fresh 0x2047 dispatch, the WPF writes a separate durable recovery record
containing endpoint IPv4/port, group name/reference, DiagnosticsBootId, and
MapRevision. An ArmedBeforeDispatch record found at startup is promoted to
RecoveryRequired. Endpoint mismatch is rejected before TCP/RPC; BootId/MapRevision
and group-reference mismatches allow only the required read-only identity checks and
send no group mutation. Verified Enable, explicit Disable, or verified PowerOff
resolves the durable record before volatile state is cleared, after a final identity
and safety-generation check. No recovery path automatically replays 0x2047.

0x2047 GroupEnable / ProfileLock, payload 1 byte
[8] Execute BYTE = 1
The handler calls LMCRobot.LockProfile for the validated static mapping. A success
ACK means the lock request was accepted. The standard Maestro
NC_GROUP_STANDBY_MASK (0x00020000) identifies standby; this adapter sets it for
the locked standby condition exposed as IsStandby/IsEnabled.
The earlier live capture exposed a handler defect: after LockProfile returned
_LMCPROF_NoError, the handler read LockState in the same CyWork call and could return
ErrorId -6 before the asynchronous state update. The tracked LASAL source now ACKs
native acceptance and verifies final lock only through 0x2045 polling; SourceOnly and
full static contracts pass. A new LASAL IDE build/download and live acceptance capture
are still required. Clients must never convert a native nonzero error to success.

0x2048 GroupDisable / ProfileUnlock, payload 1 byte
[8] Execute BYTE = 1
The handler requires ProfileInPosition(_LMCPROF_ProfileFinished), then calls
LMCRobot.UnlockProfile. A moving or unverified profile is rejected with -6.
A success ACK means the unlock request was accepted; it is not completion. The
handler does not read LockState again in the same CyWork call. Clients prove final
completion only through 0x2045 polling. BeginGroupDisableWaitForStableDisabledAsync
sends 0x2048 once, while same-session Resume and cross-session
WaitForStableDisabledAsync send only 0x2045 until three consecutive successful
samples show PowerOn + Disabled + !Standby. A newer stable PowerOff may retire the
pending Disable as superseded, but it must not be reported as Disable completion.
The standard Maestro
NC_GROUP_DISABLED_MASK (0x00010000) identifies disabled; this adapter sets it for
the unlocked disabled condition exposed as IsDisabled.

Normal group order:
GroupPowerOn -> GroupReadStatus IsPowerOn -> SetKinTransform -> GroupEnable/lock
-> motion -> GroupStop/in-position -> GroupDisable/unlock -> GroupPowerOff ->
GroupReadStatus IsPowerOn=false.

LASAL 0x209F implemented offsets:

_StdLib.MemCpy(dest:=#pos, source:=#RequestBuf[8], size:=4);
_StdLib.MemCpy(dest:=#velo, source:=#RequestBuf[12], size:=4);
_StdLib.MemCpy(dest:=#acc, source:=#RequestBuf[16], size:=4);
_StdLib.MemCpy(dest:=#dec, source:=#RequestBuf[20], size:=4);
_StdLib.MemCpy(dest:=#jer, source:=#RequestBuf[24], size:=4);
_StdLib.MemCpy(dest:=#dir, source:=#RequestBuf[28], size:=4);
_StdLib.MemCpy(dest:=#bufMode, source:=#RequestBuf[32], size:=4);
_StdLib.MemCpy(dest:=#Exec, source:=#RequestBuf[36], size:=4);

LMCAxis.MoveShortestWay(
  Position:=pos,
  Speed:=velo,
  Accel:=acc,
  Decel:=dec,
  Jerk:=jer
);

LASAL 0x20A4 implemented offsets:

GroupExecute is DINT. The C# request carries 16 DINT positions (64 bytes), while
this canonical project has four statically connected axes. The current handler
copies only the first four positions and requires wire slots 5..16 to be zero.

_StdLib.MemCpy(dest:=#GroupMovePos, source:=#RequestBuf[8], size:=16);
_StdLib.MemCpy(dest:=#GroupVelocity, source:=#RequestBuf[72], size:=4);
_StdLib.MemCpy(dest:=#GroupAccel, source:=#RequestBuf[76], size:=4);
_StdLib.MemCpy(dest:=#GroupDecel, source:=#RequestBuf[80], size:=4);
_StdLib.MemCpy(dest:=#GroupJerk, source:=#RequestBuf[84], size:=4);
_StdLib.MemCpy(dest:=#GroupCoordSystem, source:=#RequestBuf[88], size:=4);
_StdLib.MemCpy(dest:=#GroupTransitionModeInput, source:=#RequestBuf[92], size:=4);
_StdLib.MemCpy(dest:=#bufMode, source:=#RequestBuf[96], size:=4);
_StdLib.MemCpy(dest:=#GroupExecute, source:=#RequestBuf[100], size:=4);

No LREAL-to-DINT conversion is required in the PLC.

Response payloads implemented by command-specific parsers:
- AxisInfo: 8 bytes, handle/reserved[0], status UINT16[4], error INT16[6]
- Axis ReadStatus: 12 bytes, state UDINT[0], status UINT16[4], error INT16[6],
  axis error UINT16[8], status word UINT16[10]
  The canonical LASAL server writes the native _LMCAXIS_STATUS bitfield to
  state[0]. PowerOn is bit 0 (0x00000001) and Standstill is bit 25
  (0x02000000). Do not apply the legacy PMAS 0x80/0x200 readiness masks.
  StatusWord[10] is reserved and currently always 0; DS402 StatusWord is not
  connected to this response yet.
- Axis ReadActualPosition: 8 bytes, position DINT[0], status UINT16[4],
  error INT16[6]
- Group ReadStatus: 12 bytes, state UDINT[0], status UINT16[4], error INT16[6],
  group error UINT16[8], padding UINT16[10]
  0x00040000 is the project-local LASAL Power Ready extension (IsPowerOn).
  0x00020000 is the standard Maestro NC_GROUP_STANDBY_MASK and 0x00010000 is
  NC_GROUP_DISABLED_MASK. The project-local part is the adapter condition that
  maps locked standby to IsStandby/IsEnabled and unlocked disabled to IsDisabled.
- GetGroupMembersInfo: exactly 1350 bytes; see RESPONSE_MODEL_DESIGN_2026-07-09.md
- Group ReadActualPosition: exactly 68 bytes, position DINT[16] at [0..63],
  status UINT16[64], error INT16[66]. The current source copies Pos1..Pos9 and
  zero-fills slots 10..16; the intended 4-vs-9 public read contract is pending
  PLC verification. Legacy 136-byte LREAL is rejected.

Callback and ownership:
- The active/default 0x405C wire shape remains legacy. Request payload is exactly
  12 bytes:
    P0  EventMask UDINT
    P4  CallbackPort DINT, valid registered range 1..65535
    P8  CallbackIPv4 BYTE[4]
  Response payload remains exactly 4 bytes: Status UINT16 + ErrorId INT16.
- The LASAL handler validates into temporary values before commit. It requires
  CurrentPeerValid, CallbackIPv4 exactly equal to the TCP peer IPv4, and a valid
  port. The first valid tuple is committed; its exact duplicate is idempotent.
  A different re-registration fails and preserves the accepted tuple unchanged.
- Installed SIGMATEK GetBroadCastData source reconstructs IPv4 octets from a
  UDINT least-significant byte first (then SHR 8/16/24). This is static byte-order
  evidence for comparing the copied request IPv4 UDINT with
  OS_TCP_USER_GETPEERIP; PLC download and packet capture are still pending.
- No actual Maestro event datagram has been captured. Legacy callback payload is
  still exposed as raw bytes. The initial Gate C sender does not implement
  protocol-version-1 publication. Its macro remains reserved at 0, and changing
  it alone must not enable version 1. Exact proof-only fixture bytes and tests
  are required in a separate approved change. Version-1 arm/publication returns
  -6 without endpoint or FIFO mutation.
- Callback source-address validation and defensive payload copies are PC safeguards;
  they do not define a typed callback protocol.
- Raw LMCCallbackEventArgs carries SessionGeneration and exposes BelongsTo and
  BelongsToCurrentSession. The WPF dispatcher drops an event unless it still
  belongs to the active connection and current session, so a queued old-session
  event is not logged after reconnect.

Opt-in PC / dormant PLC project-local 0x405C version 2:
- The delivery source contains the bounded version-2 codec, callback models, and
  codec/fence/socket tests. LegacyRaw remains the PC default. Explicit
  Version2WakeHint mode sends 32/20, installs the exact source, BootId,
  SessionEpoch, cookie, length, policy, and sequence fences, and emits only a
  typed non-authoritative wake hint. The live receiver fence is private.
  Automatic TCP follow-up and the PLC production publisher remain absent.
- Dispatch is by exact request payload length. A 12-byte request uses legacy
  12/4. A 32-byte request uses version-2 32/20. Every other length rejects without
  endpoint mutation. There is no same-session downgrade after a v2 rejection.
- All fields are little-endian. The exact 32-byte request is:
    P0  EventMask UDINT; (EventMask AND 1) = 1
    P4  CallbackPort DINT; 1..65535
    P8  CallbackIPv4 BYTE[4]; exact current TCP peer
    P12 ProtocolVersion UINT; exactly 2
    P14 RequestedMaxDatagram UINT; 52..512
    P16 ClientCookieLo UDINT
    P20 ClientCookieHi UDINT; (ClientCookieLo OR ClientCookieHi) <> 0
    P24 Flags UDINT; exactly 0
    P28 Reserved UDINT; exactly 0
- The exact 20-byte response is:
    P0  Status UINT
    P2  ErrorId INT
    P4  AcceptedVersion UINT; 2 on success
    P6  AcceptedMaxDatagram UINT; 52..512 on success
    P8  DiagnosticsBootId UDINT; nonzero on success
    P12 SessionEpoch UDINT; nonzero on success
    P16 AcceptedFlags UDINT; exactly 0
- Arm result 0 or exact-duplicate result 1 maps to Status=0, ErrorId=0, and all
  accepted fields above. Every failure maps to Status=1, ErrorId=-1, and zeros
  for the remaining 16 bytes while preserving the prior endpoint/FIFO. The TCP
  outer status remains 0 and the payload remains exactly 20 bytes.
- A new endpoint is bound to the current trusted TCP SessionEpoch and Diagnostics
  BootId, not values supplied over UDP. An exact duplicate is idempotent. Any
  different tuple preserves the old endpoint and pending FIFO.

Opt-in PC receive / dormant PLC LMC2 UDP datagram:
- The exact fixed header is 52 bytes and has no CRC field:
    P0  Magic BYTE[4] = 4C 4D 43 32 (ASCII LMC2)
    P4  ProtocolVersion UINT = 2
    P6  HeaderBytes UINT = 52
    P8  DatagramBytes UINT = 52 + PayloadBytes, maximum 512
    P10 EventType UINT = 1 for the initial production policy
    P12 EventMaskBit UDINT = 1 for the initial production policy
    P16 BootId UDINT = accepted response value
    P20 SessionEpoch UDINT = accepted response value
    P24 CookieLo UDINT = request cookie
    P28 CookieHi UDINT = request cookie
    P32 SequenceLo UDINT
    P36 SequenceHi UDINT
    P40 EventId UDINT; any value including zero, producer correlation only
    P44 PlcTimeMs UDINT; ops.tAbsolute enqueue snapshot, never command authority
    P48 PayloadBytes UINT; format range 0..460, initial policy exactly 0
    P50 DeliveryClass BYTE = 0 for the initial production policy
    P51 Flags BYTE = 0
- The first sequence after a new arm is 1. Exact duplicate arm preserves the
  next value. Queue-full rejection consumes no sequence; enqueue assigns and
  advances it. Admission drop may leave a gap, retry preserves it, disarm/new
  arm resets it to 1, and SequenceLo wrap increments SequenceHi.
- Because initial UDP datagrams may be lost, the receiver fence accepts its
  first valid datagram as the baseline rather than requiring sequence 1.
  Thereafter unsigned delta 0 is duplicate, delta >= 0x8000000000000000 is out
  of order, and delta 1..0x7FFFFFFFFFFFFFFF is forward, including a loss gap.
- Even a fully valid datagram is only a wake hint. UDP cannot complete a motion
  command, clear ownership, acknowledge safety, or establish a terminal result.
  Only the subsequent TCP query response can update authoritative state.
- Initial production policy requires registration mask bit 1, EventType 1,
  EventMaskBit 1, DeliveryClass 0, PayloadBytes 0, and registration
  Status=0/ErrorId=0. Every EventId UDINT is accepted as opaque correlation.
  LMCCallbackProtocolPolicy.InitialV2WakeHint and targeted socket tests prove
  opt-in LmcConnection negotiation/fencing/typed dispatch against a fake peer.
  The v2 path receives into one fixed accepted-maximum buffer (52..512 bytes);
  WSAEMSGSIZE is one rejection and never reaches user dispatch. This does not
  prove live PLC traffic or authoritative TCP follow-up.

Dormant LMCUdpCallbackSender verifier state ladder:
- The exact monotonic states are VendorImported -> DerivedDeclaration ->
  DerivedWired -> DerivedCandidate. A partial, mixed, or unknown state rejects.
- VendorImported is Gate A: only the exact two vendor classes and generated
  registration exist; no derived sender, TCP client, UDP object, or UDP link
  exists.
- DerivedDeclaration is Gate B1: the exact sender declarations, empty generated
  method stubs, and mandatory internal _base Network exist. The _base Network
  retains _UDPTransceiverInterface as the base, ClassSvr/ErrorCode/ErrorMessage/
  ErrorState/State server exposure, the external _UDPTransceiver client, and
  exactly six generated connections. CriticalSection_UDP remains internal and
  is not exposed or relinked. TCPMotionInterface and every top-level Network
  file remain byte-exact to Gate A.
- DerivedWired is Gate B2: the B1 stubs remain empty, TCPMotionInterface adds
  only `CallbackSender : LMCUdpCallbackSender` with Required=false and
  Internal=false, the seven exact private v2-fence variables, and the empty
  private `DisarmRpcCallbackEndpoint() -> Result DINT` stub. Comm_Network adds
  exactly LMCUdpTransceiver1 and LMCUdpCallbackSender1 plus exactly the two
  specified callback links. No other topology delta is permitted.
- DerivedCandidate is Gate C: declarations and wiring remain exact to B2 and all
  contract-scoped sender/TCP lifecycle bodies are complete. The exact permitted custom
  source surface is the top-level LSL_DEFINES macro region plus 14 sender bodies
  and five TCP bodies. The macro is the sole non-implementation custom region.
  LASAL Save propagates exactly these four generated lines into
  Network/Comm_Network/ONE_Comm_Network_Table.st:
    //Define part of class LMCUdpCallbackSender
    #ifndef LMC_UDP_CALLBACK_ENABLE_LEGACY_FIXTURE
    #define LMC_UDP_CALLBACK_ENABLE_LEGACY_FIXTURE 0
    #endif
- State acceptance at each transition requires exact hashes captured from the
  actual canonical project after its LASAL Save All/exit checkpoint. B1 ratchets
  sender source raw/LF, Classes/include/project registries; B2 ratchets TCP,
  Network, and affected generated files; Gate C ratchets complete source and
  canonical-LF per-function bodies. Synthetic generated fixtures are planning
  and verifier-self-test inputs only, never production evidence.
- The actual current tree resolves to DerivedCandidate. Gate B1, Gate B2, and
  Gate C each have a committed physical checkpoint manifest and a validated
  A -> B1 -> B2 -> C lineage. Every derived checkpoint is capture-only with
  ProductionApproved=false and NeedsRebaseline=true. None is production or PLC
  runtime approval.

TCP v2 fence storage/lifecycle declaration contract:
- Gate B2 adds these private variables after the existing legacy callback tuple,
  immediately after RpcCallbackIPv4, in exact order and without inline
  initializers:
    RpcCallbackProtocolVersion UINT
    RpcCallbackAcceptedMaxDatagram UINT
    RpcCallbackSessionEpoch UDINT
    RpcCallbackBootId UDINT
    RpcCallbackCookieLo UDINT
    RpcCallbackCookieHi UDINT
    RpcCallbackLastDisarmResult DINT
- Gate B2 adds private non-virtual/non-global DisarmRpcCallbackEndpoint with no
  inputs and Result DINT output immediately after HandleRpcLifecycleCommands;
  its implementation was an empty generated stub at B2 and is one of the five
  completed TCP bodies in the current Gate C candidate.
- RpcCallbackProtocolVersion is the per-RPC-session shape lock. Inside an
  initialized owning RPC session, the first exact 12/32-byte registration
  attempt sets 1/2 before semantic validation, and a
  rejection still blocks the opposite shape while allowing a same-shape retry.
  RpcCallbackRegistered is the successful-commit/armed flag. Gate C calls the
  sender only for ProtocolVersion=2 and Registered=TRUE; every other tuple
  returns 1 and clears residual TCP callback fields. Armed v2 requires
  IsClientConnected(#CallbackSender), otherwise returns -9, and forwards the
  exact stored epoch/cookies. Every result is stored in
  RpcCallbackLastDisarmResult. That diagnostic latch is retained and excluded
  from the tuple clear. Only 0/1 clears the complete legacy+v2 TCP tuple;
  every negative preserves the full v2 tuple, and repeated 0x8080 initialization
  retries the same disarm and fails closed if it still cannot complete.
- V2 requires IsClientConnected(#Diagnostics), calls the current
  Diagnostics.GetDiagnosticsBootId(), and rejects zero before ArmEndpoint. The
  complete v2 tuple and RpcCallbackRegistered are committed before TCP SendData
  receives the full 28-byte frame (`udSize=28`) containing the 20-byte response
  payload. Every semantically valid v2
  arm attempt, including exact duplicate, invokes all nine ArmEndpoint inputs;
  the accepted UINT maximum
  is passed as UDINT. Legacy/v2 shape switching within one session is rejected.
- Gate C changes exactly TCPMotionInterface::ConnSocketInfo, SendData,
  HandleControlSafetyDrainPending, HandleRpcLifecycleCommands, and the new
  DisarmRpcCallbackEndpoint. The four existing methods call the helper before
  clearing callback state or incrementing SessionEpoch, and all five bodies get
  separate canonical-LF hash ratchets.
- Voluntary repeated 0x8080 initialization and explicit 0x405D close do not
  report success, clear the callback tuple, or advance the session after a
  negative disarm. Forced owner loss/takeover, partial-send quarantine, and
  safety-drain close still retire TCP after the helper call, but preserve the
  callback tuple on negative so the next initialization must retry it.
- 0x8080 validates the one-byte request and owning socket before calling the
  helper. The malformed/non-owner rejection itself does not disarm or mutate
  the callback tuple. Its failure frame still uses TCP SendData, so a partial
  failure-frame send follows the forced transport-quarantine path and may then
  disarm and advance the session. Accepted 0x405D captures the old nonzero
  SessionEpoch in PendingClosedSessionEpoch when that latch is zero before the
  ACK or increment, and must not advance SessionEpoch again when ACK SendData
  already entered partial-send quarantine.

Dormant LMCUdpCallbackSender Gate B/C contract:
- The class derives from _UDPTransceiverInterface and runs in a non-RT 10 ms
  cycle. Standard FUNCTION VIRTUAL GLOBAL CyWork(EAX UDINT) returns state(EAX)
  UDINT and must be installed through vmt.CmdTable.CyWork := #CyWork(). It is
  separate from the three public user methods.
- Exact class settings are RealtimeTask=false, CyclicTask=true,
  DefCyclictime=10 ms, BackgroundTask=false, and Sigmatek=false. Preserve the
  generated defaults OSInterface=false, HighPriority=false, Automatic=false,
  UpdateMode=Prescan, and SharedCommandTable=true. The legacy fixture define
  LMC_UDP_CALLBACK_ENABLE_LEGACY_FIXTURE defaults to 0. Do not apply pragma pack
  or private-variable inline initializers.
- TYPE declarations and field order are exact:
    _LMC_UDP_ACTIVE_ENDPOINT:
      Armed BOOL
      ProtocolVersion UINT
      EventMask UDINT
      CallbackIPv4 UDINT
      CallbackPort DINT
      SessionEpoch UDINT
      BootId UDINT
      CookieLo UDINT
      CookieHi UDINT
      MaxDatagramBytes UDINT
    _LMC_UDP_TX_SLOT:
      InUse BOOL
      ProtocolVersion UINT
      DatagramBytes UDINT
      DestinationIPv4 UDINT
      DestinationPort UDINT
      SessionEpoch UDINT
      BootId UDINT
      CookieLo UDINT
      CookieHi UDINT
      SequenceLo UDINT
      SequenceHi UDINT
      PlcTimeMs UDINT
      RetryCount UDINT
      Data ARRAY[0..511] OF BYTE
- Server declarations follow TYPE and precede private variables, in exact order:
    QueueDepth SvrCh_UDINT
    QueuedCount SvrCh_UDINT
    RingAcceptedCount SvrCh_UDINT
    AdmissionRetryCount SvrCh_UDINT
    QueueFullDropCount SvrCh_UDINT
    AdmissionErrorDropCount SvrCh_UDINT
    DisarmClearedCount SvrCh_UDINT
    TransportErrorCount SvrCh_UDINT
    LastAdmissionResult SvrCh_DINT
- Private variable declarations and order are exact:
    ActiveEndpoint _LMC_UDP_ACTIVE_ENDPOINT
    TxSlots ARRAY[0..7] OF _LMC_UDP_TX_SLOT
    ReadIndex UDINT
    WriteIndex UDINT
    Depth UDINT
    NextSequenceLo UDINT
    NextSequenceHi UDINT
  A new arm explicitly sets NextSequenceLo=1 and NextSequenceHi=0. CallbackIPv4
  remains raw UDINT. The class uses inherited Socket:DINT and State and adds no
  separate socket handle/state or client channel.
- Public GLOBAL methods, in exact order, are:
    ArmEndpoint(ProtocolVersion UINT, EventMask UDINT, CallbackIPv4 UDINT,
      CallbackPort DINT, SessionEpoch UDINT, BootId UDINT, CookieLo UDINT,
      CookieHi UDINT, MaxDatagramBytes UDINT) -> Result DINT
    DisarmEndpoint(ExpectedSessionEpoch UDINT, ExpectedCookieLo UDINT,
      ExpectedCookieHi UDINT) -> Result DINT
    PublishEvent(EventMaskBit UDINT, EventType UINT, DeliveryClass UINT,
      EventId UDINT, ProducerSessionEpoch UDINT, pPayload ^void,
      PayloadBytes UDINT) -> Result DINT
- Private methods, in exact order, are:
    EnsureSocketReady() -> Result DINT
    ValidateEndpoint(ProtocolVersion UINT, EventMask UDINT,
      CallbackIPv4 UDINT, CallbackPort DINT, SessionEpoch UDINT, BootId UDINT,
      CookieLo UDINT, CookieHi UDINT, MaxDatagramBytes UDINT) -> Result DINT
    BuildDatagram(SlotIndex UDINT, EventMaskBit UDINT, EventType UINT,
      DeliveryClass UINT, EventId UDINT, ProducerSessionEpoch UDINT,
      pPayload ^void, PayloadBytes UDINT) -> Result DINT
    FindFreeSlot() -> SlotIndex DINT
    ServiceTransmitQueue()
    SendSlot(SlotIndex UDINT) -> VendorResult DINT
    RetryOrDropSlot(SlotIndex UDINT, VendorResult DINT)
    ClearPendingFrames()
    FenceMatches(ExpectedSessionEpoch UDINT, ExpectedCookieLo UDINT,
      ExpectedCookieHi UDINT) -> Matches BOOL
- ErrorCallback is a VIRTUAL GLOBAL override with exact vendor inputs
  FSM_UDP:_UDPTransceiver::_FSM_UDP_USER,
  UdpError:_UDPTransceiver::_UDP_ERROR, ErrCode:DINT. Under one
  CriticalSection_UDP lock it directly assigns ErrorState:=FSM_UDP,
  ErrorMessage:=UdpError, and ErrorCode:=ErrCode, then saturating-increments
  TransportErrorCount. It does not depend on a base-call syntax. Asynchronous
  transport errors are not slot-correlated or retried.
- ArmEndpoint result domain is 0 new, 1 exact duplicate, -1 invalid, -2
  AddSocket failure, -3 conflict, -6 unsupported, -9 internal. Socket-open
  pending is committed as result 0.
- DisarmEndpoint result domain is 0 matched clear, 1 already disarmed/empty,
  -8 stale fence, -9 internal.
- PublishEvent result domain is 0 enqueued, -2 AddSocket failure, -4 not armed,
  -5 FIFO full/drop new, -6 unsupported policy, -7 invalid payload pointer or
  length, -8 stale producer session, -9 internal. Socket-open pending still
  enqueues and returns 0.
- The initial sender rejects version 1 with -6 regardless of the reserved
  fixture macro. Version 2 requires (CookieLo OR CookieHi) <> 0 and
  MaxDatagramBytes 52..512, nonzero SessionEpoch/BootId, valid IPv4/port, and
  EventMask bit 1. Initial version-2
  PublishEvent accepts only EventMaskBit 1, EventType 1, DeliveryClass 0, and
  PayloadBytes 0, while EventId may be any UDINT including zero.
- The application queue is exact FIFO 8 x 512 bytes with ReadIndex, WriteIndex,
  and Depth. Full depth drops only the new event and preserves queued slots.
  FindFreeSlot returns WriteIndex only when Depth<8 and
  TxSlots[WriteIndex].InUse=FALSE. The initial PLC path accepts only
  PayloadBytes=0, builds exactly the 52-byte LMC2 header synchronously in the
  slot, and sets DatagramBytes=52. It does not dereference, copy, or retain
  pPayload. SendSlot transmits only DatagramBytes. Nonzero PLC payload copy is
  not implemented, and a structurally valid nonzero payload rejects by policy
  before slot mutation.
- Gate C tests a null payload as `pPayload = NIL`, writes the DeliveryClass
  header byte with `TO_USINT(DeliveryClass)`, and calls the inherited wrapper as
  `SendData(pData, udSize, bDirect, udIpAddress, udPort)`. It does not pass a
  Socket argument or call the lower-level transceiver ABI directly.
- A matched DisarmEndpoint snapshots the old Depth, zeroes the complete
  ActiveEndpoint, and calls ClearPendingFrames. ClearPendingFrames zeroes every
  byte of all eight TxSlots and resets ReadIndex, WriteIndex, Depth, and
  QueueDepth. DisarmClearedCount saturating-adds the saved depth rather than
  incrementing once per disarm. Already-disarmed and stale-fence calls do not
  mutate this state.
- CyWork examines only the head and invokes queued SendData(bDirect:=FALSE) at
  most once. Vendor -4 retains the head for three retries, four total attempts;
  the fourth -4 drops it. Other negative admission results drop immediately.
  A retained head blocks the tail. Admission result 0 transfers ownership to the
  vendor ring; it does not prove the later OS send.
- Enqueue snapshots PlcTimeMs:=ops.tAbsolute and writes that same value at header
  offset 44. No 10 ms software accumulator is used. Queue-full consumes no
  sequence; enqueue consumes one, retry preserves it, and admission drop may
  leave a gap. SequenceLo wrap increments SequenceHi and high-word wrap returns
  explicitly to zero.
- EnsureSocketReady uses inherited Socket:DINT. Socket=0 calls AddSocket; a zero
  return is -2 failure, while every nonzero DINT bit pattern is a valid handle.
  IsOpen true returns 0 ready and false returns 1 pending. CyWork calls the
  helper every scan even while disarmed. ArmEndpoint and PublishEvent accept
  ready or pending, but do not mutate on -2. The send-only socket is preserved
  across DisarmEndpoint; the sender never calls BindSocket or DelSocket.
- Endpoint, FIFO, sequence, and counters use inherited CriticalSection_UDP.
  Each public method, CyWork, and ErrorCallback calls SectionStart and
  SectionStop exactly once, calls helpers while already locked without re-entry,
  and does not RETURN while locked. CyWork services one head only when socket
  result is 0, Armed is true, and Depth>0, then returns READY.
- Observable no-Network server channels, in exact order, are QueueDepth,
  QueuedCount, RingAcceptedCount, AdmissionRetryCount, QueueFullDropCount,
  AdmissionErrorDropCount, DisarmClearedCount, TransportErrorCount as
  SvrCh_UDINT, then LastAdmissionResult as SvrCh_DINT. Every channel uses
  Initialize=true, DefValue=0, WriteProtected=true, Retentive=false, and
  Visualized=false. UDINT counters saturate at 16#FFFFFFFF;
  DisarmClearedCount's increment is the matched pre-clear Depth.
- Gate A imported only _UDPTransceiver and _UDPTransceiverInterface. Current
  source SHA-256 values are
  C3713C1E76E0027F6E90007268BFC2DFA8962F778A7B2EE2B3E50C11F520C321
  (physical CRLF, _UDPTransceiver),
  D0D35828725B41B0E1C2323FE2120A1F492F7C6DA56254CAF9A10D07E7492DD1
  (canonical LF, _UDPTransceiver), and
  9575ED267B9629D811E18C9A5156EC4089F8223464D42DA4ADA6F1F8E8188D80
  (_UDPTransceiverInterface). Protected _StdLib, CriticalSection, and
  lsl_st_tcp_user.h hashes remain exact as listed in the callback design. The
  2026-08-07 C78/ARM Rebuild completed with zero compiler errors, Compiler Done
  twice, Linker Done, and success in 23683.9 ms. The Gate A verifier snapshot
  committed in e287d07 is
  180340 bytes with SHA-256
  F020F310CE6DFB79D504E1099E36C6F5D5703B0A75789C51AE55A5569B5BACBD;
  its self-test passed 61/61 and VendorImported passed. The exact true
  generated Classes.lcb chunks are _UDPTransceiver 52552 bytes with SHA-256
  958A2EC0945A01878261A7B055A25EBB5A44AFCADDD3BE7A2309744B69F90FAB
  and _UDPTransceiverInterface 25583 bytes with SHA-256
  7FC931079DCFBB894D29EC1A92B291E67D21A01F250B0B1639B22A82BEB614EB.
  The final
  Verify-LasalContract.ps1 -SourceOnly -ExpectedSdoWriteAxis 1 run passes in
  250.494 s with Axis1 PASS and the same verifier hash before and after. Network
  topology is unchanged; generated class registry files are the only Network-boundary
  exception. Qualified Init/AddSocket methods opened directly with no post-smoke
  CInvalidArgException. Gate A has no UDP objects/links, so class-definition
  client/server menus have no Find in Implementation. Gate B2 makes that smoke
  possible. The later Gate C smoke opened the sender and TCP implementation
  editors directly because the high-level computer-use operation failed; it did
  not execute a client/server Find in Implementation search. This does not alter
  the historical Gate A acceptance.
  The e287d07 61-fixture snapshot proves VendorImported and its then-existing
  higher-state rejection only. At that historical checkpoint, the later
  DerivedDeclaration and DerivedWired modes still required their own frozen
  verifier evidence before Gate B1 could begin; the completed records follow.
- The committed e287d07 / F020F310... / 61/61 / 250.494 s values above remain
  the historical Gate A snapshot. Later state-ladder hardening produced a
  focused verifier of 409934 bytes and 9859 lines with exact SHA-256
  E5211F3D44712ADE1B4CDE5F6AB72729993AEF530152BC36BDD695C81CDFE6FC.
  Its direct self-test and the main Verify-LasalContract.ps1
  -UdpCallbackVerifierSelfTestOnly wiring each passed 249/249. In that historical
  snapshot, the then-current VendorImported check passed with
  ProductionApproved=true and NeedsRebaseline=false. A later full
  Verify-LasalContract.ps1 -SourceOnly
  -ExpectedSdoWriteAxis 1 run passed in 242.1 s with Axis1 PASS and the same
  E5211F3D...E6FC verifier hash before and after; independent read-only review
  returned GO for that exact snapshot.
- Gate B1 DerivedDeclaration was recorded by commit 95c76fe. Its
  gate_b1_derived_declaration_checkpoint.json is 3117351 bytes with SHA-256
  F0A7DD7D192F5DE6E23F2CA921F6C0145249DE06A1F8D7B333C0FE49C7B2BFA2. It
  records the 446686-byte D126AC214DE701754CEF862167887EC0A8405BBCB6FDF59B607639DA75E00788
  verifier and a 259/259 self-test.
- Gate B2 tooling commit 85e9592 froze its verifier/capture contract; commit
  cc83311 recorded the wired source and manifest. The B2 manifest is 3122685
  bytes with SHA-256
  96DA05F0E45E3129E27C082FADE7C4C8EF48D9AFF06E0ED058803BC1C0BCC39F. It
  records the 467485-byte F553EE5D986272A9460FB6C5DB2CE18D3491FD34922EE2F1C83A1CC3665B9600
  verifier and a 262/262 self-test.
- Gate C tooling commit 9d0b8c9 freezes the 478281-byte verifier
  C0B95B5D6A6220C701C30B7EB379473C4BA43761F70D2DD5DB280AFA40FDCF12
  and the 401786-byte capture tool
  34ED0649382A4006830B714C8F57915BED786E7A22068BBFE5CC41EE3E0CB8DA.
  The verifier self-test passed 263/263; the capture-tool self-test passed 38
  positive and 64 negative cases. Trusted ValidateOnly passed in 762.1 s with
  outputCreated=false, and trusted Capture passed in 912.0 s.
- Gate C candidate commit 70c08ea records the candidate and
  gate_c_derived_candidate_checkpoint.json. That manifest is 3121423 bytes with
  SHA-256 94632D03946F337F0925D0D873A47E09162E606835DDB726F5EB1644AF407366
  and records an actual-tree DerivedCandidate PASS. Gate B1, B2, and C remain
  capture-only with ProductionApproved=false and NeedsRebaseline=true; these
  manifests and synthetic verifier fixtures are not production evidence.
- Main-wrapper parameter wiring began in a3a419e and was finalized in a6b5d5f.
  The committed wrapper explicitly supports
  -UdpCallbackExpectedState DerivedCandidate -AllowUdpCallbackDerivedCapture.
  Its current physical identity is 2061458 bytes with SHA-256
  DC9E0B02851E73265A75F2F90F1B9BA385A2E571B010BD1D8EDC4F06F36E306F.
  The full Verify-LasalContract.ps1 -SourceOnly -ExpectedSdoWriteAxis 1
  -UdpCallbackExpectedState DerivedCandidate -AllowUdpCallbackDerivedCapture
  run passed in 233.562 s with that wrapper hash unchanged before and after.
  This is full SourceOnly consumer evidence only and does not change the
  capture-only/nonproduction decision.
- A .NET SDK MSBuild Release build of LasalMotionControlLib.Tests succeeded. The
  custom runner reported TOTAL 1110, PASSED 1110, FAILED 0. Callback coverage now
  includes the codec, InitialV2WakeHint, session fencing, exact opt-in 32/20
  negotiation, gated early-datagram delivery, strict source/provenance/policy/
  sequence rejection, fixed-buffer oversized rejection, typed-only dispatch,
  endpoint evidence immutability, join-timeout gate ownership, handler-failure
  continuation, reentrant-close invalidation, safety-detach provenance, no
  downgrade, and reconnect invalidation. This
  is PC fake-peer evidence, not PLC runtime or packet-capture proof.
- The Gate C LASAL Save/Rebuild completed 128 compile lines, Compiler/Linker
  completion, zero errors, and a UI summary of 76 warnings. The relevant log
  contained no ERROR, FATAL, CInvalidArgException, or failed record. The 21 new
  W0070 diagnostics remain nonblocking warning debt after a precedence/truth-table
  audit; this is not a warning-clean build. Direct implementation editor opens
  for TCPMotionInterface and LMCUdpCallbackSender produced no new IDE exception,
  and Lasal2 exited with code 0.
- Recorder development remains frozen. Existing Recorder code and packet map
  remain, but no Recorder sample, block, completion, object, or client link may
  use this callback sender without a prior design update and explicit restart of
  that workstream.
- Multi-PC read sharing and motion/control ownership must be enforced by the LASAL
  session/dispatcher. Separate PC LMCConnection instances cannot enforce server ownership.
- The 2026-08-03 dormant common-ownership contract selects TCPMotionInterface as
  the serialized admission owner, but production owner state/token/terminal
  observation is not implemented. Ordinary Power/Reset/Stop/Move handlers still
  return ACKs without retaining a long-lived axis owner. Axis/Group Stop and
  PowerOff are safety-preemption commands and must not be rejected merely because
  Home/encoder-maintenance/motion owns the target. LMCRobot is connected to Axis1..9 and 0x20D2
  publishes that robot-connected set, while LockProfile and Cartesian profile
  moves explicitly select Axis1..4. Ownership must therefore distinguish the
  conservative robot-wide mask 0x01FF from the active profile mask 0x000F per
  command; 0x20D2 must not be reused as the profile mask.
  LMC_Home, DS402 Home and the Diagnostics SDO executor also remain independent
  capacity-one global resources until their singleton state is refactored.
  Stop/PowerOff dispatch must not wait for Home cleanup or SDO drain; dispatch it
  once immediately, then require both a stable safe state and owner-specific
  cleanup before release. Move completion additionally requires a post-acquire
  activity transition so the pre-dispatch standstill sample cannot release it.
  See docs/architecture/AXIS_COMMON_OWNERSHIP_INTERLOCK_DORMANT_CONTRACT_2026-08-03.md.
  Admin bits 4/6, Diagnostics bits 18/19, and the corresponding feature gates remain
  OFF until the bidirectional interlock, atomic Group acquisition, terminal
  release, IDE build, PLC download, and concurrent-request proof are complete.
- The project-local LMC2 schema is approved only in its dormant, fenced form
  above. The sender class/Network and TCP lifecycle candidate now exist, but
  production PublishEvent call sites remain zero. LmcConnection keeps legacy
  12/4 as its default and now has an explicit opt-in 32/20 typed wake-hint path.
  An approved event-to-authoritative-TCP-query mapping, PLC download, and live
  UDP receiver/dispatch packet proof are absent. Gate A/B/C static/build evidence
  and PC fake-peer tests are not PLC runtime proof.

MoveCircle scope:
- The current public C# API has no MoveCircle method.
- No approved LASAL-DINT command ID, payload or response contract exists for it.
- It is not part of the 23 capture-backed commands or the two project-local
  power extensions and must not be inferred from
  the vendor LMCRobot.MoveCircleCoord method alone.
