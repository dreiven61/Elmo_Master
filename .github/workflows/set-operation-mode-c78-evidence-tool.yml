name: SetOperationMode C78 evidence tool

on:
  pull_request:
    branches:
      - dev
    paths:
      - 'Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Class/LMCDiagnosticsService/LMCDiagnosticsService.st'
      - 'Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Class/Classes.lcb'
      - 'Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Elmo_EtherCAT_Test_4Axis.lcb'
      - 'tools/Capture-SetOperationModeC78Evidence.ps1'
      - 'tools/Verify-SetOperationModeDefineOrder.ps1'
      - '.github/workflows/set-operation-mode-c78-evidence-tool.yml'
  push:
    branches:
      - dev
    paths:
      - 'Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Class/LMCDiagnosticsService/LMCDiagnosticsService.st'
      - 'Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Class/Classes.lcb'
      - 'Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Elmo_EtherCAT_Test_4Axis.lcb'
      - 'tools/Capture-SetOperationModeC78Evidence.ps1'
      - 'tools/Verify-SetOperationModeDefineOrder.ps1'
      - '.github/workflows/set-operation-mode-c78-evidence-tool.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  collector-self-test:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify SetOperationMode define order
        shell: powershell
        run: |
          & '.\tools\Verify-SetOperationModeDefineOrder.ps1'
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Report tracked artifact physical identity
        shell: powershell
        run: |
          $classes = '.\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\Class\Classes.lcb'
          $project = '.\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\Elmo_EtherCAT_Test_4Axis.lcb'
          foreach ($path in @($classes, $project)) {
            $item = Get-Item -LiteralPath $path
            $sha = (Get-FileHash -LiteralPath $path -Algorithm SHA256).Hash.ToUpperInvariant()
            Write-Host ("ARTIFACT {0} bytes={1} sha256={2}" -f $path, $item.Length, $sha)
          }

      - name: Self-test fresh C78 evidence collector
        shell: powershell
        run: |
          & '.\tools\Capture-SetOperationModeC78Evidence.ps1' -SelfTest
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Diff hygiene
        shell: powershell
        run: |
          if ($env:GITHUB_EVENT_NAME -eq 'pull_request') {
            git diff --check "origin/$env:GITHUB_BASE_REF...HEAD"
          }
          elseif ($env:GITHUB_EVENT_NAME -eq 'push') {
            git diff --check HEAD^ HEAD
          }
          else {
            git diff --check
          }
