name: SetOperationMode supported-mode UI promotion

on:
  push:
    branches:
      - codex/setopmode-supported-mode-mask-current-dev
    paths:
      - 'tools/Apply-SetOperationModeSupportedModeUi.py'
      - '.github/workflows/set-operation-mode-supported-ui-promote.yml'

permissions:
  contents: write

jobs:
  promote:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: codex/setopmode-supported-mode-mask-current-dev
          fetch-depth: 0

      - name: Apply SDK and WPF promotion
        run: |
          python3 tools/Apply-SetOperationModeSupportedModeUi.py
          git diff --check

      - name: Verify production activation remains off
        shell: bash
        run: |
          grep -F '#define LMC_DIAG_SET_OPERATION_MODE_ENABLED FALSE' \
            Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Class/LMCDiagnosticsService/LMCDiagnosticsService.st
          grep -F '(pResponseFrame + 24)^$UDINT := 0x00000017;' \
            Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Class/LMCControlCommandService/LMCControlCommandService.st

      - name: Commit promoted sources and remove temporary tooling
        shell: bash
        run: |
          git rm -- tools/Apply-SetOperationModeSupportedModeUi.py \
            .github/workflows/set-operation-mode-supported-ui-promote.yml
          git add -- \
            LMC_Library/LMC_API_Delivery/src/LmcAdminSetOperationMode.cs \
            LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.AxisSetOperationModeRecovery.cs \
            LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.ReadOnlyApi.cs \
            LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/UiLocalization.cs \
            LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp.SmokeTests/RecentRecoveryPanelLocalizationTests.cs \
            LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp.SmokeTests/WpfAxisSetOperationModeRecoveryIntegrationTests.cs
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'feat(mode): bind SetOperationMode selector to PLC supported mask'
          git push origin HEAD:codex/setopmode-supported-mode-mask-current-dev
