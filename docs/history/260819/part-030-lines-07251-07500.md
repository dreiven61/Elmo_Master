>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "Encoder 버튼 활성 상태 읽기",
>   "code": "nodeRepl.write(state4.accessibility.tree.split(\"\\n\").filter(x=>/Encoder|TestReset|유지보수/.test(x)).slice(-40).join(\"\\n\"))"
> }
> ```
>
> ```text
> 						114 텍스트 해결되지 않은 Home/encoder 유지보수 복구 record가 없습니다. 명령에는 현재 capability와 명시적 확인이 계속 필요합니다. ID: TextMaintenanceRecoveryStatus
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "Encoder UI 접근성 상태 갱신",
>   "code": "await new Promise(r=>setTimeout(r,500)); state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:false}); nodeRepl.write(state4.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"ButtonArmTestReset\")||x.includes(\"ButtonExecuteTestReset\")||x.includes(\"ButtonRefreshTestResetCapabilities\")||x.includes(\"CheckTestResetPowerOffVerified\")||x.includes(\"CheckEncoderMaintenanceCompatibilityVerified\")||x.includes(\"TextTestResetResult\")).join(\"\\n\")||\"encoder-named-controls-not-exposed\")"
> }
> ```
>
> ```text
> 						184 단추 Encoder 유지보수 Capability 새로고침 ID: ButtonRefreshTestResetCapabilities
> 						188 확인란 1단계 A: 선택한 실제 축의 PowerOn=False와 안정적인 standstill을 확인했습니다. ID: CheckTestResetPowerOffVerified
> 						194 확인란 1단계 D: 선택한 drive가 이 정확한 0x20FC 유지보수 명령을 지원함을 독립적으로 확인했습니다. ID: CheckEncoderMaintenanceCompatibilityVerified
> 						196 단추 (disabled) 1단계 - Encoder 유지보수 Arm ID: ButtonArmTestReset
> 						200 단추 (disabled) 2단계 - Encoder 유지보수 1회 실행 ID: ButtonExecuteTestReset
> 						202 텍스트 Arm되거나 대기 중인 encoder 유지보수 작업이 없습니다. ID: TextTestResetResult
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "Encoder capability를 읽기 전용으로 확인",
>   "code": "state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:true}); await sky.click({window:targetWindow4,element_index:184}); await new Promise(r=>setTimeout(r,1000)); state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:false}); nodeRepl.write(state4.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextAdminCapabilities\")||x.includes(\"TextOperationState\")||x.includes(\"TextTestResetResult\")||x.includes(\"ButtonArmTestReset\")).join(\"\\n\"))"
> }
> ```
>
> ```text
> 						196 단추 (disabled) 1단계 - Encoder 유지보수 Arm ID: ButtonArmTestReset
> 						202 텍스트 Arm되거나 대기 중인 encoder 유지보수 작업이 없습니다. ID: TextTestResetResult
> 			171 텍스트 Load Axis completed ID: TextOperationState
> ```
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "Encoder capability 새로고침 실행",
>   "code": "await sky.press_key({window:targetWindow4,key:\"space\"}); await new Promise(r=>setTimeout(r,1000)); state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:false}); nodeRepl.write(state4.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextAdminCapabilities\")||x.includes(\"TextOperationState\")||x.includes(\"ButtonArmTestReset\")).join(\"\\n\"))"
> }
> ```
>
> ```text
> 						196 단추 (disabled) 1단계 - Encoder 유지보수 Arm ID: ButtonArmTestReset
> 			171 텍스트 Refresh Home/Encoder Maintenance Capabilities completed ID: TextOperationState
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "현재 Encoder 지원 비트 확인",
>   "code": "var singleTab4=state4.accessibility.tree.split(\"\\n\").find(x=>x.includes(\"탭 항목\")&&x.includes(\"단축\")); nodeRepl.write(singleTab4)"
> }
> ```
>
> ```text
> 			36 탭 항목 (selectable) 단축
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "PLC Encoder maintenance capability 결과 확인",
>   "code": "state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:true}); await sky.click({window:targetWindow4,element_index:36}); await new Promise(r=>setTimeout(r,500)); state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:false}); nodeRepl.write(state4.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"ID: TextHomeResult\")||x.includes(\"ID: TextOperationState\")).join(\"\\n\"))"
> }
> ```
>
> ```text
> 			171 텍스트 Refresh Home/Encoder Maintenance Capabilities completed ID: TextOperationState
> ```
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "Encoder capability 결과 화면 전환",
>   "code": "await sky.press_key({window:targetWindow4,key:\"space\"}); await new Promise(r=>setTimeout(r,400)); state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:false}); nodeRepl.write(state4.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"ID: TextHomeResult\")||x.includes(\"ID: TextOperationState\")).join(\"\\n\"))"
> }
> ```
>
> ```text
> 						390 텍스트 Capability를 새로고침했습니다. LMC Home=True, DS402 Home=False, EncoderTw20ErrorWarningReset=True, EncoderTw19MultiturnPositionReset=True. ID: TextHomeResult
> 			171 텍스트 Load Axis completed ID: TextOperationState
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "읽기 전용 진단 연결 종료",
>   "code": "state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:true}); await sky.click({window:targetWindow4,element_index:23}); await new Promise(r=>setTimeout(r,1300)); state4=await sky.get_window_state({window:targetWindow4,include_text:true,include_screenshot:false}); nodeRepl.write(state4.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextConnectionState\")||x.includes(\"TextOperationState\")).slice(-4).join(\"\\n\"))"
> }
> ```
>
> ```text
> 			26 텍스트 연결 끊김 ID: TextConnectionState
> 			171 텍스트 Close Connection completed ID: TextOperationState
> ```
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "진단용 예제 앱 종료",
>   "code": "await sky.click({window:targetWindow4,element_index:6}); await new Promise(r=>setTimeout(r,450)); var windowsAfterClose4=await sky.list_windows(); nodeRepl.write(windowsAfterClose4.filter(w=>String(w.title||\"\").includes(\"LASAL\")).length)"
> }
> ```
>
> ```text
> 0
> ```
>
> </details>
>
> Context compaction
>
> Source: automatic
> Status: completed
>
> 확인해 보니 단순 UI 누락은 아닙니다. 현재 앱에는 이전 SDO 쓰기 결과가 확인되지 않은 실제 복구 레코드가 남아 있어 Power On과 엔코더 리셋을 함께 막고 있습니다. 이 안전 인터록은 우회하지 않고, 정상 복구 후 두 기능이 실제로 다시 활성화되는 경로와 테스트 공백을 확인해 보완하겠습니다.
>
> 특별한 사유가 확인됐습니다. 저장 BootId `0x3B`의 이전 SDO 쓰기 결과가 `OutcomeUnverified`로 남아 있고, 현재 PLC BootId는 `0x3E`라 읽기 전용 격리 상태입니다. 엔코더 TW19는 실제 위치 기준을 바꾸는 명령이라 이 상태에서 버튼만 강제로 살리는 변경은 하지 않겠습니다. 대신 사용자가 안전 확인 후 레코드를 보관·종료하면 Power On과 TW20/TW19 진입이 다시 열리는지를 회귀 테스트로 고정하고 있습니다.
>
> 현재 기능 코드는 삭제되거나 꺼진 상태가 아닙니다. PLC도 TW20/TW19 capability를 광고하고 있습니다. 남은 작업은 안전 확인 후 stale 레코드를 종료한 다음, 재시작 세션에서 Power On뿐 아니라 두 엔코더 유지보수의 Arm 조건까지 실제로 복구되는지 자동 회귀로 확인하는 것입니다. 실제 장비에는 어떤 Power/Encoder 명령도 보내지 않고 있습니다.
>
> <details><summary>Edited a file</summary>
>
> - Wrote `./docs/architecture/LASAL_API_WPF_DIAGNOSTICS_MUTATION_RECOVERY_2026-08-13.md` (+8 -0)
> - Wrote `./docs/architecture/LASAL_API_WPF_DIAGNOSTICS_MUTATION_RECOVERY_2026-08-13.md` (+4 -1)
> - Wrote `./docs/architecture/LASAL_API_WPF_DIAGNOSTICS_MUTATION_RECOVERY_2026-08-13.md` (+3 -1)
>
> </details>
>
> 안전 장치를 풀지 않고 복구 경로를 고정했습니다. 기존 retirement→재시작 회귀에 Axis/Group Power On/Off뿐 아니라 TW20/TW19 각각의 capability, 4단계 확인 후 Arm 활성, Execute 비활성, 내부 admission 통과, 엔코더 명령 0건을 추가했습니다. 이제 관련 회귀 묶음과 변경 정합성을 마지막으로 확인합니다.
>
> <details><summary>Edited a file</summary>
>
> - Wrote `./docs/architecture/LASAL_API_WPF_DIAGNOSTICS_MUTATION_RECOVERY_2026-08-13.md` (+1 -1)
>
> </details>
</details>

특별한 안전 사유가 실제로 있어서 강제 활성화하지 않았습니다.

- 이전 SDO Write 결과가 `OutcomeUnverified`로 남아 있습니다.
- 저장 BootId는 `0x3B`, 현재 PLC는 `0x3E`입니다.
- 이 때문에 Power On과 모든 쓰기 명령이 읽기 전용 격리 상태에서 차단됩니다.
- TW19는 멀티턴 절대위치를 재설정하는 명령이므로 이 인터록을 우회하면 안 됩니다.
- 기능 자체는 살아 있습니다. 현재 PLC도 TW20/TW19 capability를 모두 `True`로 광고했습니다.

대신 안전 복구 후 기능이 다시 열리는 회귀를 보강했습니다.

- Axis/Group Power On/Off 실행 경로 확인
- TW20/TW19 각각 4개 안전확인 후 `Arm=True`
- 아직 Arm하지 않은 상태에서 `Execute=False`
- 내부 admission 정상 통과
- Encoder 명령 `0x7E53/0x7E54/0x7E55`는 0건

변경 사항은 [복구 통합 테스트](./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp.SmokeTests/WpfRecoveryRecordRetirementIntegrationTests.cs:530)와 [구현·현장시험 문서](./docs/architecture/LASAL_API_WPF_DIAGNOSTICS_MUTATION_RECOVERY_2026-08-13.md:64)에 반영했습니다. Recorder는 제외했습니다.

검증 결과:

- Release Build: PASS
- Debug/Release `Wpf.RecoveryRetirement`: 각각 `20/20 PASS`
- `git diff --check`: PASS
- 실제 Power/Encoder 명령은 보내지 않음
- 실제 저널 SHA-256과 수정 시각은 그대로 유지됨

실제 기능을 여는 마지막 절차는 사용자 물리 확인이 필요합니다.

1. 연결 후 `안전 / 복구 상세 정보`를 펼칩니다.
2. 기계와 드라이브 상태를 PLC 명령 결과와 별도로 확인합니다.
3. 이전 SDO 결과가 여전히 `UNKNOWN`임을 인정하고 확인 항목을 체크합니다.
4. `오래된 복구 레코드 보관 및 폐기`를 실행합니다.
5. 앱을 재시작하고 Axis/Group을 다시 조회합니다.

이 절차가 끝나면 Power On과 TW20/TW19 Arm이 다시 활성화됩니다.

> TW 기능 아직도 안되는데? 그리고 기본값을 TW19로 만들어
>
> User context
>
> Images:
> - `C:/Users/dreiv/AppData/Local/Temp/codex-clipboard-6bf06ed2-676f-45ac-9ad0-9887404f76af.png`

<details><summary>20 previous messages</summary>

> 다음 개발 단계로 엔코더 유지보수 UI의 표시와 실제 안전 판정을 일치시키겠습니다. 현재는 격리 상태에서도 4개 확인을 체크하면 Arm 버튼이 겉으로 활성화될 수 있지만, 클릭 후 handler에서 다시 차단되는 불일치가 있습니다. 안전 인터록은 유지하면서 버튼 상태·차단 사유·회귀 테스트를 정리하겠습니다.
>
> <details><summary>Edited 5 files</summary>
>
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.MaintenanceActions.cs` (+18 -2)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.xaml` (+6 -2)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.MaintenanceActions.cs` (+13 -0)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.xaml` (+6 -0)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.MaintenanceActions.cs` (+82 -0)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/UiLocalization.cs` (+39 -0)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp.SmokeTests/WpfMaintenanceActionIntegrationTests.cs` (+12 -0)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp/MainWindow.MaintenanceActions.cs` (+4 -3)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp.SmokeTests/WpfRecoveryRecordRetirementIntegrationTests.cs` (+96 -1)
> - Wrote `./LMC_Library/LasalApiWpfTestApp/LasalApiWpfTestApp.SmokeTests/WpfRecoveryRecordRetirementIntegrationTests.cs` (+11 -0)
>
> </details>
>
> 남아 있던 UI/handler 불일치를 수정했습니다. 이제 격리·미해결 저널·motion journal·capability·중복 Arm 상태가 Arm/최종 확인/Execute에 동일하게 반영되고, Encoder 영역에 현재 차단 원인이 직접 표시됩니다. 먼저 빌드와 두 핵심 회귀를 돌려 실제 실패 지점을 잡겠습니다.
>
> <details><summary>Edited 2 files</summary>
>
