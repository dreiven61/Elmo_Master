> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "객체 붙여넣기 실행",
>   "code": "await sky.click({window:lasalWindow,x:114,y:157}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('menu paste selected')"
> }
> ```
>
> ```text
> menu paste selected
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> </details>
>
> GUI의 복사/붙여넣기 명령은 활성 상태였지만 실제 객체가 생성되지 않았습니다. 같은 LASAL 공식 자동화 API로 내부 객체와 연결을 생성할 수 있는지 정확한 호출 형식을 확인한 뒤 이어가겠습니다.
>
> <details><summary>Called 15 tools</summary>
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "LASAL Python Pane 열기 메뉴 확인",
>   "code": "await sky.press_key({window:lasalWindow,key:'ESC'}); await sky.click({window:lasalWindow,x:193,y:43}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('view menu opened')"
> }
> ```
>
> ```text
> view menu opened
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "LASAL Python Pane 열기",
>   "code": "await sky.click({window:lasalWindow,x:245,y:133}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('python pane toggled')"
> }
> ```
>
> ```text
> python pane toggled
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "Python Pane 프롬프트 선택",
>   "code": "await sky.click({window:lasalWindow,x:84,y:936}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('python prompt focused')"
> }
> ```
>
> ```text
> python prompt focused
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 객체 생성 전 존재 여부 확인",
>   "code": "await sky.type_text({window:lasalWindow,text:\"import sigmatek.lasal.classes as classes; import sigmatek.lasal.batch as batch; prj=classes.TheApplication().Project; print batch.ExistsObject(prj,'LMCSetPositionStore','CheckSum')\"}); nodeRepl.write('preflight command typed')"
> }
> ```
>
> ```text
> preflight command typed
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 객체 존재 여부 실행",
>   "code": "await sky.press_key({window:lasalWindow,key:'ENTER'}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('preflight executed')"
> }
> ```
>
> ```text
> preflight executed
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 _CheckSum 객체 생성 명령 입력",
>   "code": "await sky.type_text({window:lasalWindow,text:\"print batch.CreateObject(prj,'LMCSetPositionStore','_CheckSum','CheckSum',120,60,False)\"}); nodeRepl.write('create object command typed')"
> }
> ```
>
> ```text
> create object command typed
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 _CheckSum 객체 생성",
>   "code": "await sky.press_key({window:lasalWindow,key:'ENTER'}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('create object executed')"
> }
> ```
>
> ```text
> create object executed
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 연결 생성 전 존재 여부 확인",
>   "code": "await sky.type_text({window:lasalWindow,text:\"print batch.ExistsConnection(prj,'LMCSetPositionStore','this','CheckSum','LMCSetPositionStore','CheckSum','ClassSvr')\"}); nodeRepl.write('connection preflight typed')"
> }
> ```
>
> ```text
> connection preflight typed
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 연결 존재 여부 실행",
>   "code": "await sky.press_key({window:lasalWindow,key:'ENTER'}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('connection preflight executed')"
> }
> ```
>
> ```text
> connection preflight executed
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "Python Pane 다시 선택",
>   "code": "await sky.click({window:lasalWindow,x:88,y:937}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('prompt focused again')"
> }
> ```
>
> ```text
> prompt focused again
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 연결 생성 전 확인 명령 입력",
>   "code": "await sky.type_text({window:lasalWindow,text:\"print batch.ExistsConnection(prj,'LMCSetPositionStore','this','CheckSum','LMCSetPositionStore','CheckSum','ClassSvr')\"}); nodeRepl.write('connection preflight typed')"
> }
> ```
>
> ```text
> connection preflight typed
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "내부 연결 생성 전 확인 실행",
>   "code": "await sky.press_key({window:lasalWindow,key:'ENTER'}); lasalState = await sky.get_window_state({window:lasalWindow}); nodeRepl.write('connection preflight executed')"
> }
> ```
>
> ```text
> connection preflight executed
> ```
