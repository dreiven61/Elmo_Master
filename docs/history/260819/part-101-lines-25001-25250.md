> OutputCodeGen: [INFO] Writing ConfigObjects.st
> OutputCodeGen: [INFO] Done
> Output I/O Manager: [INFO] Checking IO Connection Manager Entries
> Output I/O Manager: [INFO] Done
> Output: [INFO] Checking files of Connection File Manager
> Output: [INFO] Done
> OutputCodeGen: [INFO] Writing MaeExport file "C:\work\Elmo\Elmo_Master\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\MaeExp.txt"
> OutputCodeGen: [INFO] Writing MaeExport file "C:\work\Elmo\Elmo_Master\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\MaeExp.xml"
> Done - 2 error(s), 0 warning(s).
> OutputCommand: [ERROR] Command 'Build project' failed.
>
> 두 번째 Build도 동일하게 실패했으므로 `Internal=true` 텍스트만으로는 LASAL 프로젝트 DB의 client 분류가 바뀌지 않습니다. 이 경로는 더 시도하지 않겠습니다.
>
> 해결은 `CheckSum`을 정상적인 외부 Required client로 두고, `Comm_Network`에 전용 `_CheckSum` 객체를 배치해 연결하는 것입니다. 기존 class 내부에 만든 중복 객체/연결은 제거해야 합니다. 삭제·생성 명령의 정확한 API를 확인한 뒤 순서대로 드리겠습니다.
>
> 공식적으로 지원되는 외부 Client 구성으로 바꾸겠습니다. 아래 명령은 이번에 만든 class 내부의 중복 `CheckSum` 객체만 제거하고, `Comm_Network`에 전용 `_CheckSum` 객체를 생성해 연결합니다.
>
> LASAL Python Pane에서 그대로 실행하세요.
>
> ```python
> import sigmatek.lasal.classes as classes
> import sigmatek.lasal.batch as batch
>
> prj = classes.TheApplication().Project
>
> print(batch.DeleteObject(
>     prj,
>     "LMCSetPositionStore",
>     "CheckSum",
>     True))
>
> print(batch.ExistsObject(
>     prj,
>     "LMCSetPositionStore",
>     "CheckSum"))
>
> print(batch.ExistsObject(
>     prj,
>     "Comm_Network",
>     "LMCSetPositionCheckSum1"))
> ```
>
> 정상 예상값:
>
> ```text
> 1
> 0
> 0
> ```
>
> 세 값이 정확히 나오면 이어서 실행하세요.
>
> ```python
> print(batch.CreateObject(
>     prj,
>     "Comm_Network",
>     "_CheckSum",
>     "LMCSetPositionCheckSum1",
>     210,
>     1890,
>     False))
>
> print(batch.CreateConnection(
>     prj,
>     "Comm_Network",
>     "LMCSetPositionStore1",
>     "CheckSum",
>     "Comm_Network",
>     "LMCSetPositionCheckSum1",
>     "ClassSvr"))
>
> print(batch.ExistsConnection(
>     prj,
>     "Comm_Network",
>     "LMCSetPositionStore1",
>     "CheckSum",
>     "Comm_Network",
>     "LMCSetPositionCheckSum1",
>     "ClassSvr"))
> ```
>
> 정상 예상값은 `1, 1, 1`입니다. 이후 `Save All → Build`를 실행하세요. 중간 출력이 다르면 그 자리에서 멈추고 결과를 보내주세요.
>
> >>> import sigmatek.lasal.classes as classes
> >>> import sigmatek.lasal.batch as batch
> >>> prj = classes.TheApplication().Project
> >>> print(batch.DeleteObject(     prj,     "LMCSetPositionStore",     "CheckSum",     True))
> 1
> >>> print(batch.ExistsObject(     prj,     "LMCSetPositionStore",     "CheckSum"))
> 0
> >>> print(batch.ExistsObject(     prj,     "Comm_Network",     "LMCSetPositionCheckSum1"))
> 0
> >>> print(batch.CreateObject(     prj,     "Comm_Network",     "_CheckSum",     "LMCSetPositionCheckSum1",     210,     1890,     False))
> 1
> >>> print(batch.CreateConnection(     prj,     "Comm_Network",     "LMCSetPositionStore1",     "CheckSum",     "Comm_Network",     "LMCSetPositionCheckSum1",     "ClassSvr"))
> 0
> >>> print(batch.ExistsConnection(     prj,     "Comm_Network",     "LMCSetPositionStore1",     "CheckSum",     "Comm_Network",     "LMCSetPositionCheckSum1",     "ClassSvr"))
> 0
>
> 정상적으로 중단했습니다. `CreateConnection=0`인 이유는 내부 객체를 삭제한 뒤 아직 `Save All`을 하지 않아, 프로젝트 DB가 `CheckSum`을 계속 internal client로 보고 있기 때문입니다. 지금 연결을 반복하지 마세요. 먼저 저장해 client 분류를 갱신해야 합니다.
>
> 지금 `File → Save All`만 먼저 실행하세요. 저장 후 PowerShell에서 아래를 확인합니다.
>
> ```powershell
> $store = 'C:\work\Elmo\Elmo_Master\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\Class\LMCSetPositionStore\LMCSetPositionStore.st'
>
> Select-String -LiteralPath $store -Pattern `
>   'Client Name="CheckSum"', `
>   'Connection Source="this.CheckSum"'
> ```
>
> 정상 중간 상태는:
>
> - `Internal="false"`
> - `Connection Source="this.CheckSum"...` 행 없음
>
> 그 상태가 확인되면 Python Pane에서 연결을 딱 한 번 다시 만드세요.
>
> ```python
> print(batch.ExistsObject(
>     prj,
>     "Comm_Network",
>     "LMCSetPositionCheckSum1"))
>
> print(batch.CreateConnection(
>     prj,
>     "Comm_Network",
>     "LMCSetPositionStore1",
>     "CheckSum",
>     "Comm_Network",
>     "LMCSetPositionCheckSum1",
>     "ClassSvr"))
>
> print(batch.ExistsConnection(
>     prj,
>     "Comm_Network",
>     "LMCSetPositionStore1",
>     "CheckSum",
>     "Comm_Network",
>     "LMCSetPositionCheckSum1",
>     "ClassSvr"))
> ```
>
> 예상값은 `1, 1, 1`입니다. `Save All` 후 PowerShell 결과가 다르면 Python 연결을 실행하지 말고 그 결과부터 보내주세요.
>
> Retained-store reference model도 방금 수정 완료됐습니다. retirement 후 비원자적 Intent 삭제를 제거하고, `0x7D1A` commit 불확실성은 재스캔으로 결정하도록 바뀌었습니다. 사용자가 저장 작업을 하는 동안 이 모델을 독립 재실행하겠습니다.
>
> <details><summary>Edited 2 files</summary>
>
> - Wrote `./LMC_Library/LMC_API_Delivery/docs/DINT_PACKET_MAP.txt` (+6 -0)
> - Wrote `./docs/architecture/AXIS_SET_POSITION_BOUNDED_COORDINATE_CORRECTION_2026-07-31.md` (+4 -0)
>
> </details>
>
> PS C:\Windows\System32> $store = 'C:\work\Elmo\Elmo_Master\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\Class\LMCSetPositionStore\LMCSetPositionStore.st'
> PS C:\Windows\System32>
> PS C:\Windows\System32> Select-String -LiteralPath $store -Pattern `
> >>   'Client Name="CheckSum"', `
> >>   'Connection Source="this.CheckSum"'
> PS C:\Windows\System32> $store = 'C:\work\Elmo\Elmo_Master\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\Class\LMCSetPositionStore\LMCSetPositionStore.st'
> PS C:\Windows\System32>
> PS C:\Windows\System32> Select-String -LiteralPath $store -Pattern `
> >>   'Client Name="CheckSum"', `
> >>   'Connection Source="this.CheckSum"'
> PS C:\Windows\System32>
>
> 0
> >>> print(batch.ExistsObject(     prj,     "Comm_Network",     "LMCSetPositionCheckSum1"))
> 1
> >>>
> >>> print(batch.CreateConnection(     prj,     "Comm_Network",     "LMCSetPositionStore1",     "CheckSum",     "Comm_Network",     "LMCSetPositionCheckSum1",     "ClassSvr"))
> 0
> >>> print(batch.ExistsConnection(     prj,     "Comm_Network",     "LMCSetPositionStore1",     "CheckSum",     "Comm_Network",     "LMCSetPositionCheckSum1",     "ClassSvr"))
> 0
>
> 현재 출력이 없는 이유는 `DeleteObject`가 내부 객체뿐 아니라 그 객체에서 파생된 `CheckSum` Client 채널까지 함께 제거했기 때문입니다. 그래서 외부 연결 대상 Client가 없어 `CreateConnection=0`이 나온 것입니다. 더 이상 연결 명령을 반복하지 마세요. 현재 파일 상태를 확인한 뒤 Client를 외부형으로 다시 만드는 정확한 단계만 안내하겠습니다.
>
> Context compaction
>
> Source: automatic
> Status: completed
>
> 원인은 확정됐습니다. 내부 `CheckSum` 객체를 지울 때 `LMCSetPositionStore.CheckSum` 클라이언트 채널까지 같이 삭제되어, 지금은 연결할 출발점 자체가 없습니다. 현재 파일을 다시 확인한 뒤 IDE에서 채널만 복구하는 정확한 순서로 안내하겠습니다.
</details>

원인은 명확합니다. `CheckSum` 내부 객체를 삭제하면서 클래스의 `CheckSum` 클라이언트 채널까지 삭제됐습니다. 현재 남아 있는 구조는 정상입니다.

- `LMCSetPositionCheckSum1` 객체: 있음
- `LMCSetPositionStore1` 객체: 있음
- `LMCControlCommandService1 → LMCSetPositionStore1`: 연결됨
- `LMCSetPositionStore.CheckSum` 클라이언트: 없음 ← 이것만 복구해야 함

LASAL IDE에서 다음만 진행하세요.

1. `LMCSetPositionStore` 클래스의 Interface/Channels 편집 화면을 엽니다.
2. Client 채널을 추가합니다.
   - Name: `CheckSum`
   - Class/Type: `_CheckSum`
   - Required: `true`
3. 클래스 내부 Reference Network에는 `CheckSum` 객체를 추가하지 마세요.
4. `Internal` 속성은 직접 설정할 필요 없습니다. 외부 Comm Network 연결이면 저장 시 자동으로 `Internal="false"`가 됩니다.
5. `Save All`을 실행합니다.

저장 후 PowerShell로 확인하세요.

```powershell
$store = 'C:\work\Elmo\Elmo_Master\Lasal_PRG\Elmo_EtherCAT_Test_4Axis\Class\LMCSetPositionStore\LMCSetPositionStore.st'

Select-String -LiteralPath $store -Pattern `
  'Client Name="CheckSum"', `
  'Object Name="CheckSum"', `
  'Connection Source="this.CheckSum"'
```

정상 결과는 이것 하나만 나와야 합니다.

```xml
<Client Name="CheckSum" Required="true" Internal="false"/>
```

`Object Name="CheckSum"`과 `this.CheckSum`은 나오면 안 됩니다.

그 다음 LASAL Python Pane에서 연결합니다.

```python
print(batch.CreateConnection(
    prj,
    "Comm_Network",
    "LMCSetPositionStore1",
    "CheckSum",
    "Comm_Network",
    "LMCSetPositionCheckSum1",
    "ClassSvr"
))

print(batch.ExistsConnection(
    prj,
    "Comm_Network",
    "LMCSetPositionStore1",
    "CheckSum",
    "Comm_Network",
    "LMCSetPositionCheckSum1",
    "ClassSvr"
))
```

정상 기대값은 순서대로 `1`, `1`입니다. 그 뒤 `Save All`하고 Build하면 됩니다. 지금 상태에서 `CreateConnection`을 반복해도 클라이언트 채널이 없으므로 계속 `0`만 나옵니다.

> 빌드 성공
>
