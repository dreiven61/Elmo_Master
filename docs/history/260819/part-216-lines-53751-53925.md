현재 기능은 여전히 activation OFF입니다. Store/ordinary gate는 FALSE, capability는 `0x17`, max-jump는 0이며 PLC 다운로드·SRAMRETAIN 실설정·runtime·hardware E2E는 수행하지 않았습니다.

`.codex_tmp`, `outputs`, TestClass/EasyCase, 대형 history 로그, IDE 스크린샷과 packet capture 등 untracked 자료는 커밋하거나 삭제하지 않고 그대로 보존했습니다.

> 쓸대없이 추적되는 코덱스 템프랑 outputs랑 tmp에 안쓰는 내용들은 다 정리해

<details><summary>5 previous messages</summary>

> 먼저 `.codex_tmp`, `outputs`, 이름에 `tmp/temp`가 들어간 경로를 추적 여부·크기·최근 수정 시각·참조 관계로 나눠 확인하겠습니다. 삭제 대상은 해당 임시/산출물 범위로만 제한하고, 소스·문서·캡처 원본은 건드리지 않겠습니다.
>
> 정리 대상은 명확합니다.
>
> - `.codex_tmp`: 463.3MB, 8월 13일 이후 미사용, Git 추적 0
> - `outputs`: 20.1MB, 8월 12일 이후 미사용, Git 추적·소스 참조 0
> - `tmp`: 169.6MB, 렌더/문서 QA 중간 산출물, Git ignore
> - `.tmp`: 3.5MB, 로컬 빌드 DLL/PDB, Git ignore
>
> 총 약 656.5MB입니다. 최근 24시간 파일은 없고, 이제 잠금 여부와 절대 경로를 검증한 뒤 이 네 디렉터리만 삭제하겠습니다. vendored 문서 내부의 빈 `.svn/tmp`는 건드리지 않습니다.
>
> 첫 삭제 명령은 안전 정책이 변수 기반 재귀 삭제로 판단해 실행 전에 차단했습니다. 아직 삭제된 것은 없습니다. 앞서 검증한 절대 경로를 변수 없이 한 디렉터리씩 명시해 다시 처리하겠습니다.
>
> 삭제 자체는 완료됐고 약 656.5MB가 정리됐습니다. Git 추적 파일이나 stage 변화는 0입니다. 재생성을 막기 위해 현재 `.gitignore`를 확인해 `.codex_tmp`와 `outputs`가 다시 상태 목록에 나타나지 않도록 최소 규칙도 추가하겠습니다. `tmp`는 이미 ignore 규칙이 있습니다.
>
> <details><summary>Edited a file</summary>
>
> - Wrote `./.gitignore` (+2 -0)
>
> </details>
</details>

정리 완료했습니다.

- 삭제: `.codex_tmp`, `outputs`, `tmp`, `.tmp`
- 회수 용량: 약 626.11MiB
- 모두 Git 미추적/ignore 대상이며 최근 사용·잠금·소스 참조가 없었습니다.
- vendored 문서 내부의 빈 `.svn/tmp`와 다른 untracked 자료는 보존했습니다.
- Git tracked/staged 변경: 0

재발 방지 규칙도 추가해 커밋했습니다.

- `e691aad chore(repo): ignore Codex and output work caches`
- 추가 규칙: `/.codex_tmp/`, `/outputs/**/_work/`

삭제된 파일은 Git으로 복구할 수 없지만, 모두 로컬 빌드·렌더·QA·중간 출력물이므로 필요하면 관련 도구에서 다시 생성해야 합니다. 원격 push는 하지 않았습니다.

> 테스트랑 히스토리 폴더 내용들도 정리해서 커밋해

<details><summary>9 previous messages</summary>

> Context compaction
>
> Source: automatic
> Status: completed
>
> 테스트·히스토리 폴더에서 Git 추적 파일과 의미 있는 증거는 보존하고, 미추적 임시 산출물·중복 로그·스크린샷 캐시만 정확히 분류해 삭제하겠습니다. 먼저 두 영역과 참조 관계를 병렬 감사한 뒤, 삭제 목록을 고정하고 커밋까지 진행합니다.
>
> 현재 Git 변경은 없고, 남은 후보는 모두 미추적입니다. 특히 `docs/history`의 원본 로그가 약 375MiB라서, 정리된 추적 히스토리와 중복되는지 확인한 뒤 원본만 제거하고 재발 방지 ignore 규칙을 커밋하는 방향으로 좁히고 있습니다.
>
> 보존 기준을 확정했습니다. 추적된 테스트 증거와 실제 패킷 캡처, 명시적으로 보존 중인 `260721` 원본은 유지합니다. 대신 8월 10일·13일 대형 히스토리는 각각 짧은 역사 요약 1개만 남기고 원본/청크/manifest를 제거하며, LASAL UI 스크린샷·임시 소스 스냅샷·미완성 smoke 산출물도 삭제합니다.
>
> <details><summary>Created 3 files, edited a file, deleted 2 files</summary>
>
> - Deleted `./docs/history/260810/99_analysis_summary.md` (+0 -169)
> - Deleted `./docs/history/260813/99_analysis_summary.md` (+0 -214)
> - Wrote `./docs/history/260810/99_analysis_summary.md` (+51 -0)
> - Wrote `./docs/history/260813/99_analysis_summary.md` (+59 -0)
> - Wrote `./.gitignore` (+21 -0)
> - Wrote `./docs/history/260807/99_analysis_summary.md` (+40 -0)
>
> </details>
>
> 삭제 대상은 405개, 254.5MiB로 고정했고 모두 작업공간 내부·Git 미추적·비심볼릭·잠금 없음임을 재검증했습니다. 이제 파일 단위로만 삭제하고, 추적된 테스트 증거·106개 pcap 원본·`260721` 보존 원본은 건드리지 않습니다.
>
> 정리 후 `test`의 미추적 항목은 0개가 됐고, 히스토리는 날짜별 요약 3개만 새 추적 대상으로 남았습니다. 실제 pcap 121개와 기존 추적 테스트 280개, 추적 히스토리 1,394개는 그대로입니다. 이제 이 네 파일만 명시적으로 스테이징해 diff를 검토하고 커밋합니다.
>
> <details><summary>Edited 3 files</summary>
>
> - Wrote `./docs/history/260807/99_analysis_summary.md` (+2 -1)
> - Wrote `./docs/history/260810/99_analysis_summary.md` (+2 -1)
> - Wrote `./docs/history/260813/99_analysis_summary.md` (+2 -1)
>
> </details>
>
> 검증 결과 staged 삭제는 0개이고, 남은 ignored 테스트 파일은 pcap 원본 106개뿐이며 ignored 히스토리는 명시 보존된 `260721` 원본 1개뿐입니다. `git diff --check`와 cached check도 통과했으므로 정리 커밋을 생성합니다.
</details>

완료했습니다. 커밋은 `5c80afe chore(repo): compact test and history artifacts`입니다.

- 미추적/임시 파일 405개, 254.5MiB 영구 삭제
- 8월 7·10·13일 기록은 [날짜별 요약](./docs/history/260813/99_analysis_summary.md)만 보존
- 테스트 pcap 121개와 기존 추적 증거, `260721` 원본은 보존
- [.gitignore](./.gitignore)에 재발 방지 규칙 추가
- `test`, `docs/history` 미추적 파일 0개
- `git diff --check`, cached check, 커밋 검증 통과
- 관련 없는 `TestClass/`, `EasyCase/` 미추적 파일은 건드리지 않음
- push는 하지 않았습니다.

삭제 파일은 미추적/ignored 파일이어서 Git으로 복구할 수 없습니다.

> 커밋 안된 TestClass랑 LMCSetPositionStore.etf는 왜 그런거야?

<details><summary>1 previous message</summary>

> 두 항목이 실제 프로젝트에 등록된 소스인지, IDE가 만든 임시 시험 산출물인지 현재 파일·프로젝트 참조·Git ignore 기준으로 다시 확인하겠습니다. 확인된 근거로 각각 커밋 대상 여부를 바로 설명드리겠습니다.
</details>

결론부터 말하면 둘 다 커밋 대상이 아니라 임시 파일입니다.

- `TestClass`
  - 2026-07-23 생성된 LASAL 실험용 scratch입니다.
  - 구현은 `Server := 1;`뿐이고, `.lcp`, `Classes.lcb`, Network 어디에도 등록되지 않았습니다.
  - `.lba`는 생성물이어서 이미 ignore됩니다.
  - 미추적 `.st`는 엄격한 배포 검사를 방해할 수 있습니다([Build-LmcApiDistribution.ps1](./LMC_Library/LMC_API/Build-LmcApiDistribution.ps1:521)).
  - 커밋하지 말고 폴더 전체를 삭제하는 게 맞습니다.

- `LMCSetPositionStore.etf`
  - 81바이트짜리 EasyCase 일회성 import 입력입니다.
  - 내용은 RETAIN 배열 선언 하나이며, 실제 결과는 이미 [global_LMCSetPositionStore.st](./Lasal_PRG/Elmo_EtherCAT_Test_4Axis/Class/LMCSetPositionStore/global_LMCSetPositionStore.st:5)에 반영돼 `e03352b`로 커밋됐습니다.
  - 프로젝트나 테스트에서 `.etf`를 참조하지 않습니다. 이것도 삭제 대상입니다.

직전 커밋은 `test/`와 `docs/history/`만 정리하는 범위였기 때문에, `Lasal_PRG` 아래 사용자 파일을 임의로 지우지 않고 남겨둔 것입니다. 둘을 삭제해도 미추적 파일이라 새 커밋 변경분은 생기지 않습니다.

> 삭제대상이면 삭제해

<details><summary>2 previous messages</summary>

> 확인된 두 임시 대상만 삭제하겠습니다. 삭제 전 경로가 작업공간 내부인지, Git 추적 파일이 없는지, 잠금이 없는지 다시 검사한 뒤 `TestClass` 폴더와 `.etf` 파일만 제거합니다.
>
> <details><summary>Ran a command</summary>
>
> - Ran `$ErrorActionPreference='Stop'
> $workspace=(Resolve-Path -LiteralPath '.').Path.TrimEnd('\')
> $testClassDir='Lasal_PRG\Elmo_EtherCAT_Test_4Axis\Class\TestClass'
> $etf='Lasal_PRG\Elmo_EtherCAT_Test_4Axis\EasyCase\LMCSetPositionStore.etf'
> $tracked=@(git -c core.quotepath=false ls-files -- $testClassDir $etf)
> if($tracked.Count -ne 0){throw "Tracked target refused: $($tracked -join ', ')"}
> $targets=[Collections.Generic.List[string]]::new()
> if(Test-Path -LiteralPath $testClassDir){foreach($f in Get-ChildItem -LiteralPath $testClassDir -File -Force){$targets.Add($f.FullName)}}
> if(Test-Path -LiteralPath $etf -PathType Leaf){$targets.Add((Resolve-Path -LiteralPath $etf).Path)}
> $bytes=0L
> foreach($full in $targets){
>  $resolved=[IO.Path]::GetFullPath($full)
>  if(-not $resolved.StartsWith($workspace+'\',[StringComparison]::OrdinalIgnoreCase)){throw "Outside workspace: $resolved"}
>  $item=Get-Item -LiteralPath $resolved -Force
>  if(($item.Attributes -band [IO.FileAttributes]::ReparsePoint) -ne 0){throw "Reparse target refused: $resolved"}
>  try{$s=[IO.File]::Open($resolved,[IO.FileMode]::Open,[IO.FileAccess]::ReadWrite,[IO.FileShare]::None);$s.Dispose()}catch{throw "Locked target refused: $resolved :: $($_.Exception.Message)"}
>  $bytes += $item.Length
> }
> foreach($full in $targets){[IO.File]::Delete($full)}
> if(Test-Path -LiteralPath $testClassDir){
>  $resolvedDir=(Resolve-Path -LiteralPath $testClassDir).Path
>  if(-not $resolvedDir.StartsWith($workspace+'\',[StringComparison]::OrdinalIgnoreCase)){throw "Outside workspace directory: $resolvedDir"}
>  if(@(Get-ChildItem -LiteralPath $resolvedDir -Force).Count -ne 0){throw "TestClass directory not empty after exact file deletion"}
>  [IO.Directory]::Delete($resolvedDir)
> }
> $easyCaseDir=Split-Path -Parent $etf
> if(Test-Path -LiteralPath $easyCaseDir){
>  $resolvedDir=(Resolve-Path -LiteralPath $easyCaseDir).Path
>  if(@(Get-ChildItem -LiteralPath $resolvedDir -Force).Count -eq 0){[IO.Directory]::Delete($resolvedDir)}
> }
> [pscustomobject]@{DeletedFiles=$targets.Count;DeletedBytes=$bytes;TestClassExists=(Test-Path -LiteralPath $testClassDir);EtfExists=(Test-Path -LiteralPath $etf);TrackedDeleted=0} | Format-List
> git status --short --branch`
>
> </details>
</details>

삭제 완료했습니다.

- `Class/TestClass/` 전체 삭제
- `EasyCase/LMCSetPositionStore.etf` 삭제
- 총 3파일, 9,612바이트
- 추적 파일 삭제 0개
- 현재 Git 작업 트리 clean

미추적/ignored 파일이어서 추가 커밋은 없으며 Git으로 복구할 수 없습니다.
