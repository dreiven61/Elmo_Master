>
> node_repl.js
>
> ```json
> {
>   "title": "화면 캡처 상태 확인",
>   "code": "nodeRepl.write(state2.screenshots?.map(s=>({keys:Object.keys(s),type:typeof s, summary:String(s).slice(0,100)})))"
> }
> ```
>
> ```text
> [
>   {
>     keys: [
>       'id',      'zIndex',
>       'url',     'originX',
>       'originY', 'width',
>       'height'
>     ],
>     type: 'object',
>     summary: '[object Object]'
>   }
> ]
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "기본 접힘 화면 검토",
>   "code": "await nodeRepl.emitImage(state2.screenshots[0].url)"
> }
> ```
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "안전/복구 접기 헤더 확인",
>   "code": "nodeRepl.write(state2.accessibility.tree.split(\"\\n\").slice(26,42).join(\"\\n\"))"
> }
> ```
>
> ```text
> 			25 텍스트 연결 상태
> 			26 텍스트 연결 끊김 ID: TextConnectionState
> 			27 텍스트 콜백 수신기
> 			28 텍스트 중지됨 ID: TextCallbackState
> 			29 그룹 RPC 초기화 / 콜백 증거 ID: ExpanderRpcCallbackEvidence
> 				30 단추 RPC 초기화 / 콜백 증거 ID: HeaderSite
> 					31 텍스트 RPC 초기화 / 콜백 증거
> 		32 그룹 안전 / 복구 상세 정보 ID: ExpanderSafetyAndRecoveryDetails
> 			33 단추 안전 / 복구 상세 정보 ID: HeaderSite
> 				34 텍스트 안전 / 복구 상세 정보
> 		35 탭 ID: TabsMotion
> 			36 탭 항목 (selectable) 단축
> 				37 텍스트 단축
> 				38 창 ID: ScrollSingleAxis
> 					39 그룹 축 객체
> 						40 텍스트 축 객체
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "안전/복구 상세 펼침 확인",
>   "code": "await sky.click({window:targetWindow2,element_index:33}); await new Promise(r=>setTimeout(r,250)); state2 = await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:true}); nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"ExpanderSafetyAndRecoveryDetails\")||x.includes(\"TextMotionWarning\")||x.includes(\"PanelRecoveryIdentityRetirement\")).join(\"\\n\")); await nodeRepl.emitImage(state2.screenshots[0].url)"
> }
> ```
>
> ```text
> 		32 그룹 안전 / 복구 상세 정보 ID: ExpanderSafetyAndRecoveryDetails
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "연결 전 접기 상태 복원",
>   "code": "nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"HeaderSite\")||x.includes(\"ButtonConnect\")||x.includes(\"TextConnectionState\")).slice(0,8).join(\"\\n\"))"
> }
> ```
>
> ```text
> 			21 단추 연결 ID: ButtonConnect
> 			26 텍스트 연결 끊김 ID: TextConnectionState
> 				30 단추 RPC 초기화 / 콜백 증거 ID: HeaderSite
> 			33 단추 안전 / 복구 상세 정보 ID: HeaderSite
> 			168 단추 실행 로그 / 콜백 진단 ID: HeaderSite
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "격리 연결에서도 기본 접힘 확인",
>   "code": "await sky.click({window:targetWindow2,element_index:33}); await new Promise(r=>setTimeout(r,200)); state2=await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:false}); await sky.click({window:targetWindow2,element_index:21}); await new Promise(r=>setTimeout(r,1500)); state2=await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:true}); nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextConnectionState\")||x.includes(\"ExpanderSafetyAndRecoveryDetails\")||x.includes(\"TextMotionWarning\")||x.includes(\"ButtonArchiveAndRetireStaleRecovery\")).join(\"\\n\")); await nodeRepl.emitImage(state2.screenshots[0].url)"
> }
> ```
>
> ```text
> 			26 텍스트 연결 끊김 ID: TextConnectionState
> 		32 그룹 안전 / 복구 상세 정보 ID: ExpanderSafetyAndRecoveryDetails
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "연결 상태 UI 요소 재확인",
>   "code": "nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"HeaderSite\")||x.includes(\"ButtonCloseConnection\")||x.includes(\"TextConnectionState\")).slice(0,12).join(\"\\n\"))"
> }
> ```
>
> ```text
> 			23 단추 (disabled) 닫기 ID: ButtonCloseConnection
> 			26 텍스트 연결 끊김 ID: TextConnectionState
> 				30 단추 RPC 초기화 / 콜백 증거 ID: HeaderSite
> 			33 단추 안전 / 복구 상세 정보 ID: HeaderSite
> 			168 단추 실행 로그 / 콜백 진단 ID: HeaderSite
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "격리 연결 완료 대기",
>   "code": "await new Promise(r=>setTimeout(r,1600)); state2=await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:false}); nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"ButtonCloseConnection\")||x.includes(\"TextConnectionState\")||x.includes(\"HeaderSite\")).slice(0,10).join(\"\\n\"))"
> }
> ```
>
> ```text
> 			23 단추 닫기 ID: ButtonCloseConnection
> 			26 텍스트 연결됨 ID: TextConnectionState
> 				30 단추 RPC 초기화 / 콜백 증거 ID: HeaderSite
> 			33 단추 안전 / 복구 상세 정보 ID: HeaderSite
> 			168 단추 실행 로그 / 콜백 진단 ID: HeaderSite
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "상세 표시 상태 확인",
>   "code": "nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextRpcInitialization\")||x.includes(\"TextMotionWarning\")||x.includes(\"ButtonArchiveAndRetireStaleRecovery\")||x.includes(\"PanelRecoveryIdentityRetirement\")).join(\"\\n\")||\"no-detail-elements\")"
> }
> ```
>
> ```text
> 				208 텍스트 Attempt=1, Outcome=Connected, CandidateOrdinal=1, Remote=10.10.150.1:4000, Local=10.10.150.13, RequestedCallback=10.10.150.13:5000, Mode=Version2WakeHint, BoundCallback=10.10.150.13:5000, LocalSessionGeneration=1, 0x8080Attempts=1, Retry=False, InitOutcome=Succeeded, StartedUtc=2026-08-18T00:39:43.8233328Z, CompletedUtc=2026-08-18T00:39:43.8353289Z, LastACK={FrameValid=True, HeaderStatus=0, HeaderReserved=0, PayloadLength=24, HasCommandResult=False, CommandStatus=0, ErrorId=0}, Current=Active ID: TextRpcInitialization
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "상단은 접고 안전/복구 상세만 펼침",
>   "code": "await sky.click({window:targetWindow2,element_index:30}); await new Promise(r=>setTimeout(r,250)); state2=await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:false}); var lowerHeaderLine2=state2.accessibility.tree.split(\"\\n\").find(x=>x.includes(\"단추 안전 / 복구 상세 정보 ID: HeaderSite\")); nodeRepl.write(lowerHeaderLine2)"
> }
> ```
>
> ```text
> 			33 단추 안전 / 복구 상세 정보 ID: HeaderSite
> ```
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "격리 상태 안전/복구 상세 한 번에 표시 확인",
>   "code": "await sky.click({window:targetWindow2,element_index:33}); await new Promise(r=>setTimeout(r,300)); state2=await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:true}); nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextMotionWarning\")||x.includes(\"ButtonArchiveAndRetireStaleRecovery\")||x.includes(\"TextRecoveryIdentityRetirementSnapshot\")).join(\"\\n\")); await nodeRepl.emitImage(state2.screenshots[0].url)"
> }
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "안전/복구 상세 좌표 클릭 재확인",
>   "code": "await sky.click({window:targetWindow2,screenshotId:state2.screenshots[0].id,x:40,y:512}); await new Promise(r=>setTimeout(r,300)); state2=await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:true}); nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextMotionWarning\")||x.includes(\"ButtonArchiveAndRetireStaleRecovery\")||x.includes(\"TextRecoveryIdentityRetirementSnapshot\")).join(\"\\n\")||\"detail-elements-not-exposed\"); await nodeRepl.emitImage(state2.screenshots[0].url)"
> }
> ```
>
> ```text
> detail-elements-not-exposed
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
>
> node_repl.js
>
> ```json
> {
>   "title": "안전/복구 헤더 키보드 토글 확인",
>   "code": "await sky.click({window:targetWindow2,element_index:33}); await sky.press_key({window:targetWindow2,key:\"space\"}); await new Promise(r=>setTimeout(r,250)); state2=await sky.get_window_state({window:targetWindow2,include_text:true,include_screenshot:true}); nodeRepl.write(state2.accessibility.tree.split(\"\\n\").filter(x=>x.includes(\"TextMotionWarning\")||x.includes(\"ButtonArchiveAndRetireStaleRecovery\")||x.includes(\"TextRecoveryIdentityRetirementSnapshot\")).join(\"\\n\")||\"detail-elements-not-exposed\"); await nodeRepl.emitImage(state2.screenshots[0].url)"
> }
> ```
>
> ```text
> 			238 텍스트 안전: 복구 ID 읽기 전용 격리. 저장된 복구 ID가 현재 PLC와 일치하지 않습니다. 일반 non-D5 읽기 전용 확인, 로컬 초안 편집, Close/Exit만 허용됩니다. 현재 PLC 상태로 이전 명령 결과를 추정하지 마십시오. durable 복구 레코드는 변경되지 않습니다. Reconnect diagnostics mutation recovery identity 은(는) DiagnosticsBootId 또는 MapRevision이 durable 복구 레코드와 달라 차단됐습니다: Diagnostics SDO Write recovery record. Stored BootId=0x0000003B, current BootId=0x0000003E, stored MapRevision=0x957F101E, current MapRevision=0x957F101E. ID: TextMotionWarning
> 			240 편집 ID: TextRecoveryIdentityRetirementSnapshot
> 			249 단추 (disabled) 오래된 복구 레코드 보관 및 폐기 Description: PLC 명령은 전송되지 않습니다. 현재 RPC 연결을 닫고 새로 연결해 현재 PLC ID를 확인한 뒤에만 제어 또는 Write가 허용됩니다. ID: ButtonArchiveAndRetireStaleRecovery
> ```
>
> Image output: image/jpeg
>
> Image output: image/jpeg
>
> MCP tool call
