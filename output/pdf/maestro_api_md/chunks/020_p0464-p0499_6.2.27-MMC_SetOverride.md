# 6.2.27 MMC_SetOverride

- Source PDF: `Maestro Administrative and Motion API_2022_12_v2.012_bookmarks_fixed.pdf`
- PDF physical pages: 464-499
- Chunk: `020_p0464-p0499_6.2.27-MMC_SetOverride.md`

## Active Outline At Chunk Start
- p. 313 - Chapter 6 Motion and Administrative - Single Axis
  - p. 393 - 6.2 Single Axis Administrative Control
    - p. 464 - 6.2.27 MMC_SetOverride

## Contained Bookmark Outline
    - p. 464 - 6.2.27 MMC_SetOverride
    - p. 467 - 6.2.28 MMC_SetPosition
    - p. 469 - 6.2.29 MMC_TouchProbeEnable
    - p. 471 - 6.2.30 MMC_TouchProbeDisable
    - p. 473 - 6.2.31 MMC_WriteBoolParameter
    - p. 476 - 6.2.32 MMC_GlobalWriteBoolParameter
    - p. 479 - 6.2.33 MMC_WriteDigitalOutputs
    - p. 482 - 6.2.34 MMC_WriteDigitalOutputs32Bit
    - p. 485 - 6.2.35 MMC_WriteParameter
    - p. 488 - 6.2.36 MMC_GlobalWriteParameter
    - p. 491 - 6.2.37 MMC_ChngOpMode
    - p. 494 - 6.2.38 MMC_ChangeOpModeEx
    - p. 497 - 6.2.39 MMC_SetProfileConditioning

## Extracted Text

### PDF page 464
<a id="pdf-page-464"></a>
###### 6.2.27 MMC_SetOverride
6.2.27 MMC_SetOverride
Sets the values of override for the whole axis, including all functions that are operating on that axis.
MMC_LIB_API int MMC_SetOverrideCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_SETOVERRIDE_IN* pInParam,
OUT MMC_SETOVERRIDE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source Function
GMAS\includes\MMC_PLCopen_single_API.h
Structures
GMAS\includes\MMC_PLCopen_group_API.h
IEC
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_SETOVERRIDE input data structure using the MMC_SetOverride
function.
pOutParam
Points to the MMC_SETOVERRIDE_OUT output structure receiving information as a
result of calling the MMC_SetOverride function.
Remarks
The override parameters are applied to the commanded velocity, acceleration, deceleration, and jerk of the
relevant Motion function block.
MMC_SetOverride does not influence the state diagram of the axis. The following should be noted:
- The Input fAccFactor acts on positive and negative acceleration (deceleration).
- This function block sets the override factor, which is valid until a new override is set.
- The default values of the override factor are 1.0. The value of the overrides can be between 0.0 and 1.0.
The behavior of values > 1.0 is vendor specific. Values < 0.0 are not allowed. The value 0.0 is not allowed
for fAccFactor and fJerkFactor.

### PDF page 465
<a id="pdf-page-465"></a>
- The value 0.0 set to the fVelFactor stops the axis without bringing it to the state Standstill.
- Override does not act on slave axes. (Axes in the state synchronized motion).
- fVelFactor can be changed at any time and acts directly on the ongoing motion.
- If in Discrete motion, reducing the fAccFactor and/or fJerkFactor can lead to a position overshoot - a
possible cause of damage.
Activating this function block on an axis that is under control of MC_PositionProfile, MC_VelocityProfile, or
MC_AccelerationProfile, is vendor specific.
Scope
All

MMC_SETOVERRIDE_IN Structure
typedef struct{
float fVelFactor;
float fAccFactor;
float fJerkFactor;
unsigned short usUpdateVelFactorIdx;
unsigned char ucEnable;
}MMC_SETOVERRIDE_IN;
Parameters
fVelFactor
New override factor for the velocity. Any positive float value between [0 - 1].
fAccFactor
New override factor for the acceleration/deceleration. ACC/Jerk Factors are NOT
supported at this time. For future compatibility, enter "1" in the function call.
fJerkFactor
New override factor for the jerk. ACC/Jerk Factors are NOT supported at this time. For
future compatibility, enter "1" in the function call.
usUpdateVelFactorIdx
Index of changed velocity factor. Vendor defined. The default is 0. Has integer values of
0 - 2
This variable is not in use at this moment.
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_SETOVERRIDE_OUT Structure

### PDF page 466
<a id="pdf-page-466"></a>
typedef struct{
unsigned short usStatus;
short sErrorID;
} MMC_SETOVERRIDE_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 128 describes the function block for MMC_SetOverride as applied within the IEC 61131 programming.
[PDF field-code object omitted]
Figure 128: MMC_SetOverride function block
6.2.27.1 Function Block Code Example
int rc;
MMC_SETOVERRIDE_IN stSetOverride_in;
MMC_SETOVERRIDE_OUT stSetOverride_out;
//
// Inserting the structure parameters:
stSetOverride_in.fAccFactor = 0.5; // New override factor for the
acceleration/deceleration
stSetOverride_in.fJerkFactor = 0.4; // New override factor for the
jerk
stSetOverride_in.usUpdateVelFactorIdx = 0; // Index of changed velocity
factor
stSetOverride_in.fVelFactor = 0.8; // New override factor for the
velocity
stSetOverride_in.ucEnable = 1; // Enabled
//
rc = MMC_SetOverrideCmd (hConn, iAxisRef, &stSetOverride_in,
&stSetOverride_out);
if (rc != 0)
{
HandleError();
}

### PDF page 467
<a id="pdf-page-467"></a>
###### 6.2.28 MMC_SetPosition
6.2.28 MMC_SetPosition
Sends the Set Position command to the Maestro for ac specific axis.
MMC_LIB_API int MMC_SetPositionCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_SETPOSITION_IN* pInParam,
OUT MMC_SETPOSITION_OUT* pOutParam
);
Motion Mode NC - Not Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_SETPOSITION input data structure using the MMC_SetPosition
function.
pOutParam
Points to the MMC_SETPOSITION_OUT output structure receiving information, as a
result of calling the MMC_SetPosition function.
Remarks
None
Scope
All
MMC_SETPOSITION_IN Structure
typedef struct{
unsigned char ucExecute;
double dbPosition;
double dbModulus;
unsigned char ucPosMode;
}MMC_SETPOSITION_IN;

### PDF page 468
<a id="pdf-page-468"></a>
Parameters
ucExecute
Start the execution command at the rising edge. Boolean TRUE/FALSE values.
dbPosition
Target position for the motion of the axis when conditions are met. Any negative or
positive double values in technical unit [u].
dbModulus
The relative modulus of the axis. Any negative or positive double values in technical
unit [u].
ucPosMode
Boolean values for the position mode can be absolute mode = 0, or relative mode = 1
MMC_SETPOSITION_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_SETPOSITION_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 129 describes the function block for MMC_SetPosition as applied within the IEC 61131 programming.
[PDF field-code object omitted]
Figure 129: MMC_SetPosition function block

### PDF page 469
<a id="pdf-page-469"></a>
###### 6.2.29 MMC_TouchProbeEnable
6.2.29 MMC_TouchProbeEnable
Enables the touch probe to record an axis position at a trigger event.
MMC_LIB_API int MMC_TouchProbeEnableCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_TOUCHPROBEENABLE_IN*pInParam,
OUT MMC_TOUCHPROBEENABLE_OUT*pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_TOUCHPROBEENABLE input data structure using the
MMC_TouchProbeEnable function.
pOutParam
Points to the MMC_TOUCHPROBEENABLE_OUT output structure receiving information
as a result of calling the MMC_TouchProbeEnable function.
Remarks
This function block is intended for single shot operation, that is, the first event after the rising edge at
Execute is valid for recording only. Possible following events are ignored
One function block instance should represent exactly one probing command
In case of multiple instances on the same probe and axis, the elements of TRIGGER_REF should be extended
with TouchProbeID - Identification of a unique probing command - this can be linked to MC_AbortTrigger.
Scope
Supported for Gold Servo Drives only.
User notification when touch probe is over via Callback and event.
MMC_TOUCHPROBEENABLE_IN Structure

### PDF page 470
<a id="pdf-page-470"></a>
typedef struct mmc_touchprobeenable_in{
unsigned char ucExecute;
unsigned char ucTriggerType;
}MMC_TOUCHPROBEENABLE_IN;
Parameters
ucExecute
Start the execution command. Boolean TRUE/FALSE values.
ucTriggerType
Trigger enables the touch proble. Reference to the trigger signal source, where the
trigger input may be specified by the AXIS_REF. Has the following enumerator values:
eMMC_TOUCHPROBE_POS_EDGE = 0,
eMMC_TOUCHPROBE_NEG_EDGE
MMC_TOUCHPROBEENABLE_OUT Structure
typedef struct mmc_touchprobeenable_out{
unsigned short usStatus;
short sErrorID;
}MMC_TOUCHPROBEENABLE_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections 4.3 Maestro Error IDs, and NC Profiler Error
IDs. Displays an error code as negative or positive integers.
Figure 130 describes the function block for MMC_TouchProbeEnable as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 130: MMC_TouchProbeEnable function block

### PDF page 471
<a id="pdf-page-471"></a>
###### 6.2.30 MMC_TouchProbeDisable
6.2.30 MMC_TouchProbeDisable
Disables the touch probe to record an axis position at a trigger event.
MMC_LIB_API int MMC_TouchProbeDisableCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_TOUCHPROBEDISABLE_IN* pInParam,
OUT MMC_TOUCHPROBEDISABLE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_TOUCHPROBEDISABLE input data structure using the
MMC_TouchProbeDisable function.
pOutParam
Points to the MMC_TOUCHPROBEDISABLE_OUT output structure receiving information
as a result of calling the MMC_TouchProbeDisable function.
Remarks
This function block is intended for single shot operation, that is, the first event after the rising edge at
Execute is valid for recording only. Possible following events are ignored.
One function block instance should represent exactly one probing command.
In case of multiple instances on the same probe and axis, the elements of TRIGGER_REF should be extended
with TouchProbeID - Identification of a unique probing command - this can be linked to MC_AbortTrigger.
Scope
Supported for Gold Servo Drives only.
User notification when touch probe is over via Callback and event.
MMC_TOUCHPROBEDISABLE_IN Structure

### PDF page 472
<a id="pdf-page-472"></a>
typedef struct mmc_touchprobedisable_in{
unsigned char ucExecute;
}MMC_TOUCHPROBEDISABLE_IN;
Parameters
ucExecute
Start the execution command. Boolean TRUE/FALSE values.
MMC_TOUCHPROBEDISABLE_OUT Structure
typedef struct mmc_touchprobedisable_out{
unsigned short usStatus;
short sErrorID;
}MMC_TOUCHPROBEDISABLE_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and 4.6NC Profiler Error
IDs. Displays an error code as negative or positive integers.
Figure 131 describes the function block for MMC_TouchProbeDisable as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 131: MMC_TouchProbeDisable function block

### PDF page 473
<a id="pdf-page-473"></a>
###### 6.2.31 MMC_WriteBoolParameter
6.2.31 MMC_WriteBoolParameter
Modifies the value of a vendor specific parameter of type BOOL.
MMC_LIB_API int MMC_WriteBoolParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEBOOLPARAMETER_IN* pInParam,
OUT MMC_WRITEBOOLPARAMETER_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
The axis reference handle type returned by the GetAxisRef command.
pInParam
Points to the MMC_WRITEBOOLPARAMETER input data structure using the
MMC_WriteBoolParameter function.
pOutParam
Points to the MMC_WRITEBOOLPARAMETER_OUT output structure receiving
information, as a result of calling the MMC_WriteBoolParameter function.
Remarks
The parameters are defined in the table in section 6.2.21 above for the function block
MMC_ReadParameter.
Scope
All
MMC_WRITEBOOLPARAMETER_IN Structure
typedef struct{
long lValue;
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;
unsigned char ucEnable;

### PDF page 474
<a id="pdf-page-474"></a>
}MMC_WRITEBOOLPARAMETER_IN;
Parameters
lValue
Input value. Any integer.
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, which are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_WRITEBOOLPARAMETER_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEBOOLPARAMETER_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 132 describes the function block for MMC_WriteBoolParameter as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 132: MMC_WriteBoolParameter function block
6.2.31.1 Function Block Code Example
int rc;
MMC_WRITEBOOLPARAMETER_IN stWriteBoolParam_in;
MMC_WRITEBOOLPARAMETER_OUT stWriteBoolParam_out;

### PDF page 475
<a id="pdf-page-475"></a>
//
// Inserting the structure parameters:
stWriteBoolParam_in.lValue = 1; // Axis status value
of the axis parameter
stWriteBoolParam_in.eParameterNumber = MMC_AXIS_STATE_PARAM;// Enumerator
value of the parameter
stWriteBoolParam_in.iParameterArrIndex = 2; // An array index
parameter
stWriteBoolParam_in.ucEnable = 1; // Enabled
//
rc = MMC_WriteBoolParameter (hConn, iAxisRef, &stWriteBoolParam_in,
&stWriteBoolParam_out);
if (rc != 0)
{
HandleError();
}

### PDF page 476
<a id="pdf-page-476"></a>
###### 6.2.32 MMC_GlobalWriteBoolParameter
6.2.32 MMC_GlobalWriteBoolParameter
Modifies the value of a vendor global parameter of type BOOL.
MMC_LIB_API int MMC_GlobalWriteBoolParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_WRITEBOOLPARAMETER_IN* pInParam,
OUT MMC_WRITEBOOLPARAMETER_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoGlobal
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
pInParam
Points to the MMC_WRITEBOOLPARAMETER input data structure using the
MMC_GlobalWriteBoolParameter function.
pOutParam
Points to the MMC_WRITEBOOLPARAMETER_OUT output structure receiving
information, as a result of calling the MMC_GlobalWriteBoolPa rameter function.
Remarks
The parameters are defined in the table in section Parameters Tables above for the function block
MMC_ReadParameter.
Scope
All
MMC_WRITEBOOLPARAMETER_IN Structure
typedef struct{
long lValue;
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;
unsigned char ucEnable;
}MMC_WRITEBOOLPARAMETER_IN;
Parameters

### PDF page 477
<a id="pdf-page-477"></a>
lValue
Input parameter. Any integer value.
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, which are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_WRITEBOOLPARAMETER_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEBOOLPARAMETER_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and 4.6NC Profiler Error
IDs. Displays an error code as negative or positive integers.
Figure 133 describes the function block for MMC_GlobalWriteBoolParameter as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 133: MMC_GlobalWriteBoolParameter function block
6.2.32.1 Function Block Code Example
int rc;
MMC_WRITEBOOLPARAMETER_IN stWriteBoolParam_in;
MMC_WRITEBOOLPARAMETER_OUT stWriteBoolParam_out;
//
// Inserting the structure parameters:
stWriteBoolParam_in.lValue = 1; // Axis status value
of the axis parameter

### PDF page 478
<a id="pdf-page-478"></a>
stWriteBoolParam_in.eParameterNumber = MMC_MOVEMENT_TYPE_PARAM;//
Enumerator value of the parameter
stWriteBoolParam_in.iParameterArrIndex = 2; // An array index
parameter
stWriteBoolParam_in.ucEnable = 1; // Enabled
//
rc = MMC_WriteBoolParameter (hConn, iAxisRef, &stWriteBoolParam_in,
&stWriteBoolParam_out);
if (rc != 0)
{
HandleError();
}

### PDF page 479
<a id="pdf-page-479"></a>
###### 6.2.33 MMC_WriteDigitalOutputs
6.2.33 MMC_WriteDigitalOutputs

MMC_LIB_API int MMC_WriteDigitalOutputs(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEDIGITALOUTPUT_IN* pInParam,
OUT MMC_WRITEDIGITALOUTPUT_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEDIGITALOUTPUT input data structure using the
MMC_WriteDigitalOutputs function.
pOutParam
Points to the MMC_WRITEDIGITALOUTPUT_OUT output structure receiving
information, as a result of calling the MMC_WriteDigitalOutputs function.
Remarks
This function must be supported for digital output.
Scope
All
MMC_WRITEDIGITALOUTPUT_IN Structure
typedef struct MMC_WRITEDIGITALOUTPUT_IN{
int iOutputNumber;
unsigned char ucEnable;
unsigned char ucValue;
}MMC_WRITEDIGITALOUTPUT_IN;
Parameters

### PDF page 480
<a id="pdf-page-480"></a>
iOutputNumber
Selects the output. Can be part of MC_OutputRef, if only one single input is referenced.
positive integer value.
Note: This parameter is not in use at this time.
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
ucValue
Whether the selected value input signal is True. Boolean values of TRUE/FALSE.
MMC_WRITEDIGITALOUTPUT_OUT Structure
typedef struct MMC_WRITEDIGITALOUTPUT_OUT{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEDIGITALOUTPUT_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections 4.3 Maestro Error IDs, and NC Profiler Error
IDs. Displays an error code as negative or positive integers.
Figure 134 describes the function block for MMC_WriteigitalOutput
[PDF field-code object omitted]
Figure 134: MMC_WriteDigitalOutput function block
6.2.33.1 Function Block Code Example
int rc;
MMC_WRITEDIGITALOUTPUT_IN stWriteDigOutput_in;
MMC_WRITEDIGITALOUTPUT_OUT stWriteDigOutput_out;
//
// Inserting the structure parameters:
stWriteDigOutput_in.iOutputNumber = 16; //Selects the output depending
on the drive
stWriteDigOutput_in.ucEnable = 1; //Enabled
//

### PDF page 481
<a id="pdf-page-481"></a>
rc = MMC_WriteDigitalOutputs (hConn, iAxisRef, &stWriteDigOutput_in,
&stWriteDigOutput_out);
if (rc != 0)
{
HandleError();
}

### PDF page 482
<a id="pdf-page-482"></a>
###### 6.2.34 MMC_WriteDigitalOutputs32Bit
6.2.34 MMC_WriteDigitalOutputs32Bit
Writes a value to the 32-bit digital output referenced by the single argument Output (with rising edge of
Execute).
MMC_LIB_API int MMC_WriteDigitalOutputs32Bit(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEDIGITALOUTPUT32Bit_IN* pInParam,
OUT MMC_WRITEDIGITALOUTPUT32Bit_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEDIGITALOUTPUT32Bit input data structure using the
MMC_WriteDigitalOutputs32Bit function.
pOutParam
Points to the MMC_WRITEDIGITALOUTPUT32Bit_OUT output structure receiving
information, as a result of calling the MMC_WriteDigitalOutputs32Bit function.
Remarks
This function must be supported for digital output.
Scope
All
MMC_WRITEDIGITALOUTPUT32Bit_IN Structure
typedef struct MMC_WRITEDIGITALOUTPUT32BIT_IN{
int iOutputNumber;
unsigned long ulValue;
unsigned char ucEnable;

### PDF page 483
<a id="pdf-page-483"></a>
}MMC_WRITEDIGITALOUTPUT32Bit_IN;
Parameters
iOutputNumber
Selects the output. Can be part of MC_OutputRef, if only one sing le input is referenced.
positive integer value.
Note: This parameter is not in use at this time.
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries rec eived
are 0/1 1 for TRUE / 0 for FALSE.
ulValue
Total value of all the inputs together. 32 bit bitwise positive numeric value.
MMC_WRITEDIGITALOUTPUT32Bit_OUT Structure
typedef struct MMC_WRITEDIGITALOUTPUT32BIT_OUT{
unsigned short usStatus;
unsigned short sErrorID;
}MMC_WRITEDIGITALOUTPUT32Bit_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 135 describes the function block for MMC_WriteDigitalOutputs32Bit s applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 135: MMC_WriteDigitalOutputs32Bit function block
6.2.34.1 Function Block Code Example
int rc;
MMC_WRITEDIGITALOUTPUT32Bit_IN stWriteDigOutput32Bit_in;
MMC_WRITEDIGITALOUTPUT32Bit_OUT stWriteDigOutput32Bit_out;
//
// Inserting the structure parameters:

### PDF page 484
<a id="pdf-page-484"></a>
stWriteDigOutput32Bit_in.iOutputNumber = 16; //Selects the output
depending on the drive
stWriteDigOutput32Bit_in.ucEnable = 1; //Enabled
//
rc = MMC_WriteDigitalOutputs32Bit (hConn, iAxisRef,
&stWriteDigOutput32Bit_in, &stWriteDigOutput32Bit_out);
if (rc != 0)
{
HandleError();
}

### PDF page 485
<a id="pdf-page-485"></a>
###### 6.2.35 MMC_WriteParameter
6.2.35 MMC_WriteParameter
Modifies the value of a vendor specific parameter.
MMC_LIB_API int MMC_WriteParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPARAMETER_IN* pInParam,
OUT MMC_WRITEPARAMETER_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is retur ned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
The axis reference handle type returned by the GetAxisRef command.
pInParam
Points to the MMC_WRITEPARAMETER input data structure using the
MMC_WriteParameter function.
pOutParam
Points to the MMC_WRITEPARAMETER_OUT output structure receiving information as
a result of calling the MMC_WriteParameter function.
Remarks
Refer to the full explanation in section 5.5.1Blended Behavior Mechanism.
Scope
All
MMC_WRITEPARAMETER_IN Structure
typedef struct{
double dbValue;
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;
unsigned char ucEnable;

### PDF page 486
<a id="pdf-page-486"></a>
}MMC_WRITEPARAMETER_IN;
Parameters
dbValue
Parameter value. Any double value.
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, which are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_WRITEPARAMETER_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEPARAMETER_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections 4.3 Maestro Error IDs, and NC Profiler Error
IDs. Displays an error code as negative or positive integers.
Figure 136 describes the function block for MMC_WriteParameter as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 136: MMC_WriteParameter function block
6.2.35.1 Function Block Code Example
int rc;
MMC_WRITEPARAMETER_IN stWriteParam_in;
MMC_WRITEPARAMETER_OUT stWriteParam_out;

### PDF page 487
<a id="pdf-page-487"></a>
//
// Inserting the structure parameters:
stWriteParam_in.dbValue = 1000; //Parameter value
stWriteParam_in.ucEnable = 1; //Enabled
stWriteParam_in.iParameterArrIndex = 1; //Parameter array
index
stWriteParam_in.eParameterNumber = MMC_SET_POSITION_PARAM;//Axis parameter
value
//
rc = MMC_WriteParameter (hConn, iAxisRef, &stWriteParam_in,
&stWriteParam_out);
if (rc != 0)
{
HandleError();
}

### PDF page 488
<a id="pdf-page-488"></a>
###### 6.2.36 MMC_GlobalWriteParameter
6.2.36 MMC_GlobalWriteParameter
Modifies the value of a vendor global parameter.
MMC_LIB_API int MMC_GlobalWriteParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_WRITEPARAMETER_IN* pInParam,
OUT MMC_WRITEPARAMETER_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_general_API.h
GMAS\includes\MMC_PLCopen_single_API.h
GMAS Programming(IEC 61331 Program)\ElmoGlobal
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
pInParam
Points to the MMC_WRITEPARAMETER input data structure using the
MMC_GlobalWriteParameter function.
pOutParam
Points to the MMC_WRITEPARAMETER_OUT output structure receiving information as
a result of calling the MMC_GlobalWriteParameter function.
Remarks
For a detailed explanation of this function, refer to the section Blended Behavior Mechanism.
Scope
All
MMC_WRITEPARAMETER_IN Structure
typedef struct{
double dbValue;
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;
unsigned char ucEnable;
}MMC_WRITEPARAMETER_IN;
Parameters

### PDF page 489
<a id="pdf-page-489"></a>
dbValue
Parameter value. Any double (2 bytes)value.
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, which are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_WRITEPARAMETER_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEPARAMETER_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the fu nction
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.

### PDF page 490
<a id="pdf-page-490"></a>
Figure 137 describes the function block for MMC_GlobalWriteParameter as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 137: MMC_GlobalWriteParameter function block
6.2.36.1 Function Block Code Example
int rc;
MMC_WRITEPARAMETER_IN stWriteParam_in;
MMC_WRITEPARAMETER_OUT stWriteParam_out;
//
// Inserting the structure parameters:
stWriteParam_in.dbValue = 1000; //Parameter value
stWriteParam_in.ucEnable = 1; //Enabled
stWriteParam_in.iParameterArrIndex = 1; //Parameter array
index
stWriteParam_in.eParameterNumber = MMC_POSITION_PERIOD_PARAM;//Axis
parameter value
//
rc = MMC_GlobalWriteParameter(hConn, iAxisRef, &stWriteParam_in,
&stWriteParam_out);
if (rc != 0)
{
HandleError();
}

### PDF page 491
<a id="pdf-page-491"></a>
###### 6.2.37 MMC_ChngOpMode
6.2.37 MMC_ChngOpMode
Changes the motion mode between NC and Distributed. This is previous determined in the DS -402 mode.
MMC_LIB_API int MMC_ChngOpMode(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_CHANGEMOTIONMODE_IN* pInParam,
OUT MMC_CHANGEMOTIONMODE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_drive_comm_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNE CT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_CHANGEMOTIONMODE input data structure using the
MMC_ChngOpMode function.
pOutParam
Points to the MMC_CHANGEMOTIONMODE_OUT output structure receiving
information as a result of calling the MMC_ChngOpMode function.
Remarks
None
Scope
The motion mode must be changed in distributed state e.g. from position mode to velocity mode.
MMC_CHANGEMOTIONMODE_IN Structure
typedef struct{
unsigned char ucMotionMode;
}MMC_CHANGEMOTIONMODE_IN;

### PDF page 492
<a id="pdf-page-492"></a>
Parameters
ucMotionMode
Enumerator for the motion mode. The DS-402 motion modes and IDs available are:
Profile position mode = 1
Profiled velocity mode (partial) = 3
Homing Mode = 6
Interpolated position mode = 7
Any of the above positive values accepted
MMC_CHANGEMOTIONMODE_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
} MMC_CHANGEMOTIONMODE_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 138 describes the function block for MMC_ChngOpMode as applied within the IEC 61131 programming.
[PDF field-code object omitted]
Figure 138: MMC_ChngOpMode function block
6.2.37.1 Function Block Code Example
int rc;
MMC_CHANGEMOTIONMODE_IN stChangeOpMode_in;
MMC_CHANGEMOTIONMODE_OUT stChangeOpMode_out;
//
// Inserting the structure parameters:
stChangeOpMode_in.ucMotionMode = 1; //Enumerator for the motion mode
//
rc = MMC_ChngOpMode (hConn, iAxisRef, &stChangeOpMode_in,
&stChangeOpMode_out) ;
if (rc != 0)
{
HandleError() ;
}

### PDF page 493
<a id="pdf-page-493"></a>
[No extractable text on this page.]

### PDF page 494
<a id="pdf-page-494"></a>
###### 6.2.38 MMC_ChangeOpModeEx
6.2.38 MMC_ChangeOpModeEx
Changes the motion mode between NC and Distributed. This is previous determined in the PLC DS -402
mode.
MMC_LIB_API int MMC_ChangeOpModeEx(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_CHANGEOPMODE_EX_IN* pInParam,
OUT MMC_CHANGEOPMODE_EX_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_drive_comm_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_CHANGEOPMODE_EX input data structure using the
MMC_ChangeOpModeEx function.
pOutParam
Points to the MMC_CHANGEOPMODE_EX_OUT output structure receiving information
as a result of calling the MMC_ChangeOpModeEx function.
Remarks
None
Scope
The motion mode must be changed in distributed state e.g. from position mode to velocity mode.
MMC_CHANGEOPMODE_EX_IN Structure
typedef struct{
double dbInitModeValue;
MC_EXECUTION_MODE eExecutionMode;

### PDF page 495
<a id="pdf-page-495"></a>
unsigned char ucMotionMode;
unsigned char ucSpare[19];
}MMC_CHANGEOPMODE_EX_IN;
Parameters
dbInitModeValue
Initial operation mode value. 8 bit length data of any positive or negative values
eExecutionMode
Execution mode enumerator defining whether the execution is immediate or queued,
with the following values:
eMMC_EXECUTION_MODE_IMMEDIATE = 0,
eMMC_EXECUTION_MODE_QUEUED
ucMotionMode
Enumerator for the motion mode. The DS-402 motion modes and IDs available are:
Profile position mode = 1
Profiled velocity mode (partial) = 3
Homing Mode = 6
Interpolated position mode = 7
Any of the above positive values accepted
ucSpare[19]
Spare description for future use. positive char value with maximum of 19 chars.
MMC_CHANGEOPMODE_EX_OUT Structure
typedef struct{
unsigned int uiHndl;
unsigned short usStatus;
unsigned short usErrorID;
}MMC_CHANGEOPMODE_EX_OUT;
Parameters
uiHndl
Returned function block handle. Integer with any positive value
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError

### PDF page 496
<a id="pdf-page-496"></a>
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 139 describes the function block for MMC_ChangeOpModeEx as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 139: MMC_ChangeOpModeEx function block

### PDF page 497
<a id="pdf-page-497"></a>
###### 6.2.39 MMC_SetProfileConditioning
6.2.39 MMC_SetProfileConditioning
This method sets Profile Conditioning mode of operation. It switches on(1)/off(0) the mode and sets other
input parameters.
MMC_LIB_API int MMC_SetProfileConditioning(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_PROFILECOND_IN* i_params,
OUT MMC_PROFILECOND_OUT* o_params
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_drive_comm_API.h
GMAS Programming(IEC 61331 Program)\ElmoSingleAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
i_params
Points to the MMC_PROFILECOND input data structure using the
MMC_SetProfileConditioning function.
o_params
Points to the MMC_PROFILECOND_OUT output structure receiving information as a
result of calling the MMC_SetProfileConditioning function.
Remarks

Scope
This function is only used for Single Axis and for a specific axis under the condition that the axis is disabled.
MMC_PROFILECOND_IN Structure
typedef struct _is_struct_in
{
Double freq[2];
Double damp[2];
unsigned char freqs_num;

### PDF page 498
<a id="pdf-page-498"></a>
unsigned char impls_num[2];
unsigned char enable;
unsigned char spare[32];
} MMC_PROFILECOND_IN;
Parameters
freq[2]
Frequency of vibration, as measured by probe sensor determining the vibrational
frquency during motion.
damp[2]
Damping factor for vibration, dependant on the accuraccy of the profile motion
required.
freqs_num
Number of frequencies(1 or 2). if 1 then freq[0] only.
impls_num[2]
2 or 3 impulse for each frequency.
enable
Whether the Profile Conditioning is to be applied or not. D isable=0, Enable=1
spare[32]
For future use.
MMC_PROFILECOND_OUT Structure
typedef struct _is_struct_out{
unsigned short usStatus;
short usErrorID;
} MMC_PROFILECOND_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.

### PDF page 499
<a id="pdf-page-499"></a>
Figure 140 describes the function block for MMC_SetProfileConditioning as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 140: MMC_SetProfileConditioning function block
