# 7.10.28 MMC_GroupReadActualVelocity

- Source PDF: `Maestro Administrative and Motion API_2022_12_v2.012_bookmarks_fixed.pdf`
- PDF physical pages: 811-842
- Chunk: `030_p0811-p0842_7.10.28-MMC_GroupReadActualVelocity.md`

## Active Outline At Chunk Start
- p. 504 - Chapter 7 Motion and Administrative - Multi-Axis
  - p. 726 - 7.10 Multiple Axes Administrative Control
    - p. 811 - 7.10.28 MMC_GroupReadActualVelocity

## Contained Bookmark Outline
    - p. 811 - 7.10.28 MMC_GroupReadActualVelocity
    - p. 814 - 7.10.29 MMC_GroupReadError
    - p. 817 - 7.10.30 MMC_GroupReadStatus
    - p. 820 - 7.10.31 MMC_GroupReset
    - p. 823 - 7.10.32 MMC_GroupSetOverride
    - p. 827 - 7.10.33 MMC_GroupSetPosition
    - p. 832 - 7.10.34 MMC_RemoveAxisFromGroup
    - p. 835 - 7.10.35 MMC_GroupReadParameter
    - p. 837 - 7.10.36 MMC_GroupReadBoolParameter
    - p. 839 - 7.10.37 MMC_GroupWriteParameter
    - p. 841 - 7.10.38 MMC_GroupWriteBoolParameter

## Extracted Text

### PDF page 811
<a id="pdf-page-811"></a>
###### 7.10.28 MMC_GroupReadActualVelocity
7.10.28 MMC_GroupReadActualVelocity
Returns the actual velocity in the selected coordinate system of an axes group. This is an administrative
function block, since no movement is generated.
MMC_LIB_API int MMC_GroupReadActualVelocity(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_GROUPREADACTUALVELOCITY_IN* pInParam,
OUT MMC_GROUPREADACTUALVELOCITY_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_GROUPREADACTUALVELOCITY input data structure using the
MMC_GroupReadActualVelocity function.
pOutParam
Points to the MMC_GROUPREADACTUALVELOCITY_OUT output structure receiving
information, as a result of calling the MMC_GroupReadActualVelocity function.
Remarks
None
Scope
Relevant for ACS coordinate system only and not MCS. Conditional on the values of the CANbus PDO3 and
PDO4.
MMC_GROUPREADACTUALVELOCITY_IN Structure
typedef struct{
MC_COORD_SYSTEM_ENUM eCoordSystem;
unsigned char ucEnable;

### PDF page 812
<a id="pdf-page-812"></a>
}MMC_GROUPREADACTUALVELOCITY_IN;
Parameters
eCoordSystem
Define the types of supported coordinate systems using the
MC_COORD_SYSTEM_ENUM enumerator. The options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_GROUPREADACTUALVELOCITY_OUT Structure
typedef struct{
double dVelocity[NC_MAX_NUM_AXES_IN_NODE];
double dPathVelocity;
unsigned short usStatus;
short sErrorID;
}MMC_GROUPREADACTUALVELOCITY_OUT;
Parameters
dVelocity [NC_MAX_NUM_AXES_IN_NODE]
Current velocity of the group:
- For ACS the velocities of the different axes
- For MCS it provides the velocity of the TCP
The array parameter NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as
the maximum number of axis in a group.
dVelocity Any negative or positive array double value in the axis's unit [u/s]
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
dPathVelocity
Current path velocity (speed, combined result) of the TCP. dPathVelocity can have
values of any negative or positive double values in axis's unit [u].
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError

### PDF page 813
<a id="pdf-page-813"></a>
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 251 describes the function block for MMC_GroupReadActualVelocity
[PDF field-code object omitted]
Figure 251: MMC_GroupReadActualVelocity function block
7.10.28.1 Function Block Code Example
int rc;
MMC_GROUPREADACTUALVELOCITY_IN stGroupReadActualVel_in;
MMC_GROUPREADACTUALVELOCITY_OUT stGroupReadActualVel_out;
//
// Inserting the structure parameters:
stGroupReadActualVel_in.eCoordSystem = MC_MCS_COORD; //Define types of
supported coordinate systems
stGroupReadActualVel_in.ucEnable = 1; //Get the actual
position in the selected coordinate system of the axes group
//
rc = MMC_GroupReadActualVelocity (hConn, iAxisRef,
&stGroupReadActualVel_in, &stGroupReadActualVel_out);
printf("Group Actual Velocity[%ld] ErrId[%d]\n", (long
int)stGroupReadActualVel_out.dVelocity,
(short)stGroupReadActualVel_out.sErrorID);
if (rc != 0)
{
HandleError();
}

### PDF page 814
<a id="pdf-page-814"></a>
###### 7.10.29 MMC_GroupReadError
7.10.29 MMC_GroupReadError
Describes general axes group errors not relating to the function blocks. This is an admi nistrative function
block, since no movement is generated. This function is not in use at this moment.
MMC_LIB_API int MMC_GroupReadError(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_GROUPREADERROR_IN* pInParam,
OUT MMC_GROUPREADERROR_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_GROUPREADERROR input data structure using the
MMC_GroupReadError function.
pOutParam
Points to the MMC_GROUPREADERROR_OUT output structure receiving information,
as a result of calling the MMC_GroupReadError function.
Remarks
Examples are (software) limit switch exceeded or single axis error in GroupStandby state. This function is not
in use at this moment and will always produce a usGroupErrorID of 256.
Scope

MMC_GROUPREADERROR_IN Structure
typedef struct{
unsigned char ucEnable;
}MMC_GROUPREADERROR_IN;

### PDF page 815
<a id="pdf-page-815"></a>
Parameters
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_GROUPREADERROR_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
unsigned short usGroupErrorID;
}MMC_GROUPREADERROR_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
usGroupErrorID
Returned command group error ID. Signals where an group error has occurred within
the function block. These values are vendor specific. Refer to the errors listed in
sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code integer.
Figure 252 describes the function block for MMC_GroupReadError
[PDF field-code object omitted]
Figure 252: MMC_GroupReadError function block
7.10.29.1 Function Block Code Example
int rc;
MMC_GROUPREADERROR_IN stGroupReadError_in;
MMC_GROUPREADERROR_OUT stGroupReadError_out;
//
// Inserting the structure parameters:
stGroupReadError_in.ucEnable = 1; //Enabled
//
rc = MMC_GroupReadError (hConn, iAxisRef, &stGroupReadError_in,
&stGroupReadError_out);

### PDF page 816
<a id="pdf-page-816"></a>
printf("Group Error[%ld] ErrId[%d]\n", (long
int)stGroupReadError_out.usGroupErrorID,
(short)stGroupReadError_out.sErrorID);
if (rc != 0)
{
HandleError();
}

### PDF page 817
<a id="pdf-page-817"></a>
###### 7.10.30 MMC_GroupReadStatus
7.10.30 MMC_GroupReadStatus
For multiple Axes. Returns the status of an axes group according to the active Group function block. This is
an administrative function block, since no movement is generated.
MMC_LIB_API int MMC_GroupReadStatusCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_GROUPREADSTATUS_IN* pInParam,
OUT MMC_GROUPREADSTATUS_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
GMAS Programming(IEC 61331 Program)\ElmoGroupAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_GROUPREADSTATUS input data structure using the
MMC_GroupReadStatus function.
pOutParam
Points to the MMC_GROUPREADSTATUS_OUT output structure receiving information,
as a result of calling the MMC_GroupReadStatus function.
Remarks
The outputs reflect the commanded state of the group.
Scope
All
MMC_GROUPREADSTATUS_IN Structure
typedef struct{
unsigned int uiHndlr;
unsigned char ucEnable;

### PDF page 818
<a id="pdf-page-818"></a>
}MMC_GROUPREADSTATUS_IN;
Parameters
uiHndlr
Requested group handle. Values of any integer accepted.
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_GROUPREADSTATUS_OUT Structure
typedef struct{
unsigned long ulState;
unsigned short usStatus;
short sErrorID;
unsigned short usGroupErrorID;
}MMC_GROUPREADSTATUS_OUT;
Parameters
ulState
Group current state. Returned command status. Refer to the section Axis Parameters
(Explanations)
The axis parameters define the MMC_PARAMETER_LIST_ENUM eParameterNumber
values of the axis status. Refer to the section Parameters Tables for the integer
parameter definitions for the appropriate integer parameter to be used as enumerator
and the range of axis status bit masks with bitwise values.
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
usGroupErrorID
Returned command group error ID. Signals where an group error has occurred within
the function block. These values are vendor specific. Refer to the errors listed in
sections 4.3 Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code integer.
Figure 253 describes the function block for MMC_GroupReadStatus as applied within the IEC 61131
programming.

### PDF page 819
<a id="pdf-page-819"></a>
[PDF field-code object omitted]
Figure 253: MMC_GroupReadStatus function block
7.10.30.1 Function Block Code Example
int rc;
MMC_GROUPREADSTATUS_IN stGroupReadStatus_in;
MMC_GROUPREADSTATUS_OUT stGroupReadStatus_out;
//
// Inserting the structure parameters:
stGroupReadStatus_in.uiHndlr = 35; // Returned function block handle
stGroupReadStatus_in.ucEnable = 1; //Enabled
//
rc = MMC_GroupReadStatusCmd (hConn, iAxisRef, &stGroupReadStatus_in,
&stGroupReadStatus_out);
printf("Group Status[%ld] ErrId[%d]\n", (long
int)stGroupReadStatus_out.usGroupErrorID,
(short)stGroupReadStatus_out.sErrorID);
printf("Group Status[%ld]\n", (long int)stGroupReadStatus_out.ulState);
if (rc != 0)
{
HandleError();
}

### PDF page 820
<a id="pdf-page-820"></a>
###### 7.10.31 MMC_GroupReset
7.10.31 MMC_GroupReset
For multiple Axes. Makes the transition from the state GroupErrorStop to GroupDisabled by resetting all
internal group-related errors - it does not affect the output of the function block instances.
MMC_LIB_API int MMC_GroupResetCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_GROUPRESET_IN* pInParam,
OUT MMC_GROUPRESET_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
GMAS Programming(IEC 61331 Program)\ElmoGroupAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_GROUPRESET input data structure using the MMC_GroupReset
function.
pOutParam
Points to the MMC_GROUPRESET_OUT output structure receiving information, as a
result of calling the MMC_GroupReset function.
Remarks
This function block also resets all axes in this group like MMC_Reset.
Scope
All
MMC_GROUPRESET_IN Structure
typedef struct{
unsigned char ucExecute;
}MMC_GROUPRESET_IN;

### PDF page 821
<a id="pdf-page-821"></a>
Parameters
ucExecute
Start the execution command. Boolean TRUE/FALSE values.
MMC_GROUPRESET_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_GROUPRESET_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 254 describes the function block for MMC_GroupReset as applied within the IEC 61131 programming.
[PDF field-code object omitted]
Figure 254: MMC_GroupReset function block

### PDF page 822
<a id="pdf-page-822"></a>
7.10.31.1 Function Block Code Example
int rc;
MMC_GROUPRESET_IN stGroupReset_in;
MMC_GROUPRESET_OUT stGroupReset_out;
//
// Inserting the structure parameters:
stGroupReset_in.ucExecute = 1; // Start the execution command

//
rc = MMC_GroupResetCmd (hConn, iAxisRef, &stGroupReset_in,
&stGroupReset_out);
if (rc != 0)
{
HandleError();
}

### PDF page 823
<a id="pdf-page-823"></a>
###### 7.10.32 MMC_GroupSetOverride
7.10.32 MMC_GroupSetOverride
For multi-axes. Sets the values of override for the coordinated motion of several axes, and all functions
operating on that axes group.
MMC_LIB_API int MMC_GroupSetOverrideCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_SETOVERRIDE_IN* pInParam,
OUT MMC_SETOVERRIDE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not supported
Source Function
GMAS\includes\MMC_PLCopen_single_API.h
Structures
GMAS\includes\MMC_PLCopen_group_API.h
IEC
GMAS Programming(IEC 61331 Program)\ElmoGroupAxis
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_SETOVERRIDE input data structure using the MMC_SetOverride
function.
pOutParam
Points to the MMC_SETOVERRIDE_OUT output structure receiving information as a
result of calling the MMC_SetOverride function.
Remarks
The override parameters act as a factor that is multiplied to the commanded vector velocity, acceleration,
deceleration, and jerk of all axes group function blocks. MMC_GroupSetOverride is not applicable to master -
slave group synchronized motion commands.
MMC_GroupSetOverride does not influence the state diagram of the axis group. The f ollowing should be
noted:

### PDF page 824
<a id="pdf-page-824"></a>
- The Input fAccFactor acts on positive and negative acceleration (deceleration).
- This function block sets the override factor, which is valid until a new override is set.
- The default values of the override factor are 1.0. The value of the overrides can be between 0.0 and 1.0.
The behavior of values > 1.0 is vendor specific. Values < 0.0 are not allowed. The value 0.0 is not allowed
for both fAccFactor and fJerkFactor, and generates an error.
- The value 0.0 set to the fVelFactor stops the axis without bringing it to the state GroupStandby.
- Override does not act on slave axes groups in the state Group Synchronized motion.
- fVelFactor can be changed at any time and acts directly on the ongoing motion.
- Reducing the fAccFactor and/or fJerkFactor can lead to a position overshoot - a possible cause of
damage.
Scope
All

MMC_SETOVERRIDE_IN Structure
typedef struct{
float fVelFactor;
float fAccFactor;
float fJerkFactor;
unsigned short usUpdateVelFactorIdx;
unsigned char ucEnable;
}MMC_SETOVERRIDE_IN;
Parameters
fVelFactor
New override factor for the velocity. Any positive float value between [0 - 1].
fAccFactor
New override factor for the acceleration/deceleration. ACC/Jerk Factors are NOT
supported at this time. For future compatibility, enter "1" in the function call.
fJerkFactor
New override factor for the jerk. ACC/Jerk Factors are NOT supported at this time. For
future compatibility, enter "1" in the function call.
usUpdateVelFactorIdx
Index of changed velocity factor. Vendor defined. The default is 0. Has integer values of
0 - 2
This variable is not in use at this moment.
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received

### PDF page 825
<a id="pdf-page-825"></a>
are 0/1 1 for TRUE / 0 for FALSE.
MMC_SETOVERRIDE_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
} MMC_SETOVERRIDE_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted, Done, CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 255 describes the function block for MMC_GroupSetOverride as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 255: MMC_GroupSetOverride function block
7.10.32.1 Function Block Code Example
int rc;
MMC_SETOVERRIDE_IN stGSetOverride_in;
MMC_SETOVERRIDE_OUT stGSetOverride_out;
//
// Inserting the structure parameters:
stGSetOverride_in.fAccFactor = 1.0; //New override factor for the
acceleration/deceleration
stGSetOverride_in.fJerkFactor 1.0; //New override factor for the jerk
stGSetOverride_in.usUpdateVelFactorIdx = 1; //Index of changed
velocity factor
stGSetOverride_in.fVelFactor = 0.4; //New override factor for
the velocity
stGSetOverride_in.ucEnable = 1; //Enabled
//
rc = MMC_GroupSetOverrideCmd (hConn, iAxisRef, &stGSetOverride_in,
&stGSetOverride_out);
if (rc != 0)
{
HandleError();

### PDF page 826
<a id="pdf-page-826"></a>
}
7.10.32.2 Implementation Example
[PDF field-code object omitted]
Figure 256: Timing diagram for MMC_GroupSetOverride - Example
7. 1. Axes Group Vector Velocity changes to 50% with 100% of deceleration
8. 2. Axes Group Vector Velocity back to 100% with 50% acceleration
9. 3. Axes Group Vector Velocity moves to 0% with 100% deceleration
10. 4. No Change, because fAccFactor 0.0 is not allowed; Error is set

### PDF page 827
<a id="pdf-page-827"></a>
###### 7.10.33 MMC_GroupSetPosition
7.10.33 MMC_GroupSetPosition
For multiple Axes. Sets the Position of all axes in a group without moving the axes.
MMC_LIB_API int MMC_GroupSetPositionCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_GROUPSETPOSITION_IN* pInParam,
OUT MMC_GROUPSETPOSITION_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_GROUPSETPOSITION input data structure using the
MMC_GroupSetPosition function.
pOutParam
Points to the MMC_GROUPSETPOSITION_OUT output structure receiving information,
as a result of calling the MMC_GroupSetPosition function.
Remarks
The new coordinates are described in an array. Within the coordinate system input, the respective
coordinate system is selected as a method of referencing or a transformation. MMC_GroupSetPosition shifts
the position of the addressed coordinate system and affects the higher level coordinate systems (so if ACS is
selected, MCS and PCS are affected).
Scope
All
MMC_GROUPSETPOSITION_IN Structure
typedef struct{
unsigned char ucExecute;

### PDF page 828
<a id="pdf-page-828"></a>
double dbPosition[NC_MAX_NUM_AXES_IN_NODE];
MC_COORD_SYSTEM_ENUM eCoordSystem;
MC_BUFFERED_MODE_ENUM eBufferMode;
unsigned char ucMode;
}MMC_GROUPSETPOSITION_IN;
Parameters
ucExecute
Start the execution command at the rising edge. Boolean TRUE/FALSE values.
dbPosition
Target position for the motion of the axis when conditions are met. Any negative or
positive double values in technical unit [u].
An array of coordinates, incl. positions and orientations (Distance if Mode = RELATIVE).
The array parameter NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as
the maximum number of axis in a group.
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
MC_COORD_SYSTEM_ENUM eCoordSystem
Define the types of supported coordinate systems. The MC_COORD_SYSTEM_ENUM
enumerator options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
MC_BUFFERED_MODE_ENUM eBufferMode
MC_BUFFERED_MODE_ENUM defines the behavior of the axis. The MC_BufferMode
defines the velocity at the transition point. It defines the chronological sequence of the
function block relative to the previous block. Enumerator modes are as follows:
MC_ABORTING_MODE = 1
MC_BUFFERED_MODE = 2
MC_BLENDING_LOW_MODE = 3
MC_BLENDING_PREVIOUS_MODE = 4
MC_BLENDING_NEXT_MODE = 5
MC_BLENDING_HIGH_MODE = 6
Aborting Default mode without buffering. The next function block aborts an
ongoing motion and the command affects the axis immediately.
The buffer is cleared. When Aborting is applied:
- All existing function blocks in the node queue are aborted
- The Transition velocity changes to zero, which in turn stops all
current motion, and executes the new motion from zero

### PDF page 829
<a id="pdf-page-829"></a>
velocity
- This motion will be executed regardless of the Boolean
ucExecute status which may be False(0) or True(1)
Buffered When Buffering is applied, the current function block is inserted to
the function block queue in the Maestro, and on completion of the
axis\vector execution of all function blocks in the queue (with the
exception of the current function block), then the current function
block will start its execution. In addition, the Transition velocity is
zero.
BlendingLow When Blending Low is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the smaller value between the previous and
current function blocks.
BlendingPreviou
s
When Blending Previous is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), t hen
the current function block will start its execution. However, the
Transition velocity is the velocity of the previous function block.
BlendingNext When Blending Next is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the current function block.
BlendingHigh When Blending High is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the higher between the previous and the
current function blocks.
ucMode
RELATIVE =True, ABSOLUTE = False (Default)
RELATIVE means that Position is added to the actual position value of the axis at the
time of execution. This results in a recalibration by a specified dist ance. ABSOLUTE
means that the actual position value of the axis is set to the value specified in the
Position parameter.
Values accepted are Boolean, TRUE/FALSE.

### PDF page 830
<a id="pdf-page-830"></a>
ucPosMode
Boolean values for the position mode can be absolute mode = 0, or relative mode = 1
MMC_GROUPSETPOSITION_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_GROUPSETPOSITION_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 257 describes the function block for MMC_GroupSetPosition
[PDF field-code object omitted]
Figure 257: MMC_GroupSetPosition function block
7.10.33.1 Function Block Code Example
int rc;
MMC_GROUPSETPOSITION_IN stGroupSetPos_in;
MMC_GROUPSETPOSITION_OUT stGroupSetPos_out;
//
// Inserting the structure parameters:
stGroupSetPos_in.eCordSystem = MC_MCS_COORD; //Define types of supported
coordinate systems
stGroupSetPos_in.eBufferMode = MC_BUFFERED_MODE;//MC_BUFFERED_MODE_ENUM
Defines the behavior of the axis
stGroupSetPos_in.dbPosition[2] = 80000.0;//An array of coordinates,
positions and orientations
stGroupSetPos_in.dbPosition[3] = 50000.0;
stGroupSetPos_in.dbPosition[4] = 60000.0;
stGroupSetPos_in.ucMode = 1;//Position is added to the actual position
value of the axis
stGroupSetPos_in.ucExecute = 1;
//
rc = MMC_GroupSetPositionCmd (hConn, iAxisRef, &stGroupSetPos_in,
&stGroupSetPos_out);
if (rc != 0)
{
HandleError();

### PDF page 831
<a id="pdf-page-831"></a>
}

### PDF page 832
<a id="pdf-page-832"></a>
###### 7.10.34 MMC_RemoveAxisFromGroup
7.10.34 MMC_RemoveAxisFromGroup
For multiple Axes. Removes one axis from the group AxesGroup. This is an administrative function block,
since no movement is generated.
MMC_LIB_API int MMC_RemoveAxisFromGroup(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_REMOVEAXISFROMGROUP_IN* pInParam,
OUT MMC_REMOVEAXISFROMGROUP_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_REMOVEAXISFROMGROUP input data structure using the
MMC_RemoveAxisFromGroup function.
pOutParam
Points to the MMC_REMOVEAXISFROMGROUP_OUT output structure receiving
information, as a result of calling the MMC_RemoveAxisFromGroup function.
Remarks
The command cannot be buffered. If there is no axis remaining in the group, the state automat ically changes
to GroupDisabled.
Scope
Only available if the group is disabled. If issued on a group that is in GroupEnabled, GroupStandby, or
GroupErrorStop state, it generates an error and the function block is not executed. If GroupEnabled, an
explicit error is output: NC_UNSUITABLE_NODE_STATE
From MASE Release: 2.1.7.1 B20, the following rules will apply:
- Only the last axis in a vector can be removed each time. For example: If there are 3 axes a01,a02,a03,
then only a03 can be removed, after a03 is removed only a02 can be removed... (In order to know who is

### PDF page 833
<a id="pdf-page-833"></a>
the last axis, API GetMembersInfo() can be used by the user.)
- An axis can be added only to the last member in a vector. For example: If ther are 3 axes a01,a02 ,a03
then the a04 axis can only be added in place 4.
MMC_REMOVEAXISFROMGROUP_IN Structure
typedef struct{
NC_IDENT_IN_GROUP_ENUM eIdentInGroup;
}MMC_REMOVEAXISFROMGROUP_IN;
Parameters
eIdentInGroup
The NC_IDENT_IN_GROUP_ENUM enumerator identifies the order and Nodes in the
group of the added axis. Performed via an enumerator to give the different axes a name
in the order, which can be coupled to the names in the kinematic model. The options
are:
NC_NODE_1_ID = 0
...............
NC_NODE_16_ID = 15
MMC_REMOVEAXISFROMGROUP_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_REMOVEAXISFROMGROUP_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 258 describes the function block for MMC_RemoveAxisFromGroup as applied within the IEC 61131
programming.

### PDF page 834
<a id="pdf-page-834"></a>
MMC_RemoveAxisFromGroup
IdentInGroupNC_IDENT_IN_GROUP_ENUM
Boolean ucExecute
usStatus
Done, Error
usErrorID
Bitwise
Error code
@AxesGroup

Figure 258: MMC_RemoveAxisFromGroup function block
7.10.34.1 Function Block Code Example
int rc;
MMC_REMOVEAXISFROMGROUP_IN stRemoveAxisFromGrp_in;
MMC_REMOVEAXISFROMGROUP_OUT stRemoveAxisFromGrp_out;
//
// Inserting the structure parameters:
stRemoveAxisFromGrp_in.eIdentInGroup = NC_NODE_4_ID;//Identifies the
supported axes (nodes) in a group
//
rc = MMC_RemoveAxisFromGroup (hConn, iAxisRef, &stRemoveAxisFromGrp_in,
&stRemoveAxisFromGrp_out);
if (rc != 0)
{
HandleError();
}

### PDF page 835
<a id="pdf-page-835"></a>
###### 7.10.35 MMC_GroupReadParameter
7.10.35 MMC_GroupReadParameter
Reads a specific group of axes parameters.
MMC_LIB_API int MMC_GroupReadParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_READPARAMETER_IN* pInParam,
OUT MMC_READPARAMETER_OUT* pOutParam
);
Motion Mode NC - Irrelevant Distributed - Irrelevant
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_READPARAMETER input data structure using the
MMC_GroupReadParameter function.
pOutParam
Points to the MMC_READPARAMETER_OUT output structure receiving information as a
result of calling the MMC_GroupReadParameter function.
Remarks
N/A
Scope
None
MMC_READPARAMETER_IN Structure

typedef struct{
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;
unsigned char ucEnable;
}MMC_READPARAMETER_IN;

### PDF page 836
<a id="pdf-page-836"></a>
Parameters
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, which are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_READPARAMETER_OUT Structure

typedef struct{
double dbValue;
unsigned short usStatus;
short sErrorID;
}MMC_READPARAMETER_OUT;
Parameters
dbValue
Output of the specific parameter. Any Double value.
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 259 describes the function block for MMC_GroupReadParameter
[PDF field-code object omitted]
Figure 259: MMC_GroupReadParameter function block

### PDF page 837
<a id="pdf-page-837"></a>
###### 7.10.36 MMC_GroupReadBoolParameter
7.10.36 MMC_GroupReadBoolParameter
Reads a specific group axes Boolean parameter.
MMC_LIB_API int MMC_GroupReadBoolParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_READBOOLPARAMETER_IN* pInParam,
OUT MMC_READBOOLPARAMETER_OUT* pOutParam
);
Motion Mode NC - Irrelevant Distributed - Irrelevant
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_READBOOLPARAMETER input data structure using the
MMC_GroupReadBoolParameter function.
pOutParam
Points to the MMC_READBOOLPARAMETER_OUT output structure receiving
information as a result of calling the MMC_GroupReadBoolParameter function.
Remarks
N/A
Scope
None
MMC_READBOOLPARAMETER_IN Structure
typedef struct{
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;
unsigned char ucEnable;
}MMC_READBOOLPARAMETER_IN;

### PDF page 838
<a id="pdf-page-838"></a>
Parameters
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, whic h are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_READBOOLPARAMETER_OUT Structure
typedef struct{
long lValue;
unsigned short usStatus;
short sErrorID;
}MMC_READBOOLPARAMETER_OUT;
Parameters
lValue
Boolean parameters integer value
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the funct ion
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.

Figure 260 describes the function block for MMC_GroupReadBoolParameter
[PDF field-code object omitted]
Figure 260: MMC_GroupReadBoolParameter function block

### PDF page 839
<a id="pdf-page-839"></a>
###### 7.10.37 MMC_GroupWriteParameter
7.10.37 MMC_GroupWriteParameter
Modifies the value of a specific group axes parameter.
MMC_LIB_API int MMC_WriteParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPARAMETER_IN* pInParam,
OUT MMC_WRITEPARAMETER_OUT* pOutParam
);
Motion Mode NC - Irrelevant Distributed - Irrelevant
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPARAMETER input data structure using the
MMC_GroupWriteParameter function.
pOutParam
Points to the MMC_WRITEPARAMETER_OUT output structure receiving information,
as a result of calling the MMC_GroupWriteParameter function.
Remarks
N/A
Scope
None
MMC_WRITEPARAMETER_IN Structure
typedef struct{
double dbValue;
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;

### PDF page 840
<a id="pdf-page-840"></a>
unsigned char ucEnable;
}MMC_WRITEPARAMETER_IN;
Parameters
dbValue
Parameter value. Any positive double (8 bytes)value up to 15 digits.
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, which are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_WRITEPARAMETER_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEPARAMETER_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 261 describes the function block for MMC_GroupWriteParameter
[PDF field-code object omitted]
Figure 261: MMC_GroupWriteParameter function block

### PDF page 841
<a id="pdf-page-841"></a>
###### 7.10.38 MMC_GroupWriteBoolParameter
7.10.38 MMC_GroupWriteBoolParameter
Modifies the value of a specific group axes Boolean parameter.
MMC_LIB_API int MMC_GroupWriteBoolParameter(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEBOOLPARAMETER_IN* pInParam,
OUT MMC_WRITEBOOLPARAMETER_OUT* pOutParam
);
Motion Mode NC - Irrelevant Distributed - Irrelevant
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is commo n
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEBOOLPARAMETER input data structure using the
MMC_GroupWriteBoolParameter function.
pOutParam
Points to the MMC_WRITEBOOLPARAMETER_OUT output structure receiving
information, as a result of calling the MMC_GroupWriteBoolParameter function.
Remarks
N/A
Scope
None
MMC_WRITEBOOLPARAMETER_IN Structure
typedef struct{
long lValue;
MMC_PARAMETER_LIST_ENUM eParameterNumber;
int iParameterArrIndex;
unsigned char ucEnable;
}MMC_WRITEBOOLPARAMETER_IN;

### PDF page 842
<a id="pdf-page-842"></a>
Parameters
lValue
Input value. Any positive integer.
eParameterNumber
Number of the parameter. One can also use symbolic parameter names, which are
declared as VAR CONST.
Refer to the section Parameters Tables for the appropriate integer parameter to be
used as enumerator.
iParameterArrIndex
An array index parameter. Any positive integer values
ucEnable
Unsigned char value of start the execution command at the rising edge. Entries received
are 0/1 1 for TRUE / 0 for FALSE.
MMC_WRITEBOOLPARAMETER_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEBOOLPARAMETER_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and 4.6NC Profiler Error
IDs. Displays an error code as negative or positive integers.
Figure 262 describes the function block for MMC_GroupWriteBoolParameter
[PDF field-code object omitted]
Figure 262: MMC_GroupWriteBoolParameter function block
