# 4.15.9 MMC_ResetSystem

- Source PDF: `Maestro Administrative and Motion API_2022_12_v2.012_bookmarks_fixed.pdf`
- PDF physical pages: 193-195
- Chunk: `010_p0193-p0195_4.15.9-MMC_ResetSystem.md`

## Active Outline At Chunk Start
- p. 65 - Chapter 4 Error Handling
  - p. 173 - 4.15 Functional and Communication Error Handling
    - p. 193 - 4.15.9 MMC_ResetSystem

## Contained Bookmark Outline
    - p. 193 - 4.15.9 MMC_ResetSystem

## Extracted Text

### PDF page 193
<a id="pdf-page-193"></a>
###### 4.15.9 MMC_ResetSystem
4.15.9 MMC_ResetSystem
This function resets the entire system errors, including error counters of PHY, cyclic and missed frames
errors. In addition it changes all nodes to INIT and then to OPERATIONAL state, resulting in motor off of all
drives. In addition this function might reset Maestro fatal errors and return it to fully operational status.
int MMC_ResetSystem(
MMC_CONNECT_HNDL hConn,
MMC_RESETSYSTEM_IN* pInParam,
MMC_RESETSYSTEM_OUT* pOutParam
);
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
pInParam
Points to the MMC_RESETSYSTEM input structure that receives the MMC_ResetSystem
Information
pOutParam
Points to the MMC_RESETSYSTEM_OUT output structure receiving information as a
result of calling the MMC_ResetSystem function.
Remarks
None
Scope
All
MMC_RESETSYSTEM_IN Structure
typedef struct{
unsigned char ucDummy;
}MMC_RESETSYSTEM_IN;
Parameters
ucDummy

### PDF page 194
<a id="pdf-page-194"></a>
No data required. Dummy data input.

MMC_RESETSYSTEM_OUT Structure
typedef struct{
unsigned short usStatus;
short sErrorID;
} MMC_RESETSYSTEM_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.

### PDF page 195
<a id="pdf-page-195"></a>
Figure 11 describes the function block for MMC_ResetSystem.

MMC_ResetSystem
fDeceleration
Dummy value ucDummy
usStatus
Done, Busy, Active,
CommandAborted, Error
usErrorID
Bitwise
Error code
@Axis

Figure 11: MMC_ResetSystem function block
4.15.9.1 Function Block Code and Implementation Example

MMC_RESETSYSTEM_IN ResetIn;
MMC_RESETSYSTEM_OUT ResetOut;

rc = MMC_ResetSystem(ConnHndl,&ResetIn,&ResetOut);
if(rc)
{
printf("MMC_ResetSystemErrors failed error %d\n",ResetOut.sErrorID);
}
