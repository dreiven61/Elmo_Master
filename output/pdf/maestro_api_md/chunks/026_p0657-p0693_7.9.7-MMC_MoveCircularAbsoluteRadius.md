# 7.9.7 MMC_MoveCircularAbsoluteRadius

- Source PDF: `Maestro Administrative and Motion API_2022_12_v2.012_bookmarks_fixed.pdf`
- PDF physical pages: 657-693
- Chunk: `026_p0657-p0693_7.9.7-MMC_MoveCircularAbsoluteRadius.md`

## Active Outline At Chunk Start
- p. 504 - Chapter 7 Motion and Administrative - Multi-Axis
  - p. 627 - 7.9 Multiple Axes Motion Control - Functions
    - p. 657 - 7.9.7 MMC_MoveCircularAbsoluteRadius

## Contained Bookmark Outline
    - p. 657 - 7.9.7 MMC_MoveCircularAbsoluteRadius
    - p. 664 - 7.9.8 MMC_MoveCircularAbsoluteAngle
    - p. 670 - 7.9.9 MMC_MoveAngle
    - p. 675 - 7.9.10 MMC_MoveLinearAbsolute
    - p. 682 - 7.9.11 MMC_MoveLinearRelative
    - p. 689 - 7.9.12 MMC_MoveLinearAdditive

## Extracted Text

### PDF page 657
<a id="pdf-page-657"></a>
###### 7.9.7 MMC_MoveCircularAbsoluteRadius
7.9.7 MMC_MoveCircularAbsoluteRadius
For multi-axis systems. Commands an interpolated circular movement radius on an axes group from the
actual position of the TCP.
MMC_LIB_API int MMC_MoveCircularAbsoluteRadiusCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_MOVECIRCULARABSOLUTERADIUS_IN* pInParam,
OUT MMC_MOVECIRCULARABSOLUTE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by t he Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MOVECIRCULARABSOLUTERADIUS group axes input structure that
receives the MMC_MoveCircularAbsoluteRadius command.
pOutParam
Points to the MMC_MOVECIRCULARABSOLUTE_OUT group axes output structure
receiving information, as a result of calling the MMC_MoveCircularAbsoluteRadius
function.

Remarks
The end-point and auxiliary point, depending on the applied mode, are defined absolutely in the specified
coordinate system.
Scope
Only MCS coordinate system is supported.
MOVECIRCULARABSOLUTERADIUS_IN Structure
typedef struct{
double dSpearHeadPoint[NC_MAX_NUM_AXES_IN_NODE];

### PDF page 658
<a id="pdf-page-658"></a>
double dEndPoint[NC_MAX_NUM_AXES_IN_NODE];
float fVelocity;
float fAcceleration;
float fDeceleration;
float fJerk;
float fTransitionParameter[NC_MAX_NUM_AXES_IN_NODE];
NC_PATH_CHOICE_ENUM ePathChoice;
NC_ARC_SHORT_LONG_ENUM eArcShortLong;
MC_COORD_SYSTEM_ENUM eCoordSystem;
NC_TRANSITION_MODE_ENUM eTransitionMode;
MC_BUFFERED_MODE_ENUM eBufferMode;
unsigned char ucSuperimposed;
unsigned char ucExecute;
}MMC_MOVECIRCULARABSOLUTERADIUS_IN;
Parameters
ucExecute
Start the execution command at the rising edge. Boolean TRUE/FALSE values.
dSpearHeadPoint [NC_MAX_NUM_AXES_IN_NODE]
An array [1..N] of absolute radius positions for each dimension in the coordinate system
specified by the input signal CoordSystem, with N being vendor specific. The array
parameter NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the
maximum number of axis in a group.
dSpearHeadPoint can have vector array [1....3] double values in a technical unit [u].
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
dEndPoint [NC_MAX_NUM_AXES_IN_NODE]
An array [1..N] of absolute end point positions for each dimension in the coordinate
system specified by the input signal CoordSystem, with N being vendor specific. The
array parameter NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the
maximum number of axis in a group.
dEndPoint is a 2D or 3D double vector array in technical unit [u].
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
fVelocity
Value of the maximum velocity (not necessarily reached) in which t he path is defined.
Any positive float value in u/s
fAcceleration
Value of the acceleration (increasing energy of the motor). Any positive float value in
u/s2.

### PDF page 659
<a id="pdf-page-659"></a>
fDeceleration
Float value of the deceleration when stopping (decreasing energy of the motor). Any
positive float value in u/s2
fJerk
Maximum float value of the Jerk. Any positive value in u/s3
fTransitionParameter [NC_MAX_NUM_AXES_IN_NODE]
Depending on the transition mode, different supplier specific transition parameters can
be used which characterize the contour curve. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
fTransitionParameter can have any positive float value in appropriate units, dependant
on the TransitionMode parameter. Refer to the section Coordinate System and
kinematic transformation.
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
ePathChoice
Defines the NC_PATH_CHOICE_ENUM enumerator types of supported path choice. The
option are:
MC_NONE_PATH_CHOICE = 0
MC_CLOCKWISE = 1
MC_COUNTERCLOCKWISE = 2
eArcShortLong
Defines the types of supported arc length. The NC_ARC_SHORT_LONG_ENUM
enumerator options are:
MC_NONE_ARC_CHOICE = 0
MC_SHORT = 1
MC_LONG = 2
eCoordSystem
Define the types of supported coordinate systems. The MC_COORD_SYSTEM_ENUM
enumerator options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
NC_TRANSITION_MODE_ENUM eTransitionMode

### PDF page 660
<a id="pdf-page-660"></a>
Define the supported NC_TRANSITION_MODE_ENUM enumerator transition modes.
Refer to the section Multiple Axes Motion Control - Transition and Buffer Modes and
options below. The options are:
MC_TM_NONE_MODE = 0,
MC_TM_MAX_VELOCITY_MODE = 1, Not supported at this time
MC_TM_DEFINED_VELOCITY_MODE = 2,
MC_TM_CORNER_DISTANCE_MODE = 3,
MC_TM_MAX_CORNER_DEVIATION_MODE = 4,
MC_TM_SWITCH_RADIUS_MODE = 5,
MC_TM_CORNER_DIST_TC_POLYNOM = 6, Not supported
MC_TM_CORNER_DIST_CV_POLYNOM3 = 7,
MC_TM_CORNER_DIST_CV_POLYNOM5 = 8,
MC_TM_CORNER_DEVIATION_MODE_PLN6 = 9,
MC_TM_CORNER_DIST_CV_POLYNOM5_NAXES = 10,
MC_TM_CORNER_DIST_CV_POLYNOM7 = 11,
MC_TM_CORNER_DEVIATION_MODE_PLN8 = 12,
MC_TM_DIST1_DIST2_DEVIATION_PLN6 = 13,
MC_TM_DIST1_DIST2_DEVIATION_PLN8 = 14,
MC_TM_LAST_MODE
MC_BUFFERED_MODE_ENUM eBufferMode
MC_BUFFERED_MODE_ENUM defines the behavior of the axis. The MC_BufferMode
defines the velocity at the transition point. It defines the chronological sequence of the
function block relative to the previous block. Enumerator modes are as follows:
MC_ABORTING_MODE = 1
MC_BUFFERED_MODE = 2
MC_BLENDING_LOW_MODE = 3
MC_BLENDING_PREVIOUS_MODE = 4
MC_BLENDING_NEXT_MODE = 5
MC_BLENDING_HIGH_MODE = 6
Aborting Default mode without buffering. The next function block aborts an
ongoing motion and the command affects the axis immediately.
The buffer is cleared. When Aborting is applied:
- All existing function blocks in the node queue are aborted
- The Transition velocity changes to zero, which in turn stops all
current motion, and executes the new motion from zero
velocity
- This motion will be executed regardless of the Boolean
ucExecute status which may be False(0) or True(1).
Buffered When Buffering is applied, the current function block is inserted to

### PDF page 661
<a id="pdf-page-661"></a>
the function block queue in the Maestro, and on completion of the
axis\vector execution of all function blocks in the queue (with the
exception of the current function block), then the current function
block will start its execution. In addition, the Transition velocity is
zero.
BlendingLow When Blending Low is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the smaller value between the previous and
current function blocks.
BlendingPreviou
s
When Blending Previous is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the previous function block.
BlendingNext When Blending Next is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the current function block.
BlendingHigh When Blending High is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the higher between the previous and the
current function blocks.
ucSuperimposed
Whether the option to superimpose is operated or not. Values accepted are Boolean
TRUE/FALSE.
MMC_MOVECIRCULARABSOLUTE_OUT Structure
typedef struct{
unsigned int uiHndl;
unsigned short usStatus;
short sErrorID;

### PDF page 662
<a id="pdf-page-662"></a>
}MMC_MOVECIRCULARABSOLUTE_OUT;
Parameters
uiHndl
Returned function block handle. Integer with any positive value
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 211 describes the function block for MMC_MoveCircularAbsoluteRadius as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 211: MMC_MoveCircularAbsoluteRadius function block
7.9.7.1 Function Block Code Example
int rc;
MMC_MOVECIRCULARABSOLUTERADIUS_IN stMoveCircularAbsRadius_in;
MMC_MOVECIRCULARABSOLUTE_OUT stMoveCircularAbs_out;
//
// Inserting the structure parameters:
stMoveCircularAbsRadius_in.fAcceleration = 100000.0;
//Value of the acceleration
stMoveCircularAbsRadius_in.fDeceleration = 100000.0;
//Value of the deceleration
stMoveCircularAbsRadius_in.fJerk = 20000000.0; //Maximum
value of the Jerk
stMoveCircularAbsRadius_in.dSpearHeadPoint[0] = 5000;//Absolute radius
positions for each dimension
stMoveCircularAbsRadius_in.dSpearHeadPoint[1] = 5000;//Absolute radius
positions for each dimension
stMoveCircularAbsRadius_in.fTransitionParameter[0] = 10000;
//Transition parameters which characterize the contour curve
stMoveCircularAbsRadius_in.fTransitionParameter[1] = 10000;
//Transition parameters which characterize the contour curve
stMoveCircularAbsRadius_in.ePathChoice = MC_CLOCKWISE;
//Supported path choice
stMoveCircularAbsRadius_in.eArcShortLong = MC_SHORT;//Defines
the types of supported arc length
stMoveCircularAbsRadius_in.eCoordSystem =
MC_MCS_COORD;//Supported coordinate system
stMoveCircularAbsRadius_in.eTransitionMode =
MC_TM_DEFINED_VELOCITY_MODE;//Supported transition modes

### PDF page 663
<a id="pdf-page-663"></a>
stMoveCircularAbsRadius_in.eBufferMode =
MC_BUFFERED_MODE;//Defines the behavior of the axis
stMoveCircularAbsRadius_in.dEndPoint[0] = 1000.0;//Absolute end
point positions
stMoveCircularAbsRadius_in.dEndPoint[1] = 1000.0;//Absolute end
point positions
stMoveCircularAbsRadius_in.fVelocity = 5000.0; //Maximum
Velocity of the path
stMoveCircularAbsRadius_in.ucSuperimposed = 1; //Option
to superimpose is operated
stMoveCircularAbsRadius_in.ucExecute = 1;
//
rc = MMC_MoveCircularAbsoluteRadiusCmd (hConn, iAxisRef,
&stMoveCircularAbsRadius_in, &stMoveCircularAbs_out);
if (rc != 0)
{
HandleError() ;
}

### PDF page 664
<a id="pdf-page-664"></a>
###### 7.9.8 MMC_MoveCircularAbsoluteAngle
7.9.8 MMC_MoveCircularAbsoluteAngle
For multi-axis systems. Commands an interpolated circular angle movement on an axes group from the
actual position of the TCP. The movement may be in either positive or negative direction without restriction.
MMC_LIB_API int MMC_MoveCircularAbsoluteAngleCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_MOVECIRCULARABSOLUTEANGLE_IN* pInParam,
OUT MMC_MOVECIRCULARABSOLUTE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MOVECIRCULARABSOLUTEANGLE group axes input structure that
receives the MMC_MoveCircularAbsoluteAngle command.
pOutParam
Points to the MMC_MOVECIRCULARABSOLUTE_OUT group axes output structure
receiving information, as a result of calling the MMC_MoveCircularAbsoluteAngle
function.
Remarks
The end-point and auxiliary point, depending on the applied mode, are defined absolutely in the specified
coordinate system.
Scope
Motion is only permitted in two dimensions. Only MCS coordinate system is supported.
MOVECIRCULARABSOLUTEANGLE_IN Structure
typedef struct{
unsigned char ucExecute;
double dCenterPoint[NC_MAX_NUM_AXES_IN_NODE];

### PDF page 665
<a id="pdf-page-665"></a>
double dAngle;
float fVelocity;
float fAcceleration;
float fDeceleration;
float fJerk;
float fTransitionParameter[NC_MAX_NUM_AXES_IN_NODE];
MC_COORD_SYSTEM_ENUM eCoordSystem;
NC_TRANSITION_MODE_ENUM eTransitionMode;
MC_BUFFERED_MODE_ENUM eBufferMode;
unsigned char ucSuperimposed;
}MMC_MOVECIRCULARABSOLUTEANGLE_IN;
Parameters
ucExecute
Start the execution command at the rising edge. Boolean TRUE/FALSE values.
dCenterPoint [NC_MAX_NUM_AXES_IN_NODE]
An array [1..N] of absolute positions for each dimension in the coordinate system
specified by the input signal CoordSystem, with N being vendor specif ic. The array
parameter NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the
maximum number of axis in a group.
dCenterPoint can have vector array [1....3] double values in a technical unit [u].
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
dAngle
Relative angular position for the coordinate system specified by the input signal
CoordSystem. Angular double value in degrees [u], which may be positive or negative
without restriction.
fVelocity
Value of the maximum velocity (not necessarily reached) in which the path is defined.
Any positive float value in u/s
fAcceleration
Value of the acceleration (increasing energy of the motor). Any positive float value in
u/s
2.
fDeceleration
Float value of the deceleration when stopping (decreasing energy of the motor). Any
positive float value in u/s2
fJerk
Maximum float value of the Jerk. Any positive value in u/s3
fTransitionParameter [NC_MAX_NUM_AXES_IN_NODE]

### PDF page 666
<a id="pdf-page-666"></a>
Depending on the transition mode, different supplier specific transition para meters can
be used which characterize the contour curve. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
fTransitionParameter can have any positive float value in appropriate units, dependant
on the TransitionMode parameter. Refer to the section Coordinate System and
kinematic transformation.
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
eCoordSystem
Define the types of supported coordinate systems. The MC_COORD_SYSTEM_ENUM
enumerator options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
NC_TRANSITION_MODE_ENUM eTransitionMode
Define the supported NC_TRANSITION_MODE_ENUM enumerator transition modes.
Refer to the section Multiple Axes Motion Control - Transition and Buffer Modes and
options below. The options are:
MC_TM_NONE_MODE = 0,
MC_TM_MAX_VELOCITY_MODE = 1, Not supported at this time
MC_TM_DEFINED_VELOCITY_MODE = 2,
MC_TM_CORNER_DISTANCE_MODE = 3,
MC_TM_MAX_CORNER_DEVIATION_MODE = 4,
MC_TM_SWITCH_RADIUS_MODE = 5,
MC_TM_CORNER_DIST_TC_POLYNOM = 6, Not supported
MC_TM_CORNER_DIST_CV_POLYNOM3 = 7,
MC_TM_CORNER_DIST_CV_POLYNOM5 = 8,
MC_TM_CORNER_DEVIATION_MODE_PLN6 = 9,
MC_TM_CORNER_DIST_CV_POLYNOM5_NAXES = 10,
MC_TM_CORNER_DIST_CV_POLYNOM7 = 11,
MC_TM_CORNER_DEVIATION_MODE_PLN8 = 12,
MC_TM_DIST1_DIST2_DEVIATION_PLN6 = 13,
MC_TM_DIST1_DIST2_DEVIATION_PLN8 = 14,
MC_TM_LAST_MODE
MC_BUFFERED_MODE_ENUM eBufferMode
MC_BUFFERED_MODE_ENUM defines the behavior of the axis. The MC_BufferMode
defines the velocity at the transition point. It defines the chronological sequence of the
function block relative to the previous block. Enumerator modes are as follows:

### PDF page 667
<a id="pdf-page-667"></a>
MC_ABORTING_MODE = 1
MC_BUFFERED_MODE = 2
MC_BLENDING_LOW_MODE = 3
MC_BLENDING_PREVIOUS_MODE = 4
MC_BLENDING_NEXT_MODE = 5
MC_BLENDING_HIGH_MODE = 6
Aborting Default mode without buffering. The next function block aborts
an ongoing motion and the command affects the axis
immediately. The buffer is cleared. When Aborting is applied:
- All existing function blocks in the node queue are aborted
- The Transition velocity changes to zero, which in turn stops
all current motion, and executes the new motion from zero
velocity
- This motion will be executed regardless of the Boolean
ucExecute status which may be False(0) or True(1).
Buffered When Buffering is applied, the current function block is inserted
to the function block queue in the Maestro, and on completion of
the axis\vector execution of all function blocks in the queue (with
the exception of the current function block), then the current
function block will start its execution. In addition, the Transition
velocity is zero.
BlendingLow When Blending Low is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block),
then the current function block will start its execution. However,
the Transition velocity is the smaller value between the previous
and current function blocks.
BlendingPrevious When Blending Previous is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block),
then the current function block will start its execution. However,
the Transition velocity is the velocity of the previous function
block.
BlendingNext When Blending Next is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block),
then the current function block will start its execution. However,
the Transition velocity is the velocity of the current function
block.

### PDF page 668
<a id="pdf-page-668"></a>
BlendingHigh When Blending High is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block),
then the current function block will start its execution. However,
the Transition velocity is the higher between the previous and
the current function blocks.
ucSuperimposed
Whether the option to superimpose is operated or not. Values accepted are Boolean
TRUE/FALSE.
MMC_MOVECIRCULARABSOLUTE_OUT Structure
typedef struct{
unsigned int uiHndl;
unsigned short usStatus;
short sErrorID;
}MMC_MOVECIRCULARABSOLUTE_OUT;
Parameters
uiHndl
Returned function block handle. Integer with any positive value
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the fu nction
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 212 describes the function block for MMC_MoveCircularAbsoluteAngle
[PDF field-code object omitted]
Figure 212: MMC_MoveCircularAbsoluteAngle function block

### PDF page 669
<a id="pdf-page-669"></a>
7.9.8.1 Function Block Code Example
int rc;
MMC_MOVECIRCULARABSOLUTEANGLE_IN stMoveCircularAbsAngle_in;
MMC_MOVECIRCULARABSOLUTE_OUT stMoveCircularAbs_out;
//
// Inserting the structure parameters:
stMoveCircularAbsAngle_in.fAcceleration = 100000.0; //Value of the
acceleration
stMoveCircularAbsAngle_in.fDeceleration = 100000.0; //Value of the
deceleration
stMoveCircularAbsAngle_in.fJerk = 20000000.0; //Maximum
value of the Jerk
stMoveCircularAbsAngle_in.dCenterPoint[0] = 5000;//Absolute center
positions for each dimension
stMoveCircularAbsAngle_in.dCenterPoint[1] = 5000;//Absolute center
positions for each dimension
stMoveCircularAbsAngle_in.fTransitionParameter[0] = 10000; //Transition
parameters which characterize the contour curve
stMoveCircularAbsAngle_in.fTransitionParameter[1] = 10000; //Transition
parameters which characterize the contour curve
stMoveCircularAbsAngle_in.dAngle = 165; //absolute positions for
each dimension in the coordinate system
stMoveCircularAbsAngle_in.eCoordSystem = MC_MCS_COORD;
//Supported coordinate system
stMoveCircularAbsAngle_in.eTransitionMode =
MC_TM_DEFINED_VELOCITY_MODE; //Supported transition modes
stMoveCircularAbsAngle_in.eBufferMode = MC_BUFFERED_MODE;
//Defines the behavior of the axis
stMoveCircularAbsAngle_in.fVelocity = 5000.0; //Maximum
Velocity of the path
stMoveCircularAbsAngle_in.ucSuperimposed = 1; //Option to
superimpose is operated
stMoveCircularAbsAngle_in.ucExecute = 1;
//
rc = MMC_MoveCircularAbsoluteAngleCmd (hConn, iAxisRef,
&stMoveCircularAbsAngle_in, &stMoveCircularAbs_out);
if (rc != 0)
{
HandleError();
}

### PDF page 670
<a id="pdf-page-670"></a>
###### 7.9.9 MMC_MoveAngle
7.9.9 MMC_MoveAngle
Allows a user to specify a certain plane for the arc motion, where the arc motion will only be performed in one of
the planes perpendicular to each other in space (XY, XZ, or YZ).
MMC_LIB_API int MMC_MoveAngle(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_MOVEANGLE_IN* pInParam,
OUT MMC_MOVEANGLE_OUT* pOutParam);

Source GMAS\includes\ MMC_PLCopen_group_API.h
Function Parameters
hConn
[IN] Connection handle input. Refer to Function Parameters at section Multi Axes
Common Data and Code for details
hAxisRef
[IN] Axis reference of specific group. Refer to Function Parameters at section
Multi Axes Common Data and Code for details
pInParam
Points to the MMC_MOVEANGLE_IN input structure receiving the
MMC_MoveAngle Information
pOutParam
Points to the MMC_MOVEANGLE_OUT output structure receiving information as a
result of calling the MMC_MoveAngle function.
Return value
Int Returned the state of the command. Refer to Return Value at section Multi
Axes Common Data and Code for details
Remarks
MoveAngle performs the arc motion only in one of the planes perpendicular to each other in space
(XY, XZ, or YZ).

### PDF page 671
<a id="pdf-page-671"></a>
Scope
MoveAngle works just after connection to Maestro and defining exist group. The specific group should
be enabled, and all group's axes are enabled in operation mode "Cyclic Position", and the status group
is equals to "Stand By".
Before running this command, please verify that the kinematic of specific group sets to 2d coordinate
system. For more details see Coordinate System and kinematic transformation
MoveAngle supported just MCS coordinate.
MoveAngle doesn't supported function block of Aborting mode.
MMC_MOVEEANGLE_IN Structure
typedef struct mmc_moveaangle_in
{
double dCenterPoint[MAX_CENTER_POINTS];
double dAngle;
double dHelixPos;
double dVelocity;
double dAcceleration;
double dDeceleration;
double dJerk;
double dTransitionParameter[NC_MAX_NUM_AXES_IN_NODE];
MC_COORD_AXES ePlain;
MC_COORD_SYSTEM_ENUM eCoordSystem;
NC_TRANSITION_MODE_ENUM eTransitionMode;
MC_BUFFERED_MODE_ENUM eBufferMode;
unsigned char ucSuperimposed;
unsigned char ucExecute;
unsigned char spare[30];
}MMC_MOVEANGLE_IN;

Parameters
dCenterPoint[MAX_CENTER_POINTS];
double array [1..N] of absolute positions of the center point in the circular for each
dimension in the coordinate system specified by the input signal CoordSystem, with N
being vendor specific. The array parameter MAX_CENTER_POINTS is limited to 16, and
defined as the maximum number of axis center points in a group.
dCenterPoint can have vector array [1....2] double values in a technical unit [u].

### PDF page 672
<a id="pdf-page-672"></a>
dAngle
double value of relative angular position for the coordinate system specified by the input
signal CoordSystem. Angular value in degrees [u], which may be positive or negative
without restriction.
dHelixPos
Double value parameter for optional use in the future
dVelocity
double value of the maximum velocity of the rotation of axis. Refer to Input Parameters at
section Multi Axes Common Data and Code for details.
dAcceleration
double value of the acceleration (increasing energy of the motor). Refer to Input
Parameters at section Multi Axes Common Data and Code for details.
dDeceleration
double value of the deceleration when stopping. Refer to Input Parameters at section
Multi Axes Common Data and Code for details.
dJerk
double value of the Jerk prior to acceleration. Any positive value in user units [u]. Refer to
Input Parameters at section Multi Axes Common Data and Code for details.
dTransitionParameter [NC_MAX_NUM_AXES_IN_NODE]
double array [1..N] of the axes values which characterize the contour curve depending on
the transition mode. The array parameter NC_MAX_NUM_AXES_IN_NODE is limited to 16,
and defined as the maximum number of axis in a group. [NC_MAX_NUM_AXES_IN_NODE]
is an array of values [2....15]. Refer to Input Parameters at section Multi Axes Common
Data and Code for details.
MC_COORD_AXES ePlain
The selected plane. If none-selected, is 0 for old method. This parameter is added to
support angle mode with more than two axes.
MC_COORD_SYSTEM_ENUM eCoordSystem
MC_COORD_SYSTEM_ENUM defines the types of supported coordinate systems. The
enumerator options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
Refer to Input Parameters at section Multi Axes Common Data and Code for details.

### PDF page 673
<a id="pdf-page-673"></a>
NC_TRANSITION_MODE_ENUM eTransitionMode
NC_TRANSITION_MODE_ENUM defines the supported enumerator transition modes. The
enumerator options are:
MC_TM_NONE_MODE = 0,
MC_TM_MAX_VELOCITY_MODE = 1, Not supported at this time
MC_TM_DEFINED_VELOCITY_MODE = 2,
MC_TM_CORNER_DISTANCE_MODE = 3,
MC_TM_MAX_CORNER_DEVIATION_MODE = 4,
MC_TM_SWITCH_RADIUS_MODE = 5,
MC_TM_CORNER_DIST_TC_POLYNOM = 6, Not supported
MC_TM_CORNER_DIST_CV_POLYNOM3 = 7,
MC_TM_CORNER_DIST_CV_POLYNOM5 = 8,
MC_TM_CORNER_DEVIATION_MODE_PLN6 = 9,
MC_TM_CORNER_DIST_CV_POLYNOM5_NAXES = 10,
MC_TM_CORNER_DIST_CV_POLYNOM7 = 11,
MC_TM_CORNER_DEVIATION_MODE_PLN8 = 12,
MC_TM_DIST1_DIST2_DEVIATION_PLN6 = 13,
MC_TM_DIST1_DIST2_DEVIATION_PLN8 = 14,
MC_TM_LAST_MODE
Refer to Input Parameters at section Multi Axes Common Data and Code for details.
MC_BUFFERED_MODE_ENUM eBufferMode
MC_BUFFERED_MODE_ENUM defines the behavior of the axis. Enumerator modes are as
follows :
MC_ABORTING_MODE = 1
MC_BUFFERED_MODE = 2
MC_BLENDING_LOW_MODE = 3
MC_BLENDING_PREVIOUS_MODE = 4
MC_BLENDING_NEXT_MODE = 5
MC_BLENDING_HIGH_MODE = 6
Refer to Input Parameters at section Multi Axes Common Data and Code for details.
ucSuperimposed
No value is specified indicating that option superpose mode is enabled or not. Entries
received are 0/1 1 for TRUE / 0 for FALSE.
Refer to Input Parameters at section Multi Axes Common Data and Code for details.
ucExecute
unsigned char value of start the execution command at the rising edge. Entries received are
0/1 1 for TRUE / 0 for FALSE.
Refer to Input Parameters at section Multi Axes Common Data and Code for details.
spare[30]
Together with previous two parameters is an alignment of double * 4

### PDF page 674
<a id="pdf-page-674"></a>
MMC_MOVEANGLE_OUT Structure
typedef MMC_MOVECIRCULARABSOLUTEEX_OUT MMC_MOVEANGLE_OUT;
typedef struct MMC_MOVECIRCULARABSOLUTEEX_OUT
{
unsigned int uiHndl;
unsigned short usStatus;
short usErrorID;
}MMC_MOVECIRCULARABSOLUTEEX_OUT;
Parameters
uiHndl
Returned function block handle. Integer with any positive value. Refer to Output
Parameters at section Multi Axes Common Data and Code for details
usStatus

Bitwise returned command status. Refer to Output Parameters at section Single Axes
Common Data and Code for details
usErrorID

Returned command error ID. Refer to Output Parameters at section Single Axes Common
Data and Code for details

7.9.9.1 Function Block Code Example

7.9.9.2 Implementation of Example
This sample illustrates used with MMC_ MoveCircularAbsoluteAngle function.
Note: Before running this sample make sure that the v1 group is present and contains two axes. In addition,
make sure that the position of the axes is at 0.
After connectiing to the Maestro, and defining the present group, enable the group.

The required header files are:
<stdio.h>
<stdlib.h>
<string.h>
"MMC_definitions.h"

### PDF page 675
<a id="pdf-page-675"></a>
###### 7.9.10 MMC_MoveLinearAbsolute
7.9.10 MMC_MoveLinearAbsolute
For multi-axis systems. Commands an interpolated linear movement on an axes group from the actual
position of the TCP to an absolute position in the specified coordinate system.
MMC_LIB_API int MMC_MoveLinearAbsoluteCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_MOVELINEARABSOLUTE_IN* pInParam,
OUT MMC_MOVELINEARABSOLUTE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_MOVELINEARABSOLUTE group axes input structure that receives
the MMC_MoveLinearAbsolute command.
pOutParam
Points to the MMC_MOVELINEARABSOLUTE_OUT group axes output structure
receiving information, as a result of calling the MMC_MoveLinearAbsolute function.
Remarks
None
Scope
All
MMC_MOVELINEARABSOLUTE_IN Structure
typedef struct{
unsigned char ucExecute;
double dbPosition[NC_MAX_NUM_AXES_IN_NODE];
float fVelocity;

### PDF page 676
<a id="pdf-page-676"></a>
float fAcceleration;
float fDeceleration;
float fJerk;
float fTransitionParameter[NC_MAX_NUM_AXES_IN_NODE];
MC_COORD_SYSTEM_ENUM eCoordSystem;
NC_TRANSITION_MODE_ENUM eTransitionMode;
MC_BUFFERED_MODE_ENUM eBufferMode;
unsigned char ucSuperimposed;
}MMC_MOVELINEARABSOLUTE_IN;
Parameters
ucExecute
Start the execution command at the rising edge. Boolean TRUE/FALSE values.
dbPosition [NC_MAX_NUM_AXES_IN_NODE]
An array [1..N] of absolute end positions for each dimension in the specified coordinate
system, with N being vendor specific. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
dbPosition is a vector array in technical unit [u].
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
fVelocity
Value of the maximum velocity (not necessarily reached) in which the path is defined.
Any positive float value in u/s
fAcceleration
Value of the acceleration (increasing energy of the motor). Any positive float value in
u/s
2.
fDeceleration
Float value of the deceleration when stopping (decreasing energy of the motor). Any
positive float value in u/s2
fJerk
Maximum float value of the Jerk. Any positive value in u/s3
fTransitionParameter [NC_MAX_NUM_AXES_IN_NODE]

### PDF page 677
<a id="pdf-page-677"></a>
Depending on the transition mode, different supplier specific transi tion parameters can
be used which characterize the contour curve. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
fTransitionParameter can have any positive float value in appropriate units, dependant
on the TransitionMode parameter. Refer to the section Coordinate System and
kinematic transformation.
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
eCoordSystem
Define the types of supported coordinate systems. The MC_COORD_SYSTEM_ENUM
enumerator options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
NC_TRANSITION_MODE_ENUM eTransitionMode
Define the supported NC_TRANSITION_MODE_ENUM enumerator transition modes.
Refer to the section Multiple Axes Motion Control - Transition and Buffer Modes and
options below. The options are:
MC_TM_NONE_MODE = 0,
MC_TM_MAX_VELOCITY_MODE = 1, Not supported at this time
MC_TM_DEFINED_VELOCITY_MODE = 2,
MC_TM_CORNER_DISTANCE_MODE = 3,
MC_TM_MAX_CORNER_DEVIATION_MODE = 4,
MC_TM_SWITCH_RADIUS_MODE = 5,
MC_TM_CORNER_DIST_TC_POLYNOM = 6, Not supported
MC_TM_CORNER_DIST_CV_POLYNOM3 = 7,
MC_TM_CORNER_DIST_CV_POLYNOM5 = 8,
MC_TM_CORNER_DEVIATION_MODE_PLN6 = 9,
MC_TM_CORNER_DIST_CV_POLYNOM5_NAXES = 10,
MC_TM_CORNER_DIST_CV_POLYNOM7 = 11,
MC_TM_CORNER_DEVIATION_MODE_PLN8 = 12,
MC_TM_DIST1_DIST2_DEVIATION_PLN6 = 13,
MC_TM_DIST1_DIST2_DEVIATION_PLN8 = 14,
MC_TM_LAST_MODE
MC_BUFFERED_MODE_ENUM eBufferMode
MC_BUFFERED_MODE_ENUM defines the behavior of the axis. The MC_BufferMode
defines the velocity at the transition point. It defines the chronological sequence of t he
function block relative to the previous block. Enumerator modes are as follows:

### PDF page 678
<a id="pdf-page-678"></a>
MC_ABORTING_MODE = 1
MC_BUFFERED_MODE = 2
MC_BLENDING_LOW_MODE = 3
MC_BLENDING_PREVIOUS_MODE = 4
MC_BLENDING_NEXT_MODE = 5
MC_BLENDING_HIGH_MODE = 6
Aborting Default mode without buffering. The next function block aborts an
ongoing motion and the command affects the axis immediately.
The buffer is cleared. When Aborting is applied:
- All existing function blocks in the node queue are aborted
- The Transition velocity changes to zero, which in turn stops all
current motion, and executes the new motion from zero
velocity
- This motion will be executed regardless of the Boolean
ucExecute status which may be False(0) or True(1).
Buffered When Buffering is applied, the current function block is inserted to
the function block queue in the Maestro, and on completion of the
axis\vector execution of all function blocks in the queue (with the
exception of the current function block), then the current function
block will start its execution. In addition, the Transition velocity is
zero.
BlendingLow When Blending Low is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the smaller value between the previous and
current function blocks.
BlendingPreviou
s
When Blending Previous is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the previous function block.
BlendingNext When Blending Next is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the current function block.
BlendingHigh When Blending High is applied, the current function block is
inserted to the function block queue in the Maestro, and on

### PDF page 679
<a id="pdf-page-679"></a>
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the higher between the previous and the
current function blocks.
ucSuperimposed
Whether the option to superimpose is operated or not. Values accepted are Boolean
TRUE/FALSE.
MMC_MOVELINEARABSOLUTE_OUT Structure
typedef struct{
unsigned int uiHndl;
unsigned short usStatus;
short sErrorID;
}MMC_MOVELINEARABSOLUTE_OUT;
Parameters
uiHndl
Returned function block handle. Integer with any positive value
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 213 describes the function block for MMC_MoveLinearAbsolute as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 213: MMC_MoveLinearAbsolute function block
7.9.10.1 Function Block Code Example
int rc;
MMC_MOVELINEARABSOLUTE_IN stMoveLinearAbs_in;
MMC_MOVELINEARABSOLUTE_OUT stMoveLinearAbs_out;
//
// Inserting the structure parameters:

### PDF page 680
<a id="pdf-page-680"></a>
stMoveLinearAbs_in.fAcceleration = 100000.0; //Value of the
acceleration
stMoveLinearAbs_in.fDeceleration = 100000.0; //Value of the
deceleration
stMoveLinearAbs_in.fJerk = 20000000.0; //Maximum value of
the Jerk
stMoveLinearAbs_in.dbPosition[0] = 5000; //Absolute position for
each dimension
stMoveLinearAbs_in.dbPosition[1] = 5000; //Absolute position for
each dimension
stMoveLinearAbs_in.fTransitionParameter[0]= 10000; //Transition
parameters which characterize the contour curve
stMoveLinearAbs_in.fTransitionParameter[1]= 10000; //Transition
parameters which characterize the contour curve
stMoveLinearAbs_in.eCoordSystem = MC_MCS_COORD; //Supported
coordinate system
stMoveLinearAbs_in.eTransitionMode =
MC_TM_DEFINED_VELOCITY_MODE;//Supported transition modes
stMoveLinearAbs_in.eBufferMode = MC_BUFFERED_MODE;//Defines the
behavior of the axis
stMoveLinearAbs_in.fVelocity = 5000.0; //Maximum Velocity of the
path
stMoveLinearAbs_in.ucSuperimposed = 1; //Option to
superimpose is operated
stMoveLinearAbs_in.ucExecute = 1;
//
rc = MMC_MoveLinearAbsoluteCmd (hConn, iAxisRef, &stMoveLinearAbs_in,
&stMoveLinearAbs_out);
if (rc != 0)
{
HandleError();
}
7.9.10.2 Implementation Example
[PDF field-code object omitted]
Figure 214: MMC_MoveLinearAbsolute - Example
[PDF field-code object omitted]
Figure 215: MMC_MoveLinearAbsolute timing diagram - Example
[PDF field-code object omitted]
Figure 216: Sequence of two complete motions (Done>Execute)

### PDF page 681
<a id="pdf-page-681"></a>
Figure 217: Second motion interrupts first motion

### PDF page 682
<a id="pdf-page-682"></a>
###### 7.9.11 MMC_MoveLinearRelative
7.9.11 MMC_MoveLinearRelative
For multi-axis systems. Commands an interpolated linear movement on an axes group from the actual
position of the TCP to a relative distance in the specified coordinate system.
MMC_LIB_API int MMC_MoveLinearRelativeCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_MOVELINEARRELATIVE_IN* pInParam,
OUT MMC_MOVELINEARRELATIVE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection ha ndle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_MOVELINEARRELATIVE group axes input structure that receives the
MMC_MoveLinearRelative command.
pOutParam
Points to the MMC_MOVELINEARRELATIVE_OUT group axes output structure receiving
information, as a result of calling the MMC_MoveLinearRelative function.
Remarks
None
Scope
All
MMC_MOVELINEARRELATIVE_IN Structure
typedef struct{
unsigned char ucExecute;
double dbDistance[NC_MAX_NUM_AXES_IN_NODE];
float fVelocity;
float fAcceleration;

### PDF page 683
<a id="pdf-page-683"></a>
float fDeceleration;
float fJerk;
float fTransitionParameter[NC_MAX_NUM_AXES_IN_NODE];
MC_COORD_SYSTEM_ENUM eCoordSystem;
NC_TRANSITION_MODE_ENUM eTransitionMode;
MC_BUFFERED_MODE_ENUM eBufferMode;
unsigned char ucSuperimposed;
}MMC_MOVELINEARRELATIVE_IN;
Parameters
ucExecute
Start the execution command at the rising edge. Boolean TRUE/FALSE values.
dbDistance [NC_MAX_NUM_AXES_IN_NODE]
An array [1..N] of relative distances for each dimension in the specified coordinate
system, with N being vendor specific. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
dbDistance is a vector array in technical unit [u].
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
fVelocity
Value of the maximum velocity (not necessarily reached) in which the path is defined.
Any positive float value in u/s
fAcceleration
Value of the acceleration (increasing energy of the motor). Any positive float value in
u/s2.
fDeceleration
Float value of the deceleration when stopping (decreasing energy of the motor). Any
positive float value in u/s
2
fJerk
Maximum float value of the Jerk. Any positive value in u/s3

### PDF page 684
<a id="pdf-page-684"></a>
fTransitionParameter [NC_MAX_NUM_AXES_IN_NODE]
Depending on the transition mode, different supplier specific transition parameters can
be used which characterize the contour curve. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
fTransitionParameter can have any positive float value in appropriate units, dependant
on the TransitionMode parameter. Refer to the section 7.1Coor dinate System and
kinematic transformation.
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
eCoordSystem
Define the types of supported coordinate systems. The MC_COORD_SYSTEM_ENUM
enumerator options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
NC_TRANSITION_MODE_ENUM eTransitionMode
Define the supported NC_TRANSITION_MODE_ENUM enumerator transition modes.
Refer to the section Multiple Axes Motion Control - Transition and Buffer Modes and
options below. The options are:
MC_TM_NONE_MODE = 0,
MC_TM_MAX_VELOCITY_MODE = 1, Not supported at this time
MC_TM_DEFINED_VELOCITY_MODE = 2,
MC_TM_CORNER_DISTANCE_MODE = 3,
MC_TM_MAX_CORNER_DEVIATION_MODE = 4,
MC_TM_SWITCH_RADIUS_MODE = 5,
MC_TM_CORNER_DIST_TC_POLYNOM = 6, Not supported
MC_TM_CORNER_DIST_CV_POLYNOM3 = 7,
MC_TM_CORNER_DIST_CV_POLYNOM5 = 8,
MC_TM_CORNER_DEVIATION_MODE_PLN6 = 9,
MC_TM_CORNER_DIST_CV_POLYNOM5_NAXES = 10,
MC_TM_CORNER_DIST_CV_POLYNOM7 = 11,
MC_TM_CORNER_DEVIATION_MODE_PLN8 = 12,
MC_TM_DIST1_DIST2_DEVIATION_PLN6 = 13,
MC_TM_DIST1_DIST2_DEVIATION_PLN8 = 14,
MC_TM_LAST_MODE
MC_BUFFERED_MODE_ENUM eBufferMode
MC_BUFFERED_MODE_ENUM defines the behavior of the axis. The MC_BufferMode

### PDF page 685
<a id="pdf-page-685"></a>
defines the velocity at the transition point. It defines the chronological sequence of the
function block relative to the previous block. Enumerator modes are as follows:
MC_ABORTING_MODE = 1
MC_BUFFERED_MODE = 2
MC_BLENDING_LOW_MODE = 3
MC_BLENDING_PREVIOUS_MODE = 4
MC_BLENDING_NEXT_MODE = 5
MC_BLENDING_HIGH_MODE = 6
Aborting Default mode without buffering. The next function block aborts an
ongoing motion and the command affects the axis immediately.
The buffer is cleared. When Aborting is applied:
- All existing function blocks in the node queue are aborted
- The Transition velocity changes to zero, which in turn stops all
current motion, and executes the new motion from zero
velocity
- This motion will be executed regardless of the Boolean
ucExecute status which may be False(0) or True(1)
Buffered When Buffering is applied, the current function block is inserted to
the function block queue in the Maestro, and on completion of the
axis\vector execution of all function blocks in the queue (with the
exception of the current function block), then the current function
block will start its execution. In addition, the Transition velocity is
zero.
BlendingLow When Blending Low is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the smaller value between the previous and
current function blocks.
BlendingPreviou
s
When Blending Previous is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the previous function block.
BlendingNext When Blending Next is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the current function block.

### PDF page 686
<a id="pdf-page-686"></a>
BlendingHigh When Blending High is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the higher between the previous and the
current function blocks.
ucSuperimposed
Whether the option to superimpose is operated or not. Values accepted are Boolean
TRUE/FALSE.
MMC_MOVELINEARRELATIVE_OUT Structure
typedef struct mmc_movealinearrelative_out{
unsigned int uiHndl;
unsigned short usStatus;
short sErrorID;
}MMC_MOVELINEARRELATIVE_OUT;
Parameters
uiHndl
Returned function block handle. Integer with any positive value
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 218 describes the function block for MMC_MoveLinearRelative as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 218: MMC_MoveLinearRelative function block

### PDF page 687
<a id="pdf-page-687"></a>
7.9.11.1 Function Block Code Example
int rc;
MMC_MOVELINEARRELATIVE_IN stMoveLinearRel_in;
MMC_MOVELINEARRELATIVE_OUT stMoveLinearRel_out;
//
// Inserting the structure parameters:
stMoveLinearRel_in.fAcceleration = 100000.0; //Value of the
acceleration
stMoveLinearRel_in.fDeceleration = 100000.0; //Value of the
deceleration
stMoveLinearRel_in.fJerk = 20000000.0; //Maximum
value of the Jerk
stMoveLinearRel_in.dbDistance[0] = 10000; //Absolute position
for each dimension
stMoveLinearRel_in.dbDistance[1] = 10000; //Absolute position
for each dimension
stMoveLinearRel_in.fTransitionParameter[0] = 10000;
//Transition parameters which characterize the contour curve
MoveLinearRel_in.fTransitionParameter[1] = 10000;
//Transition parameters which characterize the contour curve
stMoveLinearRel_in.eCoordSystem = MC_MCS_COORD;
//Supported coordinate system
stMoveLinearRel_in.eTransitionMode = MC_TM_DEFINED_VELOCITY_MODE;
//Supported transition modes
stMoveLinearRel_in.eBufferMode = MC_BUFFERED_MODE; //Defines
the behavior of the axis
stMoveLinearRel_in.fVelocity = 5000.0; //Maximum Velocity
of the path
stMoveLinearRel_in.ucSuperimposed = 1; //Option to
superimpose is operated
stMoveLinearRel_in.ucExecute = 1;
//
rc = MMC_MoveLinearRelativeCmd (hConn, iAxisRef, &stMoveLinearRel_in,
&stMoveLinearRel_out);
if (rc != 0)
{
HandleError();
}
7.9.11.2 Implementation Example
AxesGroup
Go
DistArray_1
3000
10
10
0
2000
100
100
0
Test
Finish
OR
First Second
DistArray_2
MC_MoveLinearRelative
AxesGroup AxesGroup
Execute Done
Velocity
Acceleration
Deceleration
Jerk
Error
ErrorID
CommandAborted
Busy
Active
CoordSystem
BufferMode
Distance
TransitionMode
TransitionParameter
MC_MoveLinearRelative
AxesGroup AxesGroup
Execute Done
Velocity
Acceleration
Deceleration
Jerk
Error
ErrorID
CommandAborted
Busy
Active
CoordSystem
BufferMode
Distance
TransitionMode
TransitionParameter
Aborting
TMDefinedVelocity
100%
MCSMCS

Figure 219: MMC_MoveLinearRelative - Example
[PDF field-code object omitted]
Figure 220: Timing diagram for MMC_MoveLinearRelative - Example

### PDF page 688
<a id="pdf-page-688"></a>
Timing diagram for example above (the dots on the red line are based on the same timing difference and
representing the velocity)
Sequenceof two complete motions
Starting
point
Second
point
First
point
X
Y
DistArray_2
DistArray_2
DistArray_1
DistArray_1
Velocity
3000
Velocity
2000

Figure 221: Sequence of two complete motions
[PDF field-code object omitted]
Figure 222: Sequence where second motion interrupts first motion

### PDF page 689
<a id="pdf-page-689"></a>
###### 7.9.12 MMC_MoveLinearAdditive
7.9.12 MMC_MoveLinearAdditive
For multi-axis systems. Commands an interpolated linear movement on an axes group from the actual
position of the TCP to an additive position in the specified coordinate system.
MMC_LIB_API int MMC_MoveLinearAdditiveCmd(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_MOVELINEARADDITIVE_IN* pInParam,
OUT MMC_MOVELINEARADDITIVE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Not Supported
Source GMAS\includes\MMC_PLCopen_group_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_MOVELINEARADDITIVE group axes input structure that receives
the MMC_MoveLinearAdditive command.
pOutParam
Points to the MMC_MOVELINEARADDITIVE_OUT group axes output structure receiving
information, as a result of calling the MMC_MoveLinearAdditive function.
Remarks
None
Scope
All
MMC_MOVELINEARADDITIVE_IN Structure
typedef struct{
unsigned char ucExecute
double dbDistance[NC_MAX_NUM_AXES_IN_NODE];
float fVelocity
float fAcceleration

### PDF page 690
<a id="pdf-page-690"></a>
float fDeceleration
float fJerk
float fTransitionParameter[NC_MAX_NUM_AXES_IN_NODE]
MC_COORD_SYSTEM_ENUM eCoordSystem
NC_TRANSITION_MODE_ENUM eTransitionMode
MC_BUFFERED_MODE_ENUM eBufferMode
unsigned char ucSuperimposed
}MMC_MOVELINEARADDITIVE_IN;
Parameters
ucExecute
Start the execution command at the rising edge. Boolean TRUE/FALSE values.
dbDistance [NC_MAX_NUM_AXES_IN_NODE]
An array [1..N] of relative distances for each dimension in the specified coordinate
system, with N being vendor specific. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
dbDistance is a vector array in technical unit [u].
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
fVelocity
Value of the maximum velocity (not necessarily reached) in which the path is defined.
Any positive float value in u/s
fDeceleration
Float value of the deceleration when stopping (decreasing energy of the motor). Any
positive float value in u/s2
fJerk
Maximum float value of the Jerk. Any positive value in u/s3
fTransitionParameter [NC_MAX_NUM_AXES_IN_NODE]
Depending on the transition mode, different supplier specific transition parameters can
be used which characterize the contour curve. The array parameter
NC_MAX_NUM_AXES_IN_NODE is limited to 16, and defined as the maximum number
of axis in a group.
fTransitionParameter can have any positive float value in appropriate units accuracy to
4 bytes only, dependant on the TransitionMode parameter. Refer to the se ction
Coordinate System and kinematic transformation.
[NC_MAX_NUM_AXES_IN_NODE] is an array of values [2....15].
eCoordSystem

### PDF page 691
<a id="pdf-page-691"></a>
Define the types of supported coordinate systems. The MC_COORD_SYSTEM_ENUM
enumerator options are:
MC_NONE_COORD = 0
MC_ACS_COORD = 1
MC_MCS_COORD = 2
MC_PCS_COORD = 3
NC_TRANSITION_MODE_ENUM eTransitionMode
Define the supported NC_TRANSITION_MODE_ENUM enumerator transition modes.
Refer to the section Multiple Axes Motion Control - Transition and Buffer Modes and
options below. The options are:
MC_TM_NONE_MODE = 0,
MC_TM_MAX_VELOCITY_MODE = 1, Not supported at this time
MC_TM_DEFINED_VELOCITY_MODE = 2,
MC_TM_CORNER_DISTANCE_MODE = 3,
MC_TM_MAX_CORNER_DEVIATION_MODE = 4,
MC_TM_SWITCH_RADIUS_MODE = 5,
MC_TM_CORNER_DIST_TC_POLYNOM = 6, Not supported
MC_TM_CORNER_DIST_CV_POLYNOM3 = 7,
MC_TM_CORNER_DIST_CV_POLYNOM5 = 8,
MC_TM_CORNER_DEVIATION_MODE_PLN6 = 9,
MC_TM_CORNER_DIST_CV_POLYNOM5_NAXES = 10,
MC_TM_CORNER_DIST_CV_POLYNOM7 = 11,
MC_TM_CORNER_DEVIATION_MODE_PLN8 = 12,
MC_TM_DIST1_DIST2_DEVIATION_PLN6 = 13,
MC_TM_DIST1_DIST2_DEVIATION_PLN8 = 14,
MC_TM_LAST_MODE
MC_BUFFERED_MODE_ENUM eBufferMode
MC_BUFFERED_MODE_ENUM defines the behavior of the axis. The MC_BufferMode
defines the velocity at the transition point. It defines the chronological sequenc e of the
function block relative to the previous block. Enumerator modes are as follows:
MC_ABORTING_MODE = 1
MC_BUFFERED_MODE = 2
MC_BLENDING_LOW_MODE = 3
MC_BLENDING_PREVIOUS_MODE = 4
MC_BLENDING_NEXT_MODE = 5
MC_BLENDING_HIGH_MODE = 6
Aborting Default mode without buffering. The next function block aborts an
ongoing motion and the command affects the axis immediately.

### PDF page 692
<a id="pdf-page-692"></a>
The buffer is cleared. When Aborting is applied:
- All existing function blocks in the node queue are aborted
- The Transition velocity changes to zero, which in turn stops all
current motion, and executes the new motion from zero
velocity
- This motion will be executed regardless of the Boolean
ucExecute status which may be False(0) or True(1)
Buffered When Buffering is applied, the current function block is inserted to
the function block queue in the Maestro, and on completion of the
axis\vector execution of all function blocks in the queue (with the
exception of the current function block), then the current function
block will start its execution. In addition, the Transition velocity is
zero.
BlendingLow When Blending Low is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the smaller value between the previous and
current function blocks.
BlendingPreviou
s
When Blending Previous is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the previous function block.
BlendingNext When Blending Next is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the velocity of the current function block.
BlendingHigh When Blending High is applied, the current function block is
inserted to the function block queue in the Maestro, and on
completion of the axis\vector execution of all function blocks in
the queue (with the exception of the current function block), then
the current function block will start its execution. However, the
Transition velocity is the higher between the previous and the
current function blocks.
ucSuperimposed
Whether the option to superimpose is operated or not. Values accepted are Boolean
TRUE/FALSE.

### PDF page 693
<a id="pdf-page-693"></a>
MMC_MOVELINEARADDITIVE_OUT Structure
typedef struct mmc_movealinearadditive_out{
unsigned int uiHndl;
unsigned short usStatus;
short sErrorID;
}MMC_MOVELINEARADDITIVE_OUT;
Parameters
uiHndl
Returned function block handle. Integer with any positive value
usStatus
Bitwise returned command status with the following values:
Aborted, Done, or CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Displays an error code as negative or positive integers.
Figure 223 describes the function block for MMC_MoveLinearAdditive as applied within the IEC 61131
programming.
[PDF field-code object omitted]
Figure 223: MMC_MoveLinearAdditive function block
