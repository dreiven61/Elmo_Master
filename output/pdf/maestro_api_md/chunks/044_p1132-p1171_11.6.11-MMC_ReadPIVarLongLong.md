# 11.6.11 MMC_ReadPIVarLongLong

- Source PDF: `Maestro Administrative and Motion API_2022_12_v2.012_bookmarks_fixed.pdf`
- PDF physical pages: 1132-1171
- Chunk: `044_p1132-p1171_11.6.11-MMC_ReadPIVarLongLong.md`

## Active Outline At Chunk Start
- p. 1093 - Chapter 11 Process Image(PI)
  - p. 1101 - 12.4 Recording Definitions and Parameters
    - p. 1132 - 11.6.11 MMC_ReadPIVarLongLong

## Contained Bookmark Outline
    - p. 1132 - 11.6.11 MMC_ReadPIVarLongLong
    - p. 1134 - 11.6.12 MMC_ReadPIVarULongLong
    - p. 1136 - 11.6.13 MMC_ReadPIVarDouble
    - p. 1138 - 11.6.14 MMC_ReadLargePIVarRaw
    - p. 1140 - 11.6.15 MMC_WritePIVarBool
    - p. 1143 - 11.6.16 MMC_WritePIVarChar
    - p. 1146 - 11.6.17 MMC_WritePIVarUChar
    - p. 1149 - 11.6.18 MMC_WritePIVarUShort
    - p. 1152 - 11.6.19 MMC_WritePIVarShort
    - p. 1155 - 11.6.20 MMC_WritePIVarUInt
    - p. 1158 - 11.6.21 MMC_WritePIVarInt
    - p. 1161 - 11.6.22 MMC_WritePIVarFloat
    - p. 1164 - 11.6.23 MMC_WritePIVarRaw
    - p. 1168 - 11.6.24 MMC_WritePIVarULongLong
    - p. 1170 - 11.6.25 MMC_WritePIVarLongLong

## Extracted Text

### PDF page 1132
<a id="pdf-page-1132"></a>
###### 11.6.11 MMC_ReadPIVarLongLong
Maestro Administrative and Motion API

11.6.11 MMC_ReadPIVarLongLong
This function reads a Processing Image input\output Long Long Variable according to its index
MMC_LIB_API int MMC_ReadPIVarLongLong(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_READPIVARLONGLONG_IN* pInParam,
OUT MMC_READPIVARLONGLONG_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_READPIVARLONGLONG input data structure using the
MMC_ReadPIVarRaw function.
pOutParam
Points to the MMC_READPIVARLONGLONG_OUT output structure receiving
information as a result of calling the MMC_ReadPIVarRaw function.
Remarks
The processing image defines the following:
PI_LONG_LONG_BITSIZE 64
Scope
All
MMC_READPIVARLONGLONG_IN Structure
typedef struct mmc_readpivarsignedlonglong_in{
unsigned short usIndex;
unsigned char ucDirection;
} MMC_READPIVARLONGLONG_IN;
Parameters

### PDF page 1133
<a id="pdf-page-1133"></a>
Maestro Administrative and Motion API

usIndex
The required index to be read from the processed image. Any positive integer values.
ucDirection
This variable has two possible PI directions, output or input according to the
enumerator values:
ePI_INPUT = 0,
ePI_OUTPUT = 1
MMC_READPIVARLONGLONG_OUT Structure
typedef struct mmc_readpivarsignedlonglong_out{
#ifdef WIN32
__int64 llData;
#else
long long llData;
#endif
unsigned short usStatus;
short sErrorID;
} MMC_READPIVARLONGLONG_OUT;
Parameters
__int64 llData or long long llData
The signed long long parameter requested from the processed image.
If the function is defined for WIN32 then use __int64 llData, else use llData.
Any positive, negative (Win32) or positive 64bit (8 bytes) character and/or integer.
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 366 describes the function for MMC_ReadPIVarLongLong.
[PDF field-code object omitted]
Figure 366: MMC_ReadPIVarLongLong function

### PDF page 1134
<a id="pdf-page-1134"></a>
###### 11.6.12 MMC_ReadPIVarULongLong
Maestro Administrative and Motion API

11.6.12 MMC_ReadPIVarULongLong
This function reads a Processing Image input\output Unsigned Long Long Variable according to its index
MMC_LIB_API int MMC_ReadPIVarULongLong(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_READPIVARULONGLONG_IN* pInParam,
OUT MMC_READPIVARULONGLONG_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_READPIVARULONGLONG input data structure using the
MMC_ReadPIVarULongLong function.
pOutParam
Points to the MMC_READPIVARULONGLONG_OUT output structure receiving
information as a result of calling the MMC_ReadPIVarULongLong function.
Remarks
The processing image defines the following:
PI_LONG_LONG_BITSIZE 64
Scope
All
MMC_READPIVARULONGLONG_IN Structure
typedef struct mmc_readpivarunsignedlonglong_in{
unsigned short usIndex;
unsigned char ucDirection;
} MMC_READPIVARULONGLONG_IN;
Parameters

### PDF page 1135
<a id="pdf-page-1135"></a>
Maestro Administrative and Motion API

usIndex
The required index to be read from the processed image. Any positive integer values.
ucDirection
This variable has two possible PI directions, output or input according to the
enumerator values:
ePI_INPUT = 0,
ePI_OUTPUT = 1
MMC_READPIVARULONGLONG_OUT Structure
typedef struct mmc_readpivarunsignedlonglong_out{
#ifdef WIN32
unsigned__int64 ullData;
#else
unsigned long long ullData;
#endif
unsigned short usStatus;
short sErrorID;
} MMC_READPIVARULONGLONG_OUT;
Parameters
unsigned__int64 ullData or unsigned long long ullData
The unsigned long long parameter requested from the processed image.
If the function is defined for WIN32 then use unsigned __int64 ullData, else use
unsigned long long ullData.
Any positive, negative (Win32) or positive 64bit (8 bytes) character and/or integer.
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections 4.3 Maestro Error IDs, and NC Profiler Error
IDs.
Figure 367 describes the function for MMC_ReadPIVarULongLong.
[PDF field-code object omitted]
Figure 367: MMC_ReadPIVarULongLong function

### PDF page 1136
<a id="pdf-page-1136"></a>
###### 11.6.13 MMC_ReadPIVarDouble
Maestro Administrative and Motion API

11.6.13 MMC_ReadPIVarDouble
This function reads a Processing Image input\output Double Variable according to its index
MMC_LIB_API int MMC_ReadPIVarDouble(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_READPIVARDOUBLE_IN* pInParam,
OUT MMC_READPIVARDOUBLE_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_READPIVARDOUBLE input data structure using the
MMC_ReadPIVarDouble function.
pOutParam
Points to the MMC_READPIVARDOUBLE_OUT output structure receiving information
as a result of calling the MMC_ReadPIVarDouble function.
Remarks
The processing image defines the following:
PI_ALIASING_LENGTH 35
Scope
All
MMC_READPIVARDOUBLE_IN Structure
typedef struct mmc_readpivardouble_in{
unsigned short usIndex;
unsigned char ucDirection;
} MMC_READPIVARDOUBLE_IN;
Parameters

### PDF page 1137
<a id="pdf-page-1137"></a>
Maestro Administrative and Motion API

usIndex
The required index to be read from the processed image. Any positive integer values.
ucDirection
This variable has two possible PI directions, output or input according to the
enumerator values:
ePI_INPUT = 0,
ePI_OUTPUT = 1
MMC_READPIVARDOUBLE_OUT Structure
typedef struct mmc_readpivardouble_out{
double dbData;
unsigned short usStatus;
short sErrorID;
} MMC_READPIVARDOUBLE_OUT;
Parameters
dbData
The double parameter requested from the processed image.
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 368 describes the function for MMC_ReadPIVarDouble.
[PDF field-code object omitted]
Figure 368: MMC_ReadPIVarDouble function

### PDF page 1138
<a id="pdf-page-1138"></a>
###### 11.6.14 MMC_ReadLargePIVarRaw
Maestro Administrative and Motion API

11.6.14 MMC_ReadLargePIVarRaw
This function reads a large Processing Image input\output RAW Variable according to its index, where the
variable > 32 bit.
MMC_LIB_API int MMC_ReadLargePIVarRaw(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_READLARGEPIVARRAW_IN* pInParam,
OUT MMC_READLARGEPIVARRAW_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_READLARGEPIVARRAW input data structure using the
MMC_ReadLargePIVarRaw function.
pOutParam
Points to the MMC_READLARGEPIVARRAW_OUT output structure receiving
information as a result of calling the MMC_ReadLargePIVarRaw function.
Remarks
The processing image defines the following:
PI_LARGE_VAR_SIZE 1400
Scope
All
MMC_READLARGEPIVARRAW_IN Structure
typedef struct mmc_readlargepivarraw_in{
unsigned short usIndex;
unsigned short usByteLength;
unsigned char ucDirection;
} MMC_READLARGEPIVARRAW_IN;

### PDF page 1139
<a id="pdf-page-1139"></a>
Maestro Administrative and Motion API

Parameters
usIndex
The required index to be read from the processed image. Any positive integer values.
ucDirection
This variable has two possible PI directions, output or input according to the
enumerator values:
ePI_INPUT = 0,
ePI_OUTPUT = 1
usByteLength
Length of the RAW data including the BitStart offset.
The BitStart offset is the PI variable bit offset Modulo 8, e.g. Bit Offset = 25 with the
BitStart = 1. The Byte length is (BitStart + Bit size) Modulo 8.
MMC_READLARGEPIVARRAW_OUT Structure
typedef struct mmc_readlargepivarraw_out{
unsigned short usStatus;
short sErrorID;
unsigned char pRawData[PI_LARGE_VAR_SIZE];
} MMC_READLARGEPIVARRAW_OUT;
Parameters
pRawData[PI_LARGE_VAR_SIZE]
The RAW parameter requested from the processed image. The byte size of the RAW
data is (BitStart + Bit size) Modulo 8, which is inserted as the ucByteLength.
Dependant on the variable type array PI_REG_VAR_SIZE which is a maximum of 4.
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 369 describes the function for MMC_ReadLargePIVarRaw.
[PDF field-code object omitted]
Figure 369: MMC_ReadLargePIVarRaw function

### PDF page 1140
<a id="pdf-page-1140"></a>
###### 11.6.15 MMC_WritePIVarBool
Maestro Administrative and Motion API

11.6.15 MMC_WritePIVarBool
This function writes a Processing Image input\output Boolean variable according to its index
MMC_LIB_API int MMC_WritePIVarBool(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARBOOL_IN* pInParam,
OUT MMC_WRITEPIVARBOOL_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARBOOL input data structure using the
MMC_WritePIVarBool function.
pOutParam
Points to the MMC_WRITEPIVARBOOL_OUT output structure receiving information as
a result of calling the MMC_WritePIVarBool function.
Remarks
The processing image defines the following:
PI_ALIASING_LENGTH 35
Scope
All
MMC_WRITEPIVARBOOL_IN Structure
typedef struct mmc_writepivarbool_in{
unsigned short usIndex;
unsigned char ucBOOL;
} MMC_WRITEPIVARBOOL_IN;

### PDF page 1141
<a id="pdf-page-1141"></a>
Maestro Administrative and Motion API

Parameters
ucBOOL
The Boolean parameter to be written to the processed image.
usIndex
The required index to be written to the processed image. Any positive integer values.
MMC_WRITEPIVARBOOL_OUT Structure
typedef struct mmc_writepivarbool_out{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEPIVARBOOL_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 370 describes the function for MMC_WritePIVarBool.
[PDF field-code object omitted]
Figure 370: MMC_WritePIVarBool function
11.6.15.1 Function Block Code Example
case BOOL_BITSIZE:
{
MMC_WRITEPIVARBOOL_IN WriteIn;
MMC_WRITEPIVARBOOL_OUT WriteOut;

WriteIn.usIndex = usIndex;

printf("enter a BOOL value:\n");
scanf("%hhu",&WriteIn.ucBOOL);

### PDF page 1142
<a id="pdf-page-1142"></a>
Maestro Administrative and Motion API

rc = MMC_WritePIVarBool(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarBool failed error %d",sErr);
PrintError(sErr);
break;
}

}
break;

### PDF page 1143
<a id="pdf-page-1143"></a>
###### 11.6.16 MMC_WritePIVarChar
Maestro Administrative and Motion API

11.6.16 MMC_WritePIVarChar
This function writes a Processing Image input\output Character variable according to its index
MMC_LIB_API int MMC_WritePIVarChar(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARCHAR_IN* pInParam,
OUT MMC_WRITEPIVARCHAR_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARCHAR input data structure using the
MMC_WritePIVarChar function.
pOutParam
Points to the MMC_WRITEPIVARCHAR_OUT output structure receiving information as
a result of calling the MMC_WritePIVarChar function.
Remarks
The processing image defines the following:
PI_ALIASING_LENGTH 35
PI_INT_BITSIZE 32
Scope
All
MMC_WRITEPIVARCHAR_IN Structure
typedef struct mmc_writepivarsignedchar_in{
unsigned short usIndex;
signed char cData;
} MMC_WRITEPIVARCHAR_IN;

### PDF page 1144
<a id="pdf-page-1144"></a>
Maestro Administrative and Motion API

Parameters
usIndex
The required index to be written to the processed image. Any positive integer values.
cData
The signed character parameter to be written to the processed image.
MMC_WRITEPIVARCHAR_OUT Structure
typedef struct mmc_writepivarsignedchar_out{
unsigned short usStatus;
short sErrorID;
}MMC_WRITEPIVARCHAR_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 371 describes the function for MMC_WritePIVarChar.
[PDF field-code object omitted]
Figure 371: MMC_WritePIVarChar function
11.6.16.1 Function Block Code Example

case CHAR_BITSIZE:
{
// check signess of the variable (signed\unsigned)
switch(ucVarType)
{
case ePI_SIGNED_CHAR:
{
MMC_WRITEPIVARCHAR_IN WriteIn;
MMC_WRITEPIVARCHAR_OUT WriteOut;
char cData = 0;

### PDF page 1145
<a id="pdf-page-1145"></a>
Maestro Administrative and Motion API

WriteIn.usIndex = usIndex;

printf("enter a signed CHAR value to be written to index %d:\n",usIndex);
scanf("%hhd",&cData);
WriteIn.cData = cData;

rc = MMC_WritePIVarChar(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarSignedChar failed error %d",sErr);
PrintError(sErr);
break;
}
}
break;

### PDF page 1146
<a id="pdf-page-1146"></a>
###### 11.6.17 MMC_WritePIVarUChar
Maestro Administrative and Motion API

11.6.17 MMC_WritePIVarUChar
This function writes a Processing Image input\output Unsigned Character variable according to its index
MMC_LIB_API int MMC_WritePIVarUChar(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARUCHAR_IN* pInParam,
OUT MMC_WRITEPIVARUCHAR_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is retu rned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARUCHAR input data structure using the
MMC_WritePIVarUChar function.
pOutParam
Points to the MMC_WRITEPIVARUCHAR_OUT output structure receiving information
as a result of calling the MMC_WritePIVarUChar function.
Remarks
The processing image defines the following:
PI_ALIASING_LENGTH 35
PI_REG_VAR_SIZE 4
Scope
All
MMC_WRITEPIVARUCHAR_IN Structure
typedef struct mmc_writepivarunsignedchar_in{
unsigned short usIndex;
unsigned char ucData;
} MMC_WRITEPIVARUCHAR_IN;

### PDF page 1147
<a id="pdf-page-1147"></a>
Maestro Administrative and Motion API

Parameters
usIndex
The required index to be written to the processed image. Any positive integer values.
ucData
The unsigned character parameter to be written to the processed image.
MMC_WRITEPIVARUCHAR_OUT Structure
typedef struct mmc_writepivarunsignedchar_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARUCHAR_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 372 describes the function for MMC_WritePIVarUChar.
[PDF field-code object omitted]
Figure 372: MMC_WritePIVarUChar function
11.6.17.1 Function Block Code Example
case ePI_UNSIGNED_CHAR:
{
MMC_WRITEPIVARUCHAR_IN WriteIn;
MMC_WRITEPIVARUCHAR_OUT WriteOut;
unsigned char ucData = 0;

WriteIn.usIndex = usIndex;

printf("enter an usigned CHAR value:\n");
scanf("%hhu",&ucData);
WriteIn.ucData = ucData;

### PDF page 1148
<a id="pdf-page-1148"></a>
Maestro Administrative and Motion API

printf("scanned the value %d, %hhu\n",ucData,ucData);

rc = MMC_WritePIVarUChar(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarUnsignedChar failed error %d",sErr);
PrintError(sErr);
break;
}
}
break;
default:
{
printf("someting is wrong\n");
}
break;
}
}

### PDF page 1149
<a id="pdf-page-1149"></a>
###### 11.6.18 MMC_WritePIVarUShort
Maestro Administrative and Motion API

11.6.18 MMC_WritePIVarUShort
This function writes a Processing Image input\output Unsigned Short variable according to its index
MMC_LIB_API int MMC_WritePIVarUShort(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARUSHORT_IN* pInParam,
OUT MMC_WRITEPIVARUSHORT_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARUSHORT input data structure using the
MMC_WritePIVarUShort function.
pOutParam
Points to the MMC_WRITEPIVARUSHORT_OUT output structure receiving information
as a result of calling the MMC_WritePIVarUShort function.
Remarks
The processing image defines the following:
PI_SHORT_BITSIZE 16
Scope
All
MMC_WRITEPIVARUSHORT_IN Structure
typedef struct mmc_writepivarunsignedshort_in{
unsigned short usData;
unsigned short usIndex;
} MMC_WRITEPIVARUSHORT_IN;
Parameters

### PDF page 1150
<a id="pdf-page-1150"></a>
Maestro Administrative and Motion API

usIndex
The required index to be written to the processed image. Any positive integer values.
usData
The unsigned short parameter to be written to the processed image.
MMC_WRITEPIVARUSHORT_OUT Structure
typedef struct mmc_writepivarunsignedshort_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARUSHORT_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 373 describes the function for MMC_WritePIVarUShort.
[PDF field-code object omitted]
Figure 373: MMC_WritePIVarUShort function
11.6.18.1 Function Block Code Example
case ePI_UNSIGNED_SHORT:
{
MMC_WRITEPIVARUSHORT_IN WriteIn;
MMC_WRITEPIVARUSHORT_OUT WriteOut;
unsigned short usData = 0;

WriteIn.usIndex = usIndex;

printf("enter an unsigned SHORT value to be written to index
%d:\n",usIndex);
scanf("%hu",&usData);

WriteIn.usData = usData;

printf("scanned the data %d, now writing to index
%d\n",WriteIn.usData,WriteIn.usIndex);

rc =
MMC_WritePIVarUShort(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)

### PDF page 1151
<a id="pdf-page-1151"></a>
Maestro Administrative and Motion API

{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarUnsignedShort failed error %d",sErr);
PrintError(sErr);
break;
}
}
break;
default:
{
printf("someting is wrong\n");
}
break;
}
}
break;

### PDF page 1152
<a id="pdf-page-1152"></a>
###### 11.6.19 MMC_WritePIVarShort
Maestro Administrative and Motion API

11.6.19 MMC_WritePIVarShort
This function writes a Processing Image input\output Short variable according to its index
MMC_LIB_API int MMC_WritePIVarShort(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARSHORT_IN* pInParam,
OUT MMC_WRITEPIVARSHORT_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARSHORT input data structure using the
MMC_WritePIVarShort function.
pOutParam
Points to the MMC_WRITEPIVARSHORT_OUT output structure receiving information as
a result of calling the MMC_WritePIVarShort function.
Remarks
The processing image defines the following:
PI_SHORT_BITSIZE 16
Scope
All
MMC_WRITEPIVARSHORT_IN Structure
typedef struct mmc_writepivarsignedshort_in{
short sData;
unsigned short usIndex;
} MMC_WRITEPIVARSHORT_IN;
Parameters

### PDF page 1153
<a id="pdf-page-1153"></a>
Maestro Administrative and Motion API

usIndex
The required index to be written to the processed image. Any positive integer values.
sData
The signed short parameter to be written to the processed image.
MMC_WRITEPIVARSHORT_OUT Structure
typedef struct mmc_writepivarsignedshort_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARSHORT_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections 4.3 Maestro Error IDs, and 4.6NC Profiler
Error IDs.
Figure 374 describes the function for MMC_WritePIVarShort.

MMC_WritePIVarShort
Execute
usStatus
Done, Error
usErrorID
Bitwise
Error code
Boolean
@Axis
usIndexUnsigned Short
sDataUnsigned Char

Figure 374: MMC_WritePIVarShort function
11.6.19.1 Function Block Code Example
case ePI_SIGNED_SHORT:
{
MMC_WRITEPIVARSHORT_IN WriteIn;
MMC_WRITEPIVARSHORT_OUT WriteOut;
short sData = 0;

WriteIn.usIndex = usIndex;

### PDF page 1154
<a id="pdf-page-1154"></a>
Maestro Administrative and Motion API

printf("enter a signed SHORT value to be written to index
%d:\n",usIndex);
scanf("%hd",&sData);

WriteIn.sData = sData;

printf("scanned the data %d, now writing to index
%d\n",WriteIn.sData,WriteIn.usIndex);

rc =
MMC_WritePIVarShort(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarSignedShort failed error %d",sErr);
PrintError(sErr);
break;
}
}

### PDF page 1155
<a id="pdf-page-1155"></a>
###### 11.6.20 MMC_WritePIVarUInt
Maestro Administrative and Motion API

11.6.20 MMC_WritePIVarUInt
This function writes a Processing Image input\output Unsigned Integer Variable according to its index
MMC_LIB_API int MMC_WritePIVarUInt(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARUINT_IN* pInParam,
OUT MMC_WRITEPIVARUINT_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is com mon
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARUINT input data structure using the
MMC_WritePIVarUInt function.
pOutParam
Points to the MMC_WRITEPIVARUINT_OUT output structure receiving information as a
result of calling the MMC_WritePIVarUInt function.
Remarks
The processing image defines the following:
PI_INT_BITSIZE 32
Scope
All
MMC_WRITEPIVARUINT_IN Structure
typedef struct mmc_writepivarunsignedint_in{
unsigned int uiData;
unsigned short usIndex;
} MMC_WRITEPIVARUINT_IN;
Parameters

### PDF page 1156
<a id="pdf-page-1156"></a>
Maestro Administrative and Motion API

usIndex
The required index to be written to the processed image. Any positive integer values.
uiData
The unsigned integer parameter to be written to the processed image.
MMC_WRITEPIVARUINT_OUT Structure
typedef struct mmc_writepivarunsignedint_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARUINT_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections 4.3 Maestro Error IDs, and 4.6NC Profiler
Error IDs.
Figure 375 describes the function for MMC_WritePIVarUInt.
MMC_WritePIVarUInt
Execute
usStatus
Done, Error
usErrorID
Bitwise
Error code
Boolean
@Axis
usIndexUnsigned Short
uiDataUnsigned Integer

Figure 375: MMC_WritePIVarUInt function

### PDF page 1157
<a id="pdf-page-1157"></a>
Maestro Administrative and Motion API

11.6.20.1 Function Block Code Example
case ePI_UNSIGNED_INT:
{
MMC_WRITEPIVARUINT_IN WriteIn;
MMC_WRITEPIVARUINT_OUT WriteOut;
unsigned int uiData = 0;

WriteIn.usIndex = usIndex;

printf("enter an unsigned INT value:\n");
scanf("%d",&uiData);

WriteIn.uiData = uiData;

printf("writing the value %d, to index
%d\n",WriteIn.uiData,WriteIn.usIndex);

rc =
MMC_WritePIVarUInt(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarUnsignedInt failed error %d",sErr);
PrintError(sErr);
break;
}
}
break;

### PDF page 1158
<a id="pdf-page-1158"></a>
###### 11.6.21 MMC_WritePIVarInt
Maestro Administrative and Motion API

11.6.21 MMC_WritePIVarInt
This function writes a Processing Image input\output Integer Variable according to its index
MMC_LIB_API int MMC_WritePIVarInt(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARINT_IN* pInParam,
OUT MMC_WRITEPIVARINT_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARINT input data structure using the MMC_WritePIVarInt
function.
pOutParam
Points to the MMC_WRITEPIVARINT_OUT output structure receiving information as a
result of calling the MMC_WritePIVarInt function.
Remarks
The processing image defines the following:
PI_INT_BITSIZE 32
Scope
All
MMC_WRITEPIVARINT_IN Structure
typedef struct mmc_writepivarsignedint_in{
int iData;
unsigned short usIndex;
} MMC_WRITEPIVARINT_IN;
Parameters

### PDF page 1159
<a id="pdf-page-1159"></a>
Maestro Administrative and Motion API

usIndex
The required index to be written to the processed image. Any positive integer values.
iData
The signed integer parameter to be written to the processed image.
MMC_WRITEPIVARINT_OUT Structure
typedef struct mmc_writepivarsignedint_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARINT_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 376 describes the function for MMC_WritePIVarInt.

MMC_WritePIVarInt
Execute
usStatus
Done, Error
usErrorID
Bitwise
Error code
Boolean
@Axis
usIndexUnsigned Short
iDataUnsigned Char

Figure 376: MMC_WritePIVarInt function
11.6.21.1 Function Block Code Example
case INT_BITSIZE:
{
// check signess of the variable (signed\unsigned)
switch(ucVarType)
{
case ePI_SIGNED_INT:
{
MMC_WRITEPIVARINT_IN WriteIn;

### PDF page 1160
<a id="pdf-page-1160"></a>
Maestro Administrative and Motion API

MMC_WRITEPIVARINT_OUT WriteOut;
int iData = 0;

WriteIn.usIndex = usIndex;

printf("enter a signed INT value:\n");
scanf("%d",&iData);

WriteIn.iData = iData;

rc =
MMC_WritePIVarInt(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarSignedInt failed error %d",sErr);
PrintError(sErr);
break;
}
}

### PDF page 1161
<a id="pdf-page-1161"></a>
###### 11.6.22 MMC_WritePIVarFloat
Maestro Administrative and Motion API

11.6.22 MMC_WritePIVarFloat
This function writes a Processing Image input\output Float Variable according to its index
MMC_LIB_API int MMC_WritePIVarFloat(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARFLOAT_IN* pInParam,
OUT MMC_WRITEPIVARFLOAT_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARFLOAT input data structure using the
MMC_WritePIVarFloat function.
pOutParam
Points to the MMC_WRITEPIVARFLOAT_OUT output structure receiving information as
a result of calling the MMC_WritePIVarFloat function.
Remarks
The processing image defines the following:
PI_ALIASING_LENGTH 35
PI_REG_VAR_SIZE 4
Scope
All
MMC_WRITEPIVARFLOAT_IN Structure
typedef struct mmc_writepivarfloat_in{
float fData;
unsigned short usIndex;
} MMC_WRITEPIVARFLOAT_IN;

### PDF page 1162
<a id="pdf-page-1162"></a>
Maestro Administrative and Motion API

Parameters
usIndex
The required index to be written to the processed image. Any positive integer values.
fData
The float parameter to be written to the processed image.
MMC_WRITEPIVARFLOAT_OUT Structure
typedef struct mmc_writepivarfloat_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARFLOAT_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 377 describes the function for MMC_WritePIVarFloat.
[PDF field-code object omitted]
Figure 377: MMC_WritePIVarFloat function
11.6.22.1 Function Block Code Example
case ePI_FLOAT:
{
MMC_WRITEPIVARFLOAT_IN WriteIn;
MMC_WRITEPIVARFLOAT_OUT WriteOut;
float fData = 0;

WriteIn.usIndex = usIndex;

printf("enter FLOAT value:\n");
scanf("%f",&fData);

WriteIn.fData = fData;

rc =
MMC_WritePIVarFloat(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

### PDF page 1163
<a id="pdf-page-1163"></a>
Maestro Administrative and Motion API

printf("MMC_WritePIVarFloat failed error %d",sErr);
PrintError(sErr);
break;
}
}break;
default:
{
printf("someting is wrong\n");
}

### PDF page 1164
<a id="pdf-page-1164"></a>
###### 11.6.23 MMC_WritePIVarRaw
Maestro Administrative and Motion API

11.6.23 MMC_WritePIVarRaw
This function writes a Processing Image input\output RAW Variable according to its index
MMC_LIB_API int MMC_WritePIVarRaw(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARRAW_IN* pInParam,
OUT MMC_WRITEPIVARRAW_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARRAW input data structure using the
MMC_WritePIVarRaw function.
pOutParam
Points to the MMC_WRITEPIVARRAW_OUT output structure receiving information as a
result of calling the MMC_WritePIVarRaw function.
Remarks
The processing image defines the following:
PI_ALIASING_LENGTH 35
PI_REG_VAR_SIZE 4
Scope
All
MMC_WRITEPIVARRAW_IN Structure
typedef struct mmc_writepivarraw_in{
unsigned short usIndex;
unsigned char ucByteLength;
unsigned char pRawData[PI_REG_VAR_SIZE];

### PDF page 1165
<a id="pdf-page-1165"></a>
Maestro Administrative and Motion API

} MMC_WRITEPIVARRAW_IN;
Parameters
usIndex
The required index to be written to the processed image. Any positive integer values.
pRawData[PI_REG_VAR_SIZE]
The RAW parameter to be written to the processed image. The byte size of the RAW
data is (BitStart + Bit size) Modulo 8, which is inserted as the ucByteLength.
Dependant on the variable type array PI_REG_VAR_SIZE which is a maximum of 4.
ucByteLength
Length of the RAW data including the BitStart offset.
The BitStart offset is the PI variable bit offset Modulo 8, e.g. Bit Offset = 25 with the
BitStart = 1. The Byte length is (BitStart + Bit size) Modulo 8.
MMC_WRITEPIVARRAW_OUT Structure
typedef struct mmc_writepivarraw_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARRAW_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.

### PDF page 1166
<a id="pdf-page-1166"></a>
Maestro Administrative and Motion API

Figure 378 describes the function for MMC_WritePIVarRaw.

MMC_WritePIVarRaw
Execute
usStatus
Done, Error
usErrorID
Bitwise
Error code
Boolean
@Axis
usIndexUnsigned Short
ucDirectionUnsigned Char
pRawData
[PI_REG_VAR_SIZE]
Unsigned Char

Figure 378: MMC_WritePIVarRaw function
11.6.23.1 Function Block Code Example
// 24 bits variables need to be written as raw data
case 24:
{
MMC_WRITEPIVARRAW_IN WriteIn;
MMC_WRITEPIVARRAW_OUT WriteOut;
unsigned char *pRawData = WriteIn.pRawData;
unsigned char ucBitStart = GetOut.VarInfo.uiBitOffset & 0x07;
unsigned int uiCount = ((GetOut.VarInfo.uiBitSize + ucBitStart - 1)
>> 3) + 1;

WriteIn.usIndex = usIndex;
WriteIn.ucByteLength = uiCount;

printf("writing the value 0x[1][2][3] to the variable\n");

pRawData[0] = 1;
pRawData[1] = 2;
pRawData[2] = 3;

rc = MMC_WritePIVarRaw(ConnHndl,hAxisRef[ucID],&WriteIn,&WriteOut);
if(rc)
{
short sErr = WriteOut.sErrorID;

printf("MMC_WritePIVarRaw failed error %d",sErr);
PrintError(sErr);
break;
}
}
}
}

printf("write? 0 - exit, 1 - write\n");
scanf("%hu",&usAns);
}
}

### PDF page 1167
<a id="pdf-page-1167"></a>
Maestro Administrative and Motion API

11.6.23.2 Function Block Code IEC XML Example 1
From within the EASII application, import the XML program piTestRAW.xml to the IEC projects on the IEC
Motion Programming page, and run the program.

Figure 379: IEC Function MMC_ReadPIVarRaw and MMC_WritePIVarRaw

### PDF page 1168
<a id="pdf-page-1168"></a>
###### 11.6.24 MMC_WritePIVarULongLong
Maestro Administrative and Motion API

11.6.24 MMC_WritePIVarULongLong
This function writes a Processing Image input\output Unsigned Long Long Variable according to its index
MMC_LIB_API int MMC_WritePIVarULongLong(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARULONGLONG_IN* pInParam,
OUT MMC_WRITEPIVARULONGLONG_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARULONGLONG input data structure using the
MMC_WritePIVarULongLong function.
pOutParam
Points to the MMC_WRITEPIVARULONGLONG_OUT output structure receiving
information as a result of calling the MMC_WritePIVarULongLong function.
Remarks
The processing image defines the following:
PI_LONG_LONG_BITSIZE 64
Scope
All
MMC_WRITEPIVARULONGLONG_IN Structure
typedef struct mmc_writelargepivarunsignedlonglong_in{
#ifdef WIN32
unsigned __int64 ullData;
#else
unsigned long long ullData;
#endif
unsigned short usIndex;

### PDF page 1169
<a id="pdf-page-1169"></a>
Maestro Administrative and Motion API

} MMC_WRITEPIVARULONGLONG_IN;
Parameters
usIndex
The required index to be written to the processed image. Any positive integer values.
unsigned __int64 ullData or unsigned long long ullData
The unsigned long long parameter to be written to the processed image.
If the function is defined for WIN32 then use unsigned __int64 ull Data, else use
unsigned long long ullData.
Any positive, negative (Win32) or positive 64bit (8 bytes) character and/or integer.
MMC_WRITEPIVARULONGLONG_OUT Structure
typedef struct mmc_writepivarunlonglong_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARULONGLONG_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 380 describes the function for MMC_WritePIVarULongLong.
[PDF field-code object omitted]
Figure 380: MMC_WritePIVarULongLong function

### PDF page 1170
<a id="pdf-page-1170"></a>
###### 11.6.25 MMC_WritePIVarLongLong
Maestro Administrative and Motion API

11.6.25 MMC_WritePIVarLongLong
This function writes a Processing Image input\output Long Long Variable according to its index
MMC_LIB_API int MMC_WritePIVarLongLong(
IN MMC_CONNECT_HNDL hConn,
IN MMC_AXIS_REF_HNDL hAxisRef,
IN MMC_WRITEPIVARLONGLONG_IN* pInParam,
OUT MMC_WRITEPIVARLONGLONG_OUT* pOutParam
);
Motion Mode NC - Supported Distributed - Supported
Source GMAS\includes\MMC_General_API.h
Parameters
hConn
[IN] Connection handle input using hConn, where MMC_CONNECT_HNDL is the
Connection Handle Type. It should be noted that this connection handle is common
throughout all Maestro functions. This connection handle is returned by the Init
Connection command. If an error occurs, the function returns -1 and a MMC_LIB_API
error with more details.
hAxisRef
Axis/group reference handle type returned by the GetAxisRef command
pInParam
Points to the MMC_WRITEPIVARLONGLONG input data structure using the
MMC_WritePIVarLongLong function.
pOutParam
Points to the MMC_WRITEPIVARLONGLONG_OUT output structure receiving
information as a result of calling the MMC_WritePIVarLongLong function.
Remarks
The processing image defines the following:
PI_LONG_LONG_BITSIZE 64
Scope
All
MMC_WRITEPIVARLONGLONG_IN Structure
typedef struct mmc_writepivarlonglong_in{
#ifdef WIN32
__int64 llData;
#else
long long llData;
#endif

### PDF page 1171
<a id="pdf-page-1171"></a>
Maestro Administrative and Motion API

unsigned short usIndex;
} MMC_WRITEPIVARLONGLONG_IN;
Parameters
usIndex
The required index to be written to the processed image. Any positive integer values.
__int64 ullData or long long ullData
The long long parameter to be written to the processed image.
If the function is defined for WIN32 then use __int64 ullData, else use long long ullData.
Any positive, negative (Win32) or positive 64bit (8 bytes) character and/or integer.
MMC_WRITEPIVARLONGLONG_OUT Structure
typedef struct mmc_writepivarlonglong_out{
unsigned short usStatus;
short sErrorID;
} MMC_WRITEPIVARLONGLONG_OUT;
Parameters
usStatus
Bitwise returned command status with the following values:
Aborted
Done
CommandError
sErrorID
Returned command error ID. Signals where an error has occurred within the function
block. Refer to the errors listed in sections Maestro Error IDs, and NC Profiler Error IDs.
Figure 381 describes the function for MMC_WritePIVarLongLong.
[PDF field-code object omitted]
Figure 381: MMC_WritePIVarLongLong function
